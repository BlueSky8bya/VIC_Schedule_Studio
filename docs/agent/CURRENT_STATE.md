# Current State — VIC Schedule Studio

Last Updated: 2026-09-16 (KST)
Project Version: 0.1.0

Current work only. Completed results live in topic records. Historical snapshots are evidence, not startup instructions.

## Current Objective

Current: [R80 marine concept activation](../ambient/rounds/ROUND-80-marine-concept-activation.md) connects saved open-sea and deep-sea images as static backgrounds, matching shallow. Owner requested application and push. Seasonal layers/wave implementation remains future work at the [resume handoff](handoffs/20260916-marine-biomes-resume.md). Deep weather/season isolation retained. Deployment unverified.

2026-09-17 UI unification shipped ([ADR-0025](decisions/ADR-0025-side-panel-unification.md)). VOD window redesign shipped same day and promoted to a page: viewer `/` opens `/replay/<date>` (`?part=N`), studio preview/fixture/onair keep the modal — both are one `DayVodWindow` (components/poster/day-vod-window.tsx): one stage + 부 tabs, title in header, ? key help, gold current chapter, horizontal timeline strip, chapter fold/jump, rail = side-panel width/side, ↑↓ [ ] Shift+←→ C keys, auto-next.

Style captures are now a working pipeline (ADR-0021): every one of the owner's 449 game captures under `art-src/공통화풍참고/` is indexed with a role, and `art:pipeline request <entity>` attaches the matching ones automatically. Pine is the next delivery and waits on the owner alone: name the reference PNGs to drop, then a new run requests variants 2·3 across the three seasons with accepted baselines + CC0 references + style captures frozen together. No new art generation without the owner's word. The remaining C choices in the approved [work plan](plans/PLAN-20260909-014-rule-preservation-and-art-completion.md) still await the owner.

## Active Work

| ID | Status | Next | Record |
|---|---|---|---|
| VIEWER-SEARCH | P0–P3 shipped (`c5620f6a` + follow-up) | Owner visual review on prod; nothing queued | [PLAN-023](plans/PLAN-20260918-023-viewer-search.md) |
| AMB-FOREST | Release authorized | Review fog sky boundary and entry keys | [R69](../ambient/rounds/ROUND-69-fog-sky-boundary.md) |
| STYLE-REF-GAMES | Shipped, extensible | New captures: drop into `공통화풍참고/<게임>/`, `style:scan --sheets`, classifier agent fills the worksheet, `style:apply`, `style:check` | [공통화풍참고 README](../../art-src/공통화풍참고/README.md), ADR-0021 |
| ART-PINE-PILOT | Awaiting Owner Curation | Owner names the reference PNGs to drop; delete those pairs, rebake the notice, create a new run and request variants 2·3 across the three seasons | [Pine entry point](../../art-src/나무/소나무/프롬프트.md) |
| RULE-ART-COMPLETION | Awaiting Owner | Apply the chosen review presentation and legacy-storage policy; commit B/C only if selected | [Work-order evidence](handoffs/20260909-work-order-results.md) |
| AMB-QA-18 | Planned | Reproduce prioritized candidates on one fixed build before selecting fixes | [Ambient QA](../ambient/QA_PROGRESS.md) |
| OPS-ANALYTICS-CLEANUP | Blocked | Verify current data/backup and establish authorization before production deletion | [Open checks](verification/OPEN_CHECKS.md) |
| VERIFY-PUBLIC-002-003 | Planned | Reconcile historical mobile insights and teaser preview issue labels with current evidence | [Open checks](verification/OPEN_CHECKS.md) |
| VERIFY-REAL-SESSION | Planned | Track genuine-login/RLS/device evidence separately from fixtures and service-role tests | [Open checks](verification/OPEN_CHECKS.md) |

## Blockers

- Owner choices pending: latest run's two sheets directly in 검토; approved-only 생성본 with the existing 40 legacy sources moved to 보존본 versus a provenance-labelled source archive; and B/C follow-up commits versus uncommitted review. The ≤12 size ratio applies within one entity's variants, per the owner's answer. Raw-archive implementation is tested; do not silently choose the remaining policy.
- No new pine images have been generated. The frozen `20260909-파일럿-01` request is untouched, but `manifest.ts` changed for the style-reference prompt kinds, so that run's source-contract hash no longer matches: it cannot be normalized or promoted and must be replaced by a new run (it was already planned to be). 원본/정리본 contain no delivery.
- Pine references are curated by the owner, not by an agent. All nine are still present and named subject-first; an earlier agent-chosen deletion of six was reverted in `33d0178d`. Ask which to drop and delete only those pairs. `request` freezes whatever remains in 레퍼런스/ as `inspiration` inputs, and a reference without a CC0 sidecar fails the request.
- Style captures are commercial game assets the owner placed deliberately. Their bytes and filenames stay as placed; classification lives only in `색인.json`. Stock/watermarked/other-game files inside those folders are `none` and never attach; moving one into an entity 레퍼런스/ needs a confirmed license and sidecar. Whether attaching commercial captures to the generator is acceptable was the owner's decision (2026-09-10); reviewers still check that no game asset is copied into a delivery.
- Production cleanup has no current execution or approval evidence. Old backup paths and row counts are historical; recheck before acting.
- ISSUE-002/003 have conflicting old labels and resolution claims. These are verification tasks, not confirmed current leaks. Fixture/service-role tests do not establish genuine-login/RLS/device coverage.

## Next Exact Steps

1. ART-PINE-PILOT: ask the owner which of the nine references to drop and wait. Show the pictures rather than the filenames, and label any sheet by name, never by an index number. On their word, delete only those image files, run `node scripts/ambient-ref-notice.mjs`, regenerate the pine catalogue, then `npm run art:pipeline -- request tree-pine --run <새 회차> --variants 2,3` (style captures attach automatically; `--no-style` to opt out) and hand the owner the frozen inputs plus `고정입력/화풍참고/화풍시트.png` to attach. Keep `20260909-파일럿-01` untouched.
2. STYLE-REF-GAMES follow-ups only when the owner asks: extend `분류어휘.json` groups if a new entity family has no group, add a new game folder the same way, or move a licensed stock image into an entity 레퍼런스/.
3. RULE-ART-COMPLETION: after the owner's answers, implement C1 and the chosen legacy-source placement/meaning, regenerate affected documents and run scoped checks.
4. AMB-QA-18: choose the next reproduction from the current QA state. Do not replay commands from completed rounds.
5. VERIFY-PUBLIC-002-003 and VERIFY-REAL-SESSION: close only with current scoped evidence.
6. OPS-ANALYTICS-CLEANUP: keep the production operation separate; current authorization and recoverable backup must precede deletion.

## Last Verified

2026-09-18 (timeline bar, third pass — no floating dots): the owner found the round knob and the floating peak dots cluttered and lightweight. Both circles are gone: the playhead is now a 2.5 px gold needle spanning the track with a soft light column behind it (the column widens on hover instead of a knob scaling), and reaction peaks are soft gold light columns that bloom behind the ribbon with a small rounded tab on the bottom corner rail (the tab stretches on hover). The played fill, active-corner capsule and peak columns were all toned down so the left side no longer reads as a highlighted block.
2026-09-18 (timeline bar, second pass): the owner rejected the bar-chart version as stiff and dated. The waveform is now a single smooth **ribbon**: ~90 samples are averaged, smoothed again, shaped by a relative-contrast curve (0.04 + (v/peak)^1.9) and drawn as one mirrored Catmull-Rom→Bézier path with a blue→violet gradient, a blurred glow copy underneath and a hairline crest. The track became glass (translucent gradient, blur, shadow-only edges, 18 px radius) that lifts and stretches on hover with a springy curve; ruler lines hide until hover and their labels moved below the bar; chapter capsules moved to a thin bottom row (active one glows gold and all of them stretch on hover); the 98 item ticks were removed (a picket fence over the ribbon); peaks became slowly breathing gold sparks; the playhead is a hairline with a glowing knob that grows on hover.
2026-09-18 (timeline bar rebuilt): the strip under the player is now a five-layer instrument, 54 px tall, still matched to SOOP's progress track (20 px insets). ① an hour ruler whose labels follow the 경과/실제 시각 toggle (오후 6시 vs 3시간; step 0.5/1/2/3 h by length), ② chapter capsules along the top edge (gaps between corners, the current corner turns gold, all capsules grow on hover), ③ a mirrored waveform of chat reaction (up to 150 bars, height = messages, opacity = laughter) replacing the old area+line chart, ④ a warm "played" fill driven by the same timeUpdate signal as the playhead, ⑤ item ticks plus gold ✦ sparks for reaction peaks (the old peak label pills are gone — their text moved into the hover tooltip, which now also carries the wall clock). Above the bar sits a now-playing line (corner · chapter · time) that follows playback. Gotcha: rewriting the `.vch-strip` CSS region wiped the `.ui-pill`/`.vch-topbar` block that had been inserted there minutes earlier — restored and re-verified (pill height 24 px, caret rotation applied).
2026-09-18 (replay + search visual language): badges across both surfaces now share one pill language — `.ui-pill` (24 px, 999 px radius, 11.5 px/800, 12 px icon, translucent white + hairline; variants `is-quiet`, `is-gold`, `is-tap`/`is-on`). The replay rail header was three wrapping lines (counts, credit, clock toggle, laugh badge) and is now a fixed two-line `.vch-topbar`: a title row ("타임라인 98 · 챕터 4" with a rotating caret) and a pill row (경과/실제 시각 toggle, 많이 웃은 방송, the fan credit as a heart pill). Section counts became badges, strip peak labels, the 합방 host chip, search chips and the result row side text all adopted the same pill treatment, and the rail list gained a bottom fade. Gotcha recorded: `.vch-head` was already the strip playhead class — the new wrapper is `.vch-topbar` (the collision rendered the header as a 4 px absolutely-positioned column).
2026-09-18 (tag editor motion + stable height): the segment switch used a spring with overshoot and rows popped in with a scale — replaced by the deceleration curve cubic-bezier(0.32, 0.72, 0, 1) (thumb 0.46 s, rows 4 px travel, 10 ms stagger, no scale). The modal also grew and shrank between 콘텐츠 (13 rows) and 형식 (1 row); the scroll area now has a fixed `clamp(220px, 52vh, 560px)` height (mobile `clamp(200px, 46svh, 460px)`) so the card height is identical across segments (measured 739 px in all three states) and long lists scroll inside.
2026-09-19 (settings window: one source, not two): the first pass gave the viewer its own `pi-*` skin because studio-shell.css is 421 KB and cannot load on the public page — owner rejected that outright ("왜 자꾸 새롭게 만들어서 장기적으로 관리하기 귀찮게 해"), and was right: it was a second design to maintain. The settings window's styles now live once in `components/shared/settings-modal.css`, imported by `studio-settings.tsx` so every surface that renders the list gets them. Every rule is scoped under `.vic-settings` (the window root) — two-class specificity, so it beats the generic `.rhh-*` rules still in studio-shell.css (the mobile role-badge popover needs those) no matter the load order, and the same file is the whole story on the public page. Both surfaces now render the identical markup: `.modal-backdrop.modal-backdrop-settings.vic-settings > .modal-card.modal-card-settings > .modal-head + .settings-modal-body`. The old window-only blocks (`.modal-card-settings`, `.settings-modal-body*`, `.rhh-dev-tag`, the settings `.rhh-link`) were deleted from studio-shell.css, and the viewer skin was deleted from public-poster.css. Values were copied from the browser's computed style on the studio window before the move, and a side-by-side probe now reports identical card/head/body/row/label/switch/select values on both. The gear also gets 14 px of left margin (it sat flush against the viewport edge).
2026-09-19 (settings on the viewer surfaces + stronger dirty highlight): the settings switches lived only in StudioShell, so giving the viewer screens (preview, viewer, anonymous) a gear meant a second copy — the state is now one shared hook `useSettingsPrefs()` (components/shared/use-settings-prefs.ts: haptics, 생동감 있는 동작, 눈 편한 테마, 계절 배경, 배경 효과; StudioShell keeps only its gfx-auto toast), and both surfaces render the same `StudioSettingsList`. The gear sits at the left end of the header line that already carries 검색/기록/로그인 and opens a `pi-*` sheet; the viewer skin for the switch rows lives in public-poster.css under `.pi-sheet-settings` because studio-shell.css is 421 KB and must not load on the public page (the seasonal segment needs nothing — app/ambient.css is global). Viewer shows 4 rows, studio 5 (포스터 테마 is owner-only); toggling verified on both. Editor popover dirty highlight: owner said a bolder dashed stroke still didn't read as a highlight. The hint is now three layers in the card's own mode colour (no new hue) — a 14 px halo that breathes, a 5 px opaque core, and a white 16 px spark that slides down the line every 2.6 s — and the whole ring firms up while dirty (colour alpha 40% → 68%, stroke 2 → 2.4 via `data-dirty` on the svg; never svg-wide opacity, which would dim the marker too). It also follows the pointer: while the save tab is hidden the lit segment springs to the cursor's height and jumps to whichever side of the card the cursor is on, with no dwell/speed gate (it is a signal, not a button). Verified in the fixture: `M 4 105 V 201` on the left → `M 368 334 V 430` on the right as the cursor moved.
2026-09-18 (clock badge before the first chapter): the '지금 장면' bar only rendered once playback had reached a timeline entry, so the time badge was missing for everything before the first chapter (often 10+ minutes). The bar now always renders when the strip does; before the first entry it drops the dot and label and keeps only the right-aligned time, which reads the broadcast's start wall clock (verified: 오후 5:30 in clock mode, 0:00 in elapsed mode, then 오후 5:48 / 16:06 with the chapter label after a strip click).
2026-09-18 (replay boot path rewritten + season-3 pass): owner reported that clicking through the timeline while starting playback sometimes loaded forever or took very long. Cause found in the two-slot player orchestration: **every** pre-playback chapter click called `promoteDayVodStandby`, which swapped a↔b and reloaded the retiring iframe — so each click destroyed the player that was about to become ready, and fast clicking livelocked. Three structural fixes: (1) clicks during boot no longer re-boot — they record `wantSec` and are flushed as a single `PseekTo` when the player settles (`flushDayVodWantSec`, armed off the first timeUpdate); (2) `bootDayVod` starts on the *current* slot when it has not spent its first Pload (window open with `?t=`, first click before PonReady), and `commitDayVodSwap` no longer reloads a retiring slot that is still pristine; (3) a generic boot watchdog (6 s × 2 tries, then a notice) replaces the situation where a muted boot that never rolled left the ▶ cover up forever — and the sound watchdog now retries at the *latest* clicked position. The standby iframe is also mounted only after the active slot's PonReady (or 2.5 s), so opening no longer loads two heavy SOOP players at once. Measured with Playwright (5 chapter clicks, 700 ms apart): embed frame loads 16 → 8, cover lift after the last click 1011 ms → 1 ms; at 220 ms apart, 14 → 7. Season-3 design pass: the search sheet's purple accent (rgb 124 108 240 — a hue found nowhere else) is retired for the app's blue #3564e0 + gold #d9a21b, the sheet surface is opaque (at 96% the blurred calendar behind tinted chips mint/yellow/blue depending on what sat behind them — same chip, different colour), chips are one white language with a small leading dot for kind, rows lift with a 1.05 thumbnail pull and keyboard cursor gets a blue left bar, the sheet is 680 → 760 px; the replay *page* now spends its leftover height on the rail (a third grid row, 1440×950 had ~190 px dead) and a 68 px strip, and rail rows grow a blue left bar on hover instead of filling with a blue plate.
2026-09-18 (replay window keys + live wall clock): in the studio, ←/→ inside the replay window paged the calendar month behind it instead of seeking — the viewer poster has guarded that with `dayVodOpenRef` since 2026-09-03, but studio-shell's month-nav and main shortcut handlers only checked `overlayLocked`, and neither the replay window nor the search sheet is in `overlayDepth`. Both handlers now bail on `overlayKeysBlockedRef` (dayVodPop or searchOpen), so the window owns ←/→ (±10 s seek), Space, C, [ ] and ↑/↓ while it is open. The "지금 장면" bar's time badge showed the *chapter's* start time; it now tracks the playhead — the time subscription writes `.vch-now-time` by textContent (same imperative path as the playhead and the played fill, 4×/s, no React render), the 경과↔실제 시각 toggle rewrites it immediately from the last seen second, and the subscription no longer early-returns for VODs without timeline entries (the head and fill were dead there). Verified on /replay/2026-09-17: chapter jumps moved the badge 11:58 → 34:13 → 56:17 and the toggle flipped it to 오후 6:26; tsc/lint/build clean.
2026-09-18 (search word boundaries, 0113/0114): owner searched "와우" and got "뱅온가왕 준우승자 정체와 우승자분 무대". `search_norm` deletes whitespace **and** punctuation, so any short query can bridge a word boundary once normalised ("정체와 우승자" → "정체와우승자"). 0091's `short2` guard only fired when the query had exactly one alternative, so every word with a synonym (와우 = wow = 월드오브워크래프트) went unguarded. The guard is now per-alternative, not per-query: 2-syllable Hangul alternatives must appear **contiguously in the raw text** (0113), and — found by sweeping the corpus for the same class — 3+-syllable Hangul alternatives must start at a **word head** in the raw text (0114: `search_word_start_re`, `(^|[^가-힣])스[^가-힣]*토[^가-힣]*리`), which kills 스토리→"엘레강스 토리", 정하기→"수정 하기", 토리의→"빅토리 의상" while keeping "첫 번째"/"게임 시작"/"사이다 뱅~" (space-only spellings of the same word) and "두 번째도"/"토리님도" (trailing particles) — the rule anchors the start, never the end. Related terms (`rel`) follow the same three-way split, and all hit sites now go through one inlined `search_hit(norm, raw, o, s, h, hb)`. 2-syllable queries also stop feeding the trigram similarity floor (one trigram passes 0.25/0.3 almost anywhere). Corpus sweep: 2-char bridging was 874 rows over 239 words, 3+-char misaligned bridging ~40 rows over the top 300 words. Verified: 20-query regression battery byte-identical to before except the junk rows, and latency unchanged (토리 1361 ms on 0098 → 920 ms on 0114, 게임/와우/노래 within noise).
2026-09-18 (tag editor overhaul): owner said the tag name/colour modal had no consistent rhythm and that adding a 콘텐츠/형식 meant scrolling up and down past the other section. Rebuilt around a fixed header: an iOS-style segmented control (콘텐츠 N | 형식 N, sliding thumb, accent flips to amber for 형식), an always-visible **추가** button that adds to the active segment, and the four tip lines collapsed behind a "?" disclosure. Only the active segment's rows render, the list is a responsive multi-column grid (the modal's scroll wrapper is no longer 2-col, which had trapped the list in half the width), rows animate in staggered, a newly added row flashes a ring, scrolls into view and selects its name field, and a one-line lede says what the current kind does on the calendar. Classes are namespaced `.tge-*` with tokens on `.tge`.
2026-09-18 (wall clock format): 24-hour "17:40" looked identical to an elapsed "10:34" (m:ss), so the wall clock now renders in Korean 12-hour form ("오후 5:40", past midnight "오전 1:07") and the rail carries `data-clock` so the timecode column widens to 74px and switches ink colour in that mode.
2026-09-18 (chat backfill done + wall clock + rail hit area): the chat backfill finished — 383/383 VODs, 749,705 term rows, 123,878 30-second bins, 4,383 person rows. Its final rebuild RPCs failed through PostgREST ("DELETE requires a WHERE clause"), so rebuilds are run over the direct connection (`scripts/apply-db.mjs`) instead; `search_trending` then timed out against 750k rows, so 0112 precomputes it into `search_trends` via `search_trending_rebuild()` (read path 60 ms) and both pipeline tick and the vod-chat cron call that function. Replay window: VOD wall-clock start is derived as `reg_date − duration_ms` (matches `broadcast_session.started_at` to the minute in 11/12 samples, available for all 383 VODs) and shipped as `PublicVodEntry.startedAt`; the chapter rail gets a 경과 ↔ 실제 시각 toggle (localStorage `vic.vod.clock`, verified 10:39 → 17:40) and the strip tooltip appends the wall clock. Search sheet date rail: hit area 14×6 → 24×21 px and every dot carries a month tip shown on hover (scrolling still shows only the current one).
2026-09-18 (sub-chapter rail): the parent heading and its child chapter rows sat at 6px vs 8px left margin with a 4px margin gap, so the purple rail looked broken rather than deliberately indented. Both now share `--ps-rail`/`--ps-sub-pad` tokens on `.ps-chapters` (rail at 10px, text at 14px), vertical spacing moved from margin to padding so the line is continuous (verified: parent and child left edges both 379px, gap 0), and the rail colour was strengthened to 45% alpha.
2026-09-18 (suggestion quality per research brief, 0104–0109): a literature survey (Google/YouTube autocomplete, Cai & de Rijke QAC survey, Bhatia et al. log-less suggestion, Bouma NPMI, Algolia rules) was distilled into rules now in `search_suggest`: candidates are a corpus phrase dictionary (titles/chapters/events = `search_doc_words`, refreshed by the term-graph rebuild) plus entities/games/genres/seed synonyms; chat-only words never appear in autocomplete (only as related chips after a search); related-tier seeds must themselves be corpus words/entities/games; a "too common" cap, adverb/interjection stop list, particle-ending regex and inflection collapse (hash-join CTE — the per-row version took 37 s) remove junk; streamer aliases appear only when actually used (≥3 broadcasts) and at most 3 per person; person resolution for "함께" chips uses syn synonyms only (auto-rel co-occurrence had resolved 마최 to 샬롯); the person graph merges alias names into the most-mentioned canonical (마최→아마최, 젠황→젠투). UI: suggestion dropdown is opaque and z-indexed above the sticky toolbar (text no longer bleeds through), the sheet header keeps its rounded corners while the dropdown is open, and the document scroll is locked while the sheet is open.
2026-09-18 (streamer aliases from the web, 0100–0102): owner rule — streamer aliases should be researched online, not only owner-supplied. Three research agents covered every entity with docs≥3 (namu.wiki, 왁타버스 wiki, community); results were filtered (no generic-word nicknames like 버거/왕자/공주/이모/함장, no 1-char/number aliases) and seeded as `search_synonyms` source `seed-web` (102 groups, 2,046 directed pairs), then `search_graph_rebuild` + `search_person_typos_rebuild`. Owner-given: 진희=진솔, 젠황=젠투=사랑전도사 젠투, 아마최=마최=아마데우스 최 (0100/0101). Memory `vic-slang-dictionary` holds the standing instruction. False positives are fixed by deleting the pair.
2026-09-18 (suggestions v2, 0099): the suggestion list is YouTube-like — direct prefix/contains matches first, then up to 8 filled with neighbours of the seeds (the query itself plus its top-3 direct matches) from `search_term_relations`/curated synonyms (kind `related`, shown with a › icon and "관련"), filtered by an adverb/interjection stop list, a "too common" cap (docs > 12% of max) and a verb-ending regex unless the term is a known game/person/seed. The list now overlays the header (absolute, animated) so the sheet height no longer jumps with the number of suggestions (verified: sheet height constant while typing).
2026-09-18 (search: Enter-to-search + suggestions, perf, correction guard, 0096–0098): the sheet no longer searches on every keystroke — typing fetches cheap suggestions (`search_suggest`: prefix-first over search_terms/entities/games/genres/seed synonyms, 초성 aware, inflected forms filtered; ~100ms) shown under the input with ↑↓/Enter/click, and the heavy `search_public` runs only on Enter, chip or suggestion pick (`submitted` state; re-Enter on the same word retries). Three real causes of "검색이 잠시 안 돼요"/empty results were found: (1) PostgREST schema cache lagging after function DDL ("Could not find the function … in the schema cache") → `apply-db.mjs` now sends `notify pgrst, 'reload schema'` after every file (memory: postgrest-schema-cache-gotcha); (2) `search_public` had become 1–3 s and hit Supabase statement_timeout under backfill load because 0093's chat hits regex-scanned the 440k-row `vod_chat_terms` — 0097 uses `t.term = any(q.alts)` (index) and trending only joins recent chat; (3) the typo corrector rewrote "마비" to "뮤비" once chat added 뮤비 to the dictionary — 0098 skips correction when the query is a prefix/substring of any corpus word, game, person, VOD title, chapter or event title (노레→노래, 챌린쥐→챌린지 still correct). "mic" chips came from "MIC ON" chapters → stop words in the term graph. The loader now logs RPC failures (`[search] rpc failed:`).
2026-09-18 (Latin typo dual search): "anfmv" used to hit the Latin fuzzy match "akmu" (악뮤) and never reach 무릎 because the 두벌식 retry only ran when the Latin search was empty. `searchPublic` now runs both candidates in parallel whenever `fixLatinTypo` yields Hangul; if the Hangul candidate has hits they come first (corrected = the Hangul word) and only *exact* Latin hits are appended (deduped); otherwise the Latin result stands. Integration test added (10/10).
2026-09-18 (search cache bug): owner saw "무릎" return nothing although "아이유 - 무릎" exists; RPC/API were fine by the time we looked. Cause: a request that lands while a migration drops/recreates `search_public` gets an RPC error, the loader mapped that to an empty result and the route cached it as 200 for s-maxage 300 + SWR 600 → an empty answer pinned for up to 15 min for that query. Now the loader returns `failed: true` on RPC error and the route answers 503 with `Cache-Control: no-store` (the sheet shows the retry hint).
2026-09-18 (mobile search): at ≤640px the search sheet blurs the input as soon as results arrive (keyboard drops), VOD/chapter rows render as `<a>` to the SOOP player (`?change_second=` for chapters, new tab) instead of opening the replay window, the ▶ replay button is omitted, and the layout is re-tuned — 112×63 thumbnails, VOD titles up to 3 lines, duration moved into the meta line (right column hidden), chapters as timecode + two-line label without the trailing play icon, event titles two lines. `.ps-row` gets `text-decoration: none` since it can be an anchor now.
2026-09-18 (search polish, 0095): event rows carry that day's first VOD thumbnail (RPC subquery; future events none) and show it in place of the calendar badge; the "go to that day" action icon is now a calendar in both viewer and studio (event rows everywhere, VOD rows in studio), and the studio flashes the target day cell (`.studio-day.cell-flash`, same animation as the viewer) after the month slide. Studio chapter rows now open the replay window at that timecode directly (no per-chapter calendar/▶ duplication — the VOD row above keeps calendar + ▶). Replay window sound: `isVodSoundAutoplayBlocked()` ignores the 3-day "blocked" memory when `navigator.userActivation.hasBeenActive` is true (a click on our page delegates autoplay to the iframe), so search/chapter clicks start with sound; a real block still falls back to muted after the 1.5s watchdog.
2026-09-18 (genre search, 0094): owner asked that 공포/공겜/공포겜 find every VOD where a horror game was played, and likewise RPG/코옵/오픈월드 etc. `search_genres` maps 33 genres (공포·코옵·오픈월드·rpg·mmorpg·fps·배틀로얄·moba·샌드박스·생존·로그라이크·덱빌딩·리듬·퍼즐·플랫포머·메트로배니아·소울라이크·격투·파티·레이싱·스포츠·축구·카드·시뮬레이션·농장·추리·탐정·어드벤처·전략·힐링·vr·슈팅) to normalized game-name fragments (curated from `search_games` plus common titles; 818 rows); genre words are `search_intents` intent genre, their slang/synonyms are `search_synonyms` seed-genre (282 rows), and 게임/겜/장르 are intent noise so "공포 게임" does not demand the literal token. RPC: `genre_terms` (direct or via synonym) → `gre2` regex → `hit_genre` on event title+tags, VOD title or any chapter game/section, and chapter label/section/game — scored 2.0–2.3, exact, matched_on genre. Genre coverage is a curated seed, not Steam-derived: unknown games need a row in `search_genres` (developer). Search sheet: VOD titles wrap to two lines (line-clamp 2) now that thumbnails are 192×108. Chat backfill: first run looped forever on VOD 118474497 (chunk list shorter than duration → 0 fetched, never complete); fixed with a give-up rule in `syncVodChat` (0 fetched in a pass with plan left → complete) and a same-target guard in the script; restarted.
2026-09-18 (chat terms as search hits + nickname stems + backfill, 0093): `vod_chat_terms` now carries `peak_bin`/`peak_cnt`/`bins` (the 30-second bin where a word clustered), and `search_public` unions a `chat_scored` CTE — a chapter-shaped row titled 채팅 "X" 반응 구간 (section 채팅, matched_on chat, exact when ≥10 mentions) pointing at that bin, only when the whole chat word equals the query/synonym, ≥5 mentions, and neither the fan chapters nor the VOD title already hit. Nickname variants of the owner are unfolded by the collector (`nicknameStems`): ○○토리 → stem (탐정토리→탐정, 바보토리→바보), 빅○○ → stem (빅드럭→드럭), and a one-syllable stem is resolved against that VOD's title/chapter words (탐토리→탐정); syllable-swap puns (빅명한) stay unresolved. Search UI: chat hits get a MessageCircle icon; thumbnails 192×108 (3×); in the studio the VOD/chapter row action icon is a calendar (row = go to day) while ▶ is the separate replay-window button. Backfill: `scripts/vod-chat-backfill.mts` (oldest-first, 400 chunks per VOD, then all rebuild RPCs) was started locally on 2026-09-18 — GH Actions keeps taking the newest 4 per run so the two do not collide; the two previously synced VODs were reset to pick up peaks.
2026-09-18 (studio replay window + typography + slang, 0092): owner slang seeded as `search_synonyms` source `seed-owner` — 프클=프로클럽=fc프로클럽 (was being typo-corrected to 퍼클), 잔디=fc/fc25/fc26/피파 (우왁굳-origin slang for the FC football series), rel 프클↔잔디↔고멤fc; memory `vic-slang-dictionary`. Search RPC now returns `thumb` per VOD/chapter row and the loader maps the SnapshotLoad query, so the studio (no VOD bundle) shows thumbnails too (`SearchVodGroup.thumb`, `thumbOf() ?? v.thumb`). Studio ▶ no longer opens a new tab: `onReplay` opens the same `DayVodWindow` (variant modal, side = avatar side) inside the studio, with that day's VOD list fetched once from `/api/public/vic/events` (vods) and cached in a ref; the `.ps-line` class collision that hid rows was fixed earlier (`.ps-rowline`). Typography raised for both viewer and studio in one block (`.ps-sheet`: width 780 / studio 880, row 58, chapter 38, input 18, title 16, chapter 14.5, timecode 13, chips 13.5, thumb 64×40).
2026-09-18 (search quality + strip alignment, 0091): owner review of the studio search — (a) studio mode now splits results into 직접 (events: title/description/tags, what the owner wrote) first and 간접 (VOD/chapter hits from timelines/chat, click = go to that day) and hides the 요즘 chips; viewer mode unchanged. (b) "이게" matched "이 게임" because search_norm strips spaces: 0091 adds a `short2` flag (2-syllable Hangul query with no dictionary expansion and no correction) that additionally requires raw-text containment (lowercased title/description/tag string/chapter label/section) and disables the trigram fuzzy fallback for such queries. (c) Trending chips: chat-derived words must exist in the title/chapter/event corpus or a dictionary (synonyms/entities/games/intents) so every chip yields results; stoplist grew (아니야·이게·사실·아마…). (d) Strip alignment: the true SOOP progress track is `.progress_track` inside the iframe at a fixed 20px inset on both sides (measured via Playwright frame DOM at player widths 782/1132/1369; the earlier 8px was the idle thin line before playback). `.vch-strip` margin is now 0 20px and page-x/width match the track exactly (364/1092 at 1500 viewport). Gold knob on the playhead, glassier track.
2026-09-18 (chat → replay window + studio search, 0090): the collector now also keeps 30-second bins (message count, unique-speaker count, ㅋ laugh score, top 3 terms) in `vod_chat_bins` and known-streamer greetings/mentions in `vod_chat_people` — known names come from `search_known_people()` (entities with docs≥3 or 합방, plus seed-person aliases; greeting shortcuts = first/last syllable + 하, ambiguous ones dropped); "○○님" tokens are kept only for known names, so viewer names never land in `vod_chat_terms`; owner names are excluded from people. The public RPC `vod_chat_profile` returns ratios only (h/d/l 0–1 per bin + laughTier when ≥8 comparable VODs) — no counts ever reach viewers (owner rule). Replay window: `.vch-strip` is now inset 8px each side to match SOOP's player progress bar (measured at 800/1200px), draws the chat heat as a smoothed area + speaker density as a gold line, corner segments shrink to a 5px top ribbon, up to 5 peak pills (top terms or "반응 ↑") sit under the strip and jump on click, hover tip gains a second line (반응 ↑ · 웃음 · terms), and a "😂 많이 웃은 방송" row appears under the chapter toggle; chapter-less VODs get the strip alone. Studio: new "검색" tool tile (first; "태그 편집" → "태그") and `/` hotkey open the same `PublicSearch` sheet; event hits move month + open that event card, VOD/chapter hits move month + select the day, and each VOD/chapter row has a ▶ that opens `/replay/<date>?t=` in a new tab. Search sheet bug fixed: Enter after typing a new word picked a stale hovered row (cursor survived query change) — cursor resets on query change, Enter with no valid cursor flushes the debounce immediately, hover sets cursor only on real mouse movement. Developer-only `/studio/search-dictionary` (settings modal → 은어 사전 초안) lists chat-learned words absent from dictionaries/titles; "뜻 저장" writes curated syn pairs, "무시" hides. Save-tab reveal: ZONE_OUT 90→150, dwell 220→120ms, slow threshold 0.3→0.55. Verified: 1 VOD synced (33 chunks, 946 msgs) → strip renders with peaks on /replay/2026-09-17; tsc/lint clean.
2026-09-18 (chat learning tuning, 0089 + GH backfill): owner ruled SOOP emotes (/빅하/) worthless for chips, so the tokenizer drops them and 0089 purged existing emote rows and removed them from `search_trending`. Old-VOD slang is treated as fading: `search_term_relations.last_day` (max co-occurrence day) feeds a decay in `search_related_terms` (score × (0.4 + 0.6·e^(−age/365d))), auto-rel chips require a co-occurrence within 540 days, and chat terms join the term graph / abbreviation mining only from VODs within 540 days (titles/chapters stay unlimited — they are the search corpus). Backfill: `.github/workflows/vod-chat-backfill.yml` calls `/api/cron/vod-chat?limit=4&chunks=120` at :07/:37 (GH throttles to ~1–2h effective) using the same CRON_SECRET repo secret as broadcast-poll; together with the calm-tier pipeline tick (~1.9k chunks/day) the ~400-VOD backfill should take roughly 1–2 weeks. Verified after apply: trending = 굿즈·잔디·아르마·마비노기·벨로나·룰렛… (no emotes), unit 2/2.
2026-09-18 (viewer search: aliases, typo tolerance, chat learning, 0085–0088): `search_synonyms` gained seed-person groups (owner-provided 프리터=아르바=바이터, 불독=보디가드 불곰, 쵸로키=데스헤머 쵸로키/초로키, 시리안=시리안 레인, plus 왁타버스 stage names and 이세돌; 280 rows) and auto-typo variants for every frequent person name (쵸↔초, 캐↔케, 롯↔럿…; 90 rows, skipped when the variant is itself a real name). General typos: `search_jamo` decomposes syllables, `search_correct` picks the nearest dictionary word by jamo Levenshtein (fuzzystrmatch; ≤1 for ≤2 syllables, ≤2 for 3, ≤3 beyond, same first jamo) and 0087 wires it into the RPC for single-word queries not in any dictionary (노레→노래, 챌린쥐→챌린지, 시리언→시리안, 우앆굳→우왁굳); rows carry `corrected`, the sheet prints "'노래'(으)로 찾았어요". Latin-keyboard typos (shfo→노래) are undone in the loader by a 두벌식 converter (`lib/search/hangul.ts`) when the first RPC returns nothing. Fan marker emoji are stripped from displayed labels only when they duplicate our kind icon (🎵🎶🎤🎧🕺💃); ✨👗🐸 stay. Chat learning: SOOP's VOD view API exposes `files[].chat` (ChatLoadSplit.php rowKey …_c, 600s XML chunks); `lib/broadcast/vod-chat.ts` stores only per-VOD term counts (emotes like /빅하/ kept as terms, ids/nicks never stored) in `vod_chat_terms`, runs 1 VOD/40 chunks per calm pipeline tick and via `/api/cron/vod-chat?limit&chunks` for backfill; term graph, abbreviation mining and trending now include chat terms. First run: 1 VOD, 8 chunks, 243 messages → /빅하/ 34, /힝/ 14, /으에에/ 6, 빅타민… Backfill of ~400 VODs is NOT done (needs repeated cron calls; ~60–200 chunks each).

2026-09-18 (viewer search: performance kinds, 0084): `song_kind` now sung / listen / dance / hum / null(talk). Signals from fan timelines only: 🎤 = sung; 🎵 in a VOD that also uses 🎤 = listen (author distinguishes), in a dance corner = dance, else sung; 🕺💃 or 챌린지·댄스·안무·숏츠·릴스·틱톡 in the label or a 춤/댄스/짤래뱅 corner = dance; 허밍·흥얼·콧노래·나비보벳따우 = hum; talk vocabulary (스토리·썰·시절·근황·반응·ㅋㅋ…) is never a song even inside 노래뱅; '부르는/불러' = sung outside listening corners. Dance titles learned anywhere propagate to the same artist+title elsewhere (🎵예나 - 캐치캐치 → dance). Counts: sung 592, dance 384, listen 328, hum 2. Intents: 노래 → sung first (hum 1.0, listen 0.8 behind), 춤/챌린지 → dance, 허밍 → hum. Sheet: one kind icon per row (♪ sung, headphones listen, footprints dance, audio-lines hum) and fan marker emoji stripped from the displayed label (tooltip keeps the original) — fixes the doubled 🎵♪. A curated web list of trending challenges was considered and skipped: it goes stale fast and the corner/label/emoji signals already separate 춤 from 노래; a list can be added later as a `dance` seed.

2026-09-18 (viewer search: 종겜 intent, 0083): learned game list `search_games` — sections "게임 - X" always, other section names only when they recur in ≥2 VODs or match the curated 종겜 seed (76 names), non-game corner vocabulary excluded; kind major/minor by `search_game_is_major` (owner definition: 롤·마크·배그·아르마·옵치·발로·메이플·FC·스타·와우·롤토체스·카트·던파·로아·디아·히오스·하스·검사·서든·마비노기·명조·원신·명방; VR챗/릴동파 stages are not 종겜). Chapter index gets game_norm + is_section_start. Intent 종겜·종합게임·스팀겜·패키지게임·인디겜 → minor_game: VODs whose title names a 종겜 or that have a 종겜 corner rank first (2.8), the corner's first chapter (2.4) sits under them, events 2.6; extra tokens narrow ("종겜 할로우"). `search_game_refresh()` runs after each sync.

2026-09-18 (viewer search sheet redesign, 3rd pass): position sense without numbers — right-edge date rail (one dot per day group; passed dots filled in accent, current dot enlarged with a "YY.MM" tip while scrolling or on hover; tap to jump), 2px scroll-progress line under the header (doubles as the loading flow line), sticky day headers, floating "맨 위로" after 480px. Rows: 96×54 thumbnail / 36px event icon, title + one meta line, right-side auxiliary text (duration · start time) that cross-fades into the action glyph on hover/keyboard cursor; rows lift 1px with a soft shadow, thumbnails zoom 4%, timecodes turn ink. Sheet: 680px, 22px radius, layered shadow, spring rise; rows stagger in (≤12 × 22ms); sort segment has a sliding thumb; chip rows are single-line horizontal scroll with a fade mask; count shows "200+". reduce-motion disables all of it. Benchmarks: iOS Photos/Contacts index rail, Medium progress bar, Apple Music search rows, Notion sticky headers. Playwright: 24 rail dots, 12 filled at 55% scroll, tip 26.04; mobile OK; tsc/eslint/unit clean.

2026-09-18 (viewer search: song intent, 0082): chapter entries are classified `song_kind` sung/listen from (a) section names (노래뱅·풀트노래뱅·빅래뱅·뱅종곡·노래방 = sung; 월드컵·N강·빅이봤·영상·플리 = listen), (b) a learned artist dictionary `search_artists` (left side of "가수 - 곡" inside sung sections, ≥2 songs: 아이유 39, 볼빨간 사춘기 16, AKMU 14, 한로로 12…) so "🎵:아이유-스물셋" under 소통 or "한로로-입춘" under 메이플스토리 also count as sung, (c) 🎵/cover/live markers. `search_intents` maps 노래·곡·노래뱅·커버·싱스트림… → song; the RPC then gives sung entries bonus 2.8 (above any label match), listen 0.8, and remaining tokens narrow inside songs ("노래 한로로" → only 한로로 songs). "노래" → 365 sung hits, top 10 all songs. Sheet: ♪ mark on song rows (muted for listen), per-VOD "N개 더 보기" expands all matched chapters, fetch limit 200 (RPC cap 400). `search_song_refresh()` runs after each timeline sync. Integration 8/8, unit 44/44, tsc/eslint clean.

2026-09-18 (viewer search: chapter context + 1-char slang, 0081): `vod_chapter_index` now carries section (fan [코너], 81% of entries), parent (nearest depth-0 label, 19%) and depth; RPC matches labels + section + parent (section bonus 0.9 between label 1.2 and related 0.5; multi-token over all three) and returns section/parent/matched_on. "노래" → 85 section hits (songs under 노래뱅/풀트노래뱅/뱅종곡) + 107 label hits. Sheet shows the section as a sub-header above chapter rows (once per run of the same section, highlighted when the section itself matched) and the parent as a breadcrumb. Audit of 1/2-char tokens, tags and long title words added 22 abbreviation groups (메·주보·맵·구플뱅·리캡·카페·오픈런·왁체·고카·슬더스·명방·마듀·팰월드·배크·릴동파·천타·뢴가·FC…). 1-char queries are allowed only when the dictionary has them (메·롤·숲·겜) and expand only via the dictionary (the raw char is not matched, otherwise 메카/메이린 flood). Integration 7/7, tsc/eslint clean.

2026-09-18 (viewer search: relationship graph + related terms, 0079/0080): seed dictionary widened to SOOP/왁타버스/VTuber slang (왁굳형·뱅온·뱅종·별풍·팬치·고멤·이세돌·왁물원·데뷔·졸업·모캡…). Person graph: "○○님" mentions in VOD titles, fan chapters and public events → `search_entities` (206) and `search_relations` (5,981 edges; co-doc count + 합방 flag + PPMI); name-variant auto synonyms (왁굳↔우왁굳, 세구↔고세구, name↔name님). Term graph: all words (Hangul 2–6 / latin 3–12) → `search_terms` (2,507) and `search_term_relations` (36,778, PPMI ≥ 0.5) = the "embedding" (PPMI adjacency rows; no 2D layout yet). Synonym kinds: syn (same thing, bonus 1.2) vs rel (same series, curated, bonus 0.5, included in results: 할나 → 실크송 hits). RPCs `search_related` (people), `search_related_terms` (curated first, auto neighbours filtered for particles/endings), `search_trending` (last 30d burst vs prior 180d). All three graph rebuilds run after every timeline sync. Sheet: "관련 검색어"/"함께 자주 나온"(합방 n) chip rows under the sort bar, "요즘 자주 나온 말" + tag chips before typing. Integration 7/7, tsc/eslint clean. Latency: single word 130–250ms, 할나 ~580ms first call.

2026-09-18 (viewer search: slang/abbreviations, sort, thumbnails): migration 0078 applied — `search_synonyms` (46 curated groups → 292 bidirectional rows + auto-mined rows from title-word↔chapter-word abbreviation pairs with ≥3 supporting VODs, e.g. 돌발↔돌발서버; `search_synonyms_rebuild()` also runs after every timeline sync), query expansion per token (`토큰|동의어…` regex groups, IDF kept), `search_abbrev_match` first-syllable subsequence across word boundaries as a low-tier exact=false hit (배그∈배틀그라운드, 츠온∈츠쿠요미 온라인), `popularity` column. Timing after regex/LIKE prefilter optimisation: single word ~110–150ms, two words ~500ms. Sheet: sort segment 관련도·최신·오래된·인기 (client-side over day groups, remembered per device), thumbnails 112×63 (mobile 88×50), hit count. Probe: "배그" → 배틀그라운드 chapter found; sort buttons reorder correctly. Unit 44/44, integration 6/6 (synonym + abbreviation cases), tsc/eslint clean.

2026-09-18 (viewer search P3 + owner feedback): migration 0077 applied (IDF-weighted token ratio, `search_choseong` + `label_cho` column, `exact` flag, RPC return type changed → dropped/recreated). Probe: "마크 왁피스" now ranks the 왁피스 VOD first; "ㅁㅋ" → 마크 chapters (128 rows). Results list redesigned (YouTube results/key moments + Spotlight): no per-row text CTAs, kind = left icon/thumbnail/timecode, one action glyph at the right, chapters under a guide line, "비슷한 결과" divider for fuzzy-only groups, tag chips before typing. Header pill "이 달 기록" → "기록". VOD/chapter picks also move the calendar month (cookie), and on mount the viewer re-reads the cookie month because Next's router cache restored the stale month after back from /replay (probe: back → 2025년 12월). Unit 8/8, data-act 35/35, integration 5/5, tsc/eslint clean.

2026-09-18 (viewer search P1/P2): header `poster-interest` now holds a `search-open` pill (same `insights-open` part, `--mid-pill-w` measured against it); ♥ filter stays in the agenda legend, which also gained a mobile search chip. Sheet = `components/poster/public-search.tsx` on the `pi-*` shell, sharing the one sheet state/history slot with '이 달 기록' (`sheet: null|insights|search`). Event hit → `moveMonth` + `.cell-flash` (grid `data-date` / agenda `data-flip-key`); vod/chapter hit → `openDayVod(date, part, sec)` → viewer `/replay/<date>?part=&t=`, preview modal `initialPart/initialSec`; `DayVodWindow.initialSec` seeks via `jumpDayVod` one tick after mount. Closing the sheet before navigating had to wait for its `history.back()` popstate (it cancelled `router.push`, see memory overlay-history-back-cancels-nav). Playwright probe on a temp :3111 dev server (anon): pill present, 77 rows/30 days for "엔딩", event pick moved to 2026-07 and flashed, chapter pick landed on `/replay/2025-10-19?t=16721` with the chapter gold and strip head in place, mobile chip → sheet → agenda flash. `tsc`/eslint clean; data-act dictionary test passes again (added vod-tab, title-input, search entries). e2e spec `tests/e2e/public-search.spec.ts` added.

2026-09-18 (viewer search P0): migration 0076 applied to Supabase (`pg_trgm`, `search_norm`, `vod_chapter_index` 17,844 rows from 354 timelines via trigger + backfill, `search_public` RPC). Probe: single word ~90ms, two words ~330–480ms. `searchPublic` loader + `/api/public/vic/search` verified on a temporary dev server (200, `s-maxage=300`). Unit 7/7 (`tests/unit/search-group.test.ts`), integration 5/5 (`tests/integration/public-search.test.ts`: private/draft/tombstone/unrevealed-teaser titles return 0 event hits, DTO has no private field names). `tsc` clean, eslint clean on touched files. Pre-existing unrelated unit failures: activity-labels data-act dictionary, ambient-codex (3). Known ranking gap: no IDF — "마크 왁피스" ranks a popular 마크 VOD above the 왁피스 VOD (P3).

2026-09-10 (style captures): 449 images (데이브 더 다이버 186 = 184 depiction + 2 mood; 모여봐요 동물의 숲 263 = 8 mood + 195 depiction + 13 form + 47 none) were classified by visual review of 38 contact sheets and applied through the real `style:scan → 분류대기.json → style:apply` path; `style:check` passes with 0 unindexed and 0 hash drift. `style:pick` for tree-pine yields 2 mood scenes, fish-crucian 3 exact/pond icons, fish-anglerfish 2 exact + 1 deep-sea, animal-chipmunk mood only (no land-mammal captures exist). Unit tests cover scan/apply/pick/freeze; the full gate results for this tree are recorded in the commit message. [Reference cleanup and classification](handoffs/20260909-reference-library-cleanup.md) remains the record for the earlier 69-image pass.

2026-09-10 (earlier): reference attachment shipped and pushed through `a6a20777`. The kickoff note `20260910-next-session-kickoff.md` is wrong where it calls work-order A-1 unfinished: `RULE_MIGRATION.md` exists from `27abd6dd`/`628123d5`.

Automatic host hook activation, real-device and production DB checks remain NOT VERIFIED. Spring provenance, browser checks, A/B/C review and deployed public verification are in ROUND-20; owner visual feedback remains separate.
