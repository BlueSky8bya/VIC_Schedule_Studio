# Privacy audit and timeline deployment — 2026-09-21 KST

Status: DB containment and release7549df6b deployed/verified2026-09-21 KST. Owner authorized audit/fixes, migration application and push/deployment. Historical exposure and provider/retention follow-ups remain open.

## Scope and plan

Live insights follow-up: owner requested secure restoration of the live tab. The replacement
uses existing visit-session counts behind per-request developer authentication; no public
Realtime and no new collection. See [implementation and verification](../plans/PLAN-20260921-live-insights.md).
The legacy open-tab/provider Realtime setting investigation remains separate.

Inventory identity storage, public DTO/API/RSC/cache, auth/unlock, DB RLS/RPC/storage privileges,
analytics/logging/retention, tracked artifacts and production dependencies. Independent reviewers
cover app boundaries and storage. Main agent checks live DB metadata/anonymous reads without
printing personal records, fixes confirmed weaknesses, verifies and deploys.

Apply additive 0124 before collector/readers. Preserve manual choices and existing projection.
No production cleanup, account deletion, key rotation or unrelated user files included.

## Evidence limits

Source review and scoped live probes cannot prove absence of historical disclosure or compromise.
Real login roles, hosting/account security, log access, backup access and vendor retention need
their own evidence; mark unverified rather than infer from service-role tests.

## Constitution

AGENTS G-20 makes data minimization, public identity separation, allowlisted payloads,
least privilege, fail-closed authorization and privacy regression checks release requirements.
Duplicate second G-18 renamed G-21 without changing its independent-review requirement.

## Findings and containment

| Finding | Evidence | Action |
|---|---|---|
| Admin SECURITY DEFINER RPCs inherited PUBLIC EXECUTE | Live catalog: performance/dictionary/rebuild functions executable as anon | 0125 revokes PUBLIC/anon/authenticated, preserves service_role |
| Direct REST bypassed teaser masking, tombstones and calendar identity projection | events public SELECT policy lacked reveal/deleted conditions; calendar owner_id selectable | 0125 restricts source RLS and calendar column privileges; explicit masked public_schedule_events view consumed by public loader |
| Private ALL policies could bypass unlock SELECT policy | PostgreSQL permissive policies OR together | 0125 requires private unlock on ALL policies and retains owner-only scope |
| Direct passcode settings writes may bypass app verification | Legacy authenticated DML grants | 0125 revokes all client settings privileges |
| Nonpublic VOD metadata/profile access | 12 nonpublic archive rows; archive using(true), definer profile without auth filter | 0125 restricts archive/profile to auth_no=101; 0124 already restricts timeline/chapter rows |
| Passcode check fail-open and parallel guessing | Settings SELECT error ignored, count/insert race, change route unthrottled | Fail-closed reads; shared advisory-locked 5 attempts/10min RPC, reserved before verification; version compare-and-swap password update |
| OAuth external redirect | Local /backslash path resolves to external origin | Shared same-origin return path validator; controls/backslashes rejected |
| Missing cron secret allowed service execution | Both cron routes conditionally checked secret | Missing secret503; invalid credential401 before work |
| Arbitrary personal text could enter analytics | Metadata denylist, arbitrary target/visit keys | Reviewed key/type/enum allowlists, static control names, UUID keys, fixed device enum |
| Public Realtime presence disclosed role/device/visibility and tab activity | Public presence:vic channel | Broadcast/subscription removed; developer panel states unavailable, no fake zero counts |
| Production backup and account emails tracked in public repo | Repository API confirms public; exact configured-secret scan found owner addresses in2docs, no current credential matches; developer address also present in seed/deployment doc | Backup preserved locally and removed from index; account emails replaced with invalid-domain examples, developer seed reads local configuration. History NOT erased |
| Fixed known salt fallback | Missing server secret generated predictable hash | Return null if no secret; treat stable hashes as pseudonymous personal data |
| Cleanup builders never executed | activity query discarded lazy PostgREST delete requests | Awaited pruneActivity with result; default disabled until restricted backup and explicit cleanup authorization |
| Production dependency advisories | Before: Next15.5.18, sharp0.34.5, postcss8.4.31, nanoid3.3.12 | Next15.5.24 plus compatible scoped patches; production npm audit0 |
| DB tool TLS and console identity | rejectUnauthorized:false and full owner-email logs | Verified CA/hostname; configurable SUPABASE_DB_CA_PATH, emails omitted |

0124 and0125 applied successfully with verified TLS. Initial TLS attempt failed closed before
any SQL ran; official Supabase CA over verified HTTPS resolved trust. No production rows deleted.

Live anonymous REST checks after0125: eight private tables denied, calendar owner_id denied,
performance/dictionary RPCs denied, nonpublic VOD/private event/deleted event/future raw teaser
queries returned0. Masked event view returned582 public rows. No future teaser existed at probe
time, so hidden-title canaries were tested in isolated PostgreSQL, not invented production fixtures.
Storage catalog contains0 buckets. All inspected public base tables have RLS enabled.

Operational source refresh of the originally reported VOD207499291 completed through the new
collector:66 entries,1 visible variant saved. Search chapter trigger updates the representative.
Three ancillary learning refresh RPCs reported existing safe-update DELETE-without-WHERE errors
(song, graph, trends); they did not roll back timeline ingestion and remain an operational follow-up.

## Data inventory and retention limits

| Data | Purpose and boundary | Retention evidence |
|---|---|---|
| Google/Supabase Auth email, name/avatar metadata, sessions | Authentication; no public-profile fallback | Vendor/account configuration and deletion process unverified |
| Owner/developer email allowlist | Authorization, server configuration/private table | Needed while authorized; public docs now examples only |
| Private schedule ciphertext, passcode hash, grant token hash/session binding | Restricted schedule access | Grants expire/version invalidate; expired-record cleanup separate |
| visit_session stable account/device pseudonym, exact timing/device/role | Developer-only operational diagnostics | 26,059 rows at audit; no established automatic TTL found. Define/minimize retention before cleanup |
| activity_event and daily counts | Developer diagnostics; normal viewer actions aggregate | Intended90days/diagnostic3days; 11,859 expired diagnostic rows counted. Deletion not performed |
| Fan timelines | Public SOOP nickname + timestamp labels; private moderation/source keys | Raw IDs/IPs/unrelated comment bodies not persisted; source deletion removes public projection on next complete sync; private decision rows retained |
| VOD chat | Terms/counts/normalized profiles; no raw speaker IDs or chat transcript stored | Source frequency data retained; nonpublic source now blocked from direct public access |
| Hosting/access/error logs, backups | Provider operations | Vendor retention/access, historical disclosures and deletion not verified |

## Remaining work — do not call this a complete safety guarantee

1. Public Git history still contains old operational backup and owner email addresses. Current
   checkout removal is containment only. History rewrite/force-push, collaborator coordination,
   cached copies/forks and hosting/CDN copies require a separately authorized recovery plan.
2. Already-open old tabs can continue public Realtime presence until reload. Server-side public
   channel disable/termination has not been verified; new release stops producing this data.
3. Retention cleanup flag remains off. Prepare restricted recovery backup, exact selection/counts,
   then authorize deletion. visit_session TTL and privacy notice/rights-request workflow remain
   policy work; hashes are not anonymous merely because email was replaced.
4. Development-tool advisories are reported separately from production npm audit (after safe updates:
   6 total:1critical,2high,3moderate; Vitest/Vite ecosystem requires a major upgrade). No public test
   UI/dev server should be exposed. Major test-tool migration is outside this deployment patch.
5. Genuine owner/developer/viewer OAuth sessions, provider MFA/session/access-log settings,
   encrypted backup custody, full historic-secret scan and actual abuse/collection history are
   NOT VERIFIED. Isolated SQL roles and anon live probes do not prove those properties.

## Incident context

User-provided Namu Wiki page and Fancim homepage could not be fetched. A2026-09-19 report
quotes the company's notice attributing nickname/real-name association to a wrong display-column
connection during an update; independent forensic verification and final impact count are not
available in this audit. No victim identities or leaked records were retrieved or reproduced.
Source: https://www.topcelev.co.kr/news/articleView.html?idxno=3974

## Verification

- Independent app/storage review before integrating0125; main reviewed auth, logging, nullable hash
  and realtime-removal diffs. No claim of a penetration test or absence of past abuse.
- Isolated PGlite:0124 and0125 idempotence, timeline moderation/public projection, synthetic hidden
  title canaries, column/RPC privileges, owner/developer/unlock differences and attempt reservations.
- Live anon HTTP: private tables/owner_id/admin RPC denial and source row filtering verified.
- Typecheck, full lint and Next15.5.24 production build pass. Full unit suite1018pass,
  2 pre-existing ambient-codex shallow-biome failures (1020total). Both1440px/390px production
  fixture interaction tests pass; light/dark captures reviewed, no horizontal overflow.
 91 client JS/map assets contained none of the configured secret/owner-email exact values.
 Local anonymous events/timeline APIs200; timeline no-store. Studio/timelines emits the intended
 streamed redirect with no management board/candidate payload. Production npm audit0;
 development-only audit6 remains as above.
- Harness duplicate G-18/UI-18 declarations corrected without weakening rules; regenerated rule
  inventory. Remaining pre-existing size-budget failures: root AGENTS and CURRENT_STATE exceed
  their configured byte budgets. The current-state history was not silently discarded.

## Deployment evidence

Release7549df6b: Vercel reported success at2026-09-21 22:37:31 KST. Production root JS includes
that commit. Root/events API200; target timeline207499291 returns66entries/1variant and no-store.
Both cron routes return401 without a credential. Anonymous studio/timelines returns a streamed
redirect without board/candidate payload; production fixture returns the streamed404 boundary
without fixture data. Follow-up removes the last developer-email seed literal (no DB seed run),
records this evidence and fixes parser trailing whitespace; missing-config/idempotent seed SQL
checks and script lint pass. Local personal evidence and unrelated user files are not committed.
