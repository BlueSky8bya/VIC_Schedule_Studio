# ROUND-26 — 자연스러운 구름 윤곽

Status: 로컬 구현·검증·독립 검토 완료, 소유자 시각 확인 대기 · 2026-09-11 KST

소유자 피드백: R25 구름 모양의 규칙성이 강하다. 작은 구름의 격자/대칭 봉우리/비슷한 타원 판을 비대칭 큰 덩어리, 불균등한 빈틈과 찢긴 가장자리로 바꾼다.

수정 묶음 1: 구름 원본 V2와 추출 좌표. 10속×3형태, 픽셀 팔레트, 기존 이동 경로·날씨별 양과 종류 유지. 영향 예상: 구름이 보이는 모든 비심해 장면. 안개만 있는 화면·심해·땅 자체·천체 좌표는 불변. 코드로 이미지를 늘리거나 찢어 반복을 숨기는 대신 원본 실루엣을 새로 그린다.

수정 묶음 2: B 사전 검토에서 큰 층구름 너비 약 35%가 Math.min 상한에 모이는 원인 확인. 기존 범위 안 연속 크기 분포로 교체해 같은 최대폭 반복을 제거. 속도·경로 수·재등장 순환·날씨 풀은 유지. 영향은 큰 층구름이 있는 시나리오이며 맑음의 적운/권운 크기 범위는 유지한다. 순수 함수 결정성/화면 밖 진입 검사 유지.

A/B/C 사전 독립 검토 완료. A는 첫 시안의 삼각형 적운/모루 반복 잔존, B는 최대폭 쏠림, C는 실제 비율 기준 상하 경계와 추출 여백 검사를 지적했다. 첫 시안은 적용하지 않고 보존. 두 번째 시안은 새 색감 입력으로 생성했다.

검증: 기존 build Sp-_j8cjYtXSt09zBOlCl에서 같은 시나리오 before 확보, 아틀라스 여백/파편 검사, 제품 게이트, 같은 시나리오 after 및 독립 A/B/C 검토. 공개 DTO/권한/DB/KST 날짜 선택 변경 없음. 로컬 검토용이며 푸시하지 않는다.

생성 요청·원본·실제 첨부 입력: `art-src/하늘·천체/공통하늘/작업회차/20260911-구름불규칙성-01/`(반려), `20260911-구름불규칙성-02/`(선택). Built-in imagegen. 각 delivery.json에 프롬프트·입력·원본 해시. 새 public 파일은 `public/ambient/art/cloud-atlas-v2.png`, 생성 원본과 바이트 동일. V1 보존.

## 적용 결과

- 두 번째 생성기는 요청한 5×6 대신 실제 5×7/35형태를 냈다. 원본 1254²를 유지하고 실제 형태로 30개를 선택했다. 메타데이터 `cloud-atlas-layout.json`은 10속별 3개의 서로 다른 셀과 원본 픽셀 경계를 기록한다. 나머지 다섯 형태는 미사용. 분류는 스타일화된 형태 해석이며 관측 표본이라고 주장하지 않는다.
- 권운의 빗살, 작은 구름의 격자, 적운의 동일 삼각형, 층운의 비슷한 타원 윤곽을 완화. 형상마다 넓은 빈틈·치우친 봉우리·불균등한 조각이 있다. A 지적에 따라 층운 세 번째의 깃털형 후보는 낮은 구름판으로 교체했다.
- 시트형 크기 분포를 기존 최소/최대 범위 안의 연속 값으로 바꿔 최대폭 쏠림 제거. 실제 추출 비율을 배치와 그리기가 공유하고 위아래 8px 여백을 보장. 등비율 렌더, 로드 때 한 번 매트 제외/작은 캔버스 30개 캐시 유지. 새로운 프레임별 이미지 가공 없음.
- B의 거리별 농도/최근형태 회피 제안은 이번에 추가하지 않았다. 같은 종류 세 형태의 반복 가능성은 남으며 모든 구름이 무한히 고유하다는 뜻은 아니다. 기존 날씨별 종류·수량·속도·순환은 유지했다.

## 검증

- Before build `Sp-_j8cjYtXSt09zBOlCl`, after `qFCAq1_vbrlFTOKs-GHYd`. 같은 시나리오 14장: `.scratch-pw/qa/r25/r26-before/` ↔ `r26-after/`. 사계절 남/북·6날씨·실제 미리보기 제어 검사 통과, 브라우저 오류 0. 안개 장면 전후 해시 동일, 14장 하늘의 강한 자홍 픽셀 0.
- 단위 75파일/824개, lint·typecheck·production build 통과. 새 테스트: 크기 분포가 최대폭에 중복되지 않고 실제 그림 전체가 180/270/900 높이 하늘 안에 유지되는지. 기존 1,600초 순환 진입·결정성 검사 유지.
- source 30개 고유 셀의 비어 있지 않은 원본 영역·여백≥5px·실측 경계 일치 검사 통과. `.scratch-pw/qa/r26/atlas-layout.json`.
- 반응형 35검사 통과: PC 9비율, 정지/줄인 움직임/가볍게, 모바일 3크기에서 엔진/아트 제외. `.scratch-pw/qa/r26/responsive/`. 실기기/OBS 성능은 별도 미측정.
- selftest 3시나리오 결정성·시간 전진 경로 동치·정지·시드/시간대 반응·심해 날씨 봉인 통과(30초). 전수 연속 영상 시각 검토라고 주장하지 않는다.
- 독립 A/B 최종 실제 전후 PNG 검토: 윤곽/너비 복제감 감소, 맑음·바람의 열린 하늘 유지, 잘린 파편·자홍 테두리·상하 절단·가로 늘어짐 없음. 비 화면의 작은 모루 두 개가 비슷한 한계는 남지만 새 차단 문제 없음.
- 독립 C 실제 화면 8장/코드 검토: 실측 추출/배치 비율과 위아래 경계, 화면 밖 형태 교체, 기존 날씨 흐름/로드 시 1회 추출 확인. 추가 수정 필요 사항 없음. 각 독립 검토는 PNG/코드 범위이며 별도 테스트 재실행을 주장하지 않는다.
- 문서 harness·diff whitespace 검사 통과. 커밋/푸시하지 않음.
