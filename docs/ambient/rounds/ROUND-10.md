# ROUND-10 — 2026-09-06 — 자율 6라운드(10~15)의 첫 라운드: 시간대·안개·접촉을 시스템 뿌리에서

> 소유자 부재 중 자율 진행(라운드 10~15 + Final Visual Audit). 운영 규칙(소유자 지시 원문):
> Inspector 3인 동시 · 각 ≤ 5건 · 메인은 **≤ 2건(같은 뿌리면 ≤ 3건)** · before/after/diff · 회귀 재검사 ·
> 라운드별 커밋 `fix(ambient): complete visual QA round NN`. 우선순위 A(시간대) > B(날씨) > C(간격·레이어) >
> D(물 접촉) > E(안개 박스) > F(갯벌 물길) > G(언덕 지형) > H(해안 판) > I(산·계곡·민물 잔여).

## 입력

- 빌드 `b134f9d`(= 라운드 9 + 눈 편한 테마 유리 토큰) · 시드 42 · 뷰포트 1400×860 · 시나리오 16
- 시트: `.scratch-pw/qa/r10/before/index.md`
- A·B·C 동시 검토(원문 `ROUND-10-reports/{A,B,C}.md`, 각 ≤ 5건)

## 선택(같은 뿌리 A "시간대", 3건)

세 보고가 한 곳으로 수렴했다: **조명이 전 화면 후처리라 시간대·날씨·안개가 전부 "필터"로 읽힌다**(C의 새 지표 tintRes — 지면 변화를
전역 affine 색보정 한 장으로 설명한 뒤 남는 잔차: 초원 0.17% · 갯벌 0.03% · 언덕만 14~62%로 반증). A는 같은 것을 "13/16이
같은 그림에 필터 여섯 장"으로, 명도는 세 쌍(새벽≈노을 · 저녁≈밤 · 아침≈점심)으로 접히고 저녁 채도가 ×.39로 붕괴한다고 봤다.
깊은 바다는 셋 다 동일 의견 — 날씨·계절 봉인은 유지하고 **시간대만 장면 안에서** 열 것.

1. **하늘·조명 띠 프로파일**(A #1) — `sky.ts BAND_SKY` · `light.ts BAND`.
2. **빛의 방향을 지면에도**(C #1 처방 ①) — `view.ts drawLightPass`에 가로 multiply 성분. 전역 affine으로 설명되지 않는 첫 공간 채널.
3. **깊은 바다 시간대**(A #3 · C 의견) — `deep.ts`가 `f.time.band`만 읽는다. 계약 개정.

라운드 11 이후로 넘긴 것: E 안개 밀도장(C #3 처방 — 라운드 11) · C 간격·레이어(B #1·#2 — 라운드 12) · D 해안 클립(B #3 — 라운드 13) ·
B 날씨의 지면·수면 채널 + 바람 무나무 바이옴(C #2·#4 — 라운드 14) · F 갯골 · G 언덕 · 구름 원반 · 물풀 섬(B #4·#5 · A #4·#5 — 라운드 15).

## 수정

### ① 띠 프로파일

- `BAND_SKY`: 새벽 천정 "112 132 170" → **"96 114 150"**(노을보다 낮게 — 해 뜨기 전 차가움) · 저녁 지평선 "212 220 232" → **"214 200 204"**(노을 잔광,
  회장미 저채도 — 색상으로도 밤과 갈라놓는다) · 밤 천정 "84 100 140" → **"50 62 98"**(규칙 L* ≈ 28~35; 옛 값은 저녁보다 밝은 "흐린 저녁") ·
  밤 지평선 "246 248 252" → **"190 200 216"**(L* ≤ 80 — "하늘 > ①" 순서는 지평선 림 광만으로 지킨다, ROUND-05 열린 결정 유지).
- `BAND` evening/night: **파란 multiply가 따뜻한 색의 크로마를 상쇄**해 채도가 규칙(×.8/×.62)의 절반으로 빠졌다 → mul을 중성 쪽으로
  (휘도는 그대로 217/202), 색온도는 `tint`가 맡는다(저녁 tint α .07 → .10). desat 저녁 .12 → **.04**, 밤 .38 → **.16**.
- `BAND.morning.shadow.alpha` .95 → **1.15**(아침 그림자가 2배 확대에서도 안 읽혔다).

### ② 빛의 방향 — 지면

`drawLightPass`의 multiply 뒤에 **가로 성분** 한 겹: 해 쪽(`reflect.x`)은 그대로, 반대쪽 끝으로 갈수록 최대 −10%(L* ≈ −4).
세기 = |shadow.dx| × (shadow.len − .9)/.9 — 점심·아침(len ≤ 1.3)은 거의 항등, 새벽·노을·저녁에 보인다. 지평선 위(하늘)는 건드리지
않는다(하늘의 방향은 기존 skyK 판). 이것이 "전역 색보정 한 장"으로 설명되지 않는 첫 채널이다 — C의 처방 ②(서 있는 스프라이트 2톤)는
라운드 14의 날씨·시간대 채널 묶음과 함께.

### ③ 깊은 바다 — 시간대만

- `deep.ts draw()`가 `f.time.band`에서 `dayK`(점심 1 · 아침 .8 · 새벽/노을 .35 · 저녁 .12 · 밤 0)와 `nightK`를 만든다.
  **빛줄기**: 수 4 → 2 → 1 · α × dayK · 해가 낮으면 기울기 .09 → .2(기둥이 눕는다) · 밤 0. **위 1/3 명도**: multiply 세로 그라데이션
  (점심 → 밤 TOP L −12), 바닥은 밤낮 같다. **밤 발광**: 해파리 글로우 ×1.5 · 반지름 ×1.2, 먼 물고기 α ×.6(먼 것이 먼저 사라진다).
- `sealed()` 유지 — 엔진 입자·대기 안개·조명 패스는 그대로 건너뛴다. 별·노을 색온도·그림자·날씨 입자는 없다.
- **계약 개정**: "6띠 × 5날씨 30 해시 동일" → **"띠마다 5날씨 해시 동일(고유 해시 정확히 6)"**. `selftest.mjs` ④(띠 변화)·⑨(날씨 봉인) ·
  `BIOME_GRAMMAR §11`(날씨 행 "빛줄기 ×.5" 폐기 → 없음·봉인; 코드 참조 `sea.ts deep=true` → `scenes/deep.ts`) · `CLAUDE.md` 깊은 바다 항 ·
  `ADR-0017` "⑯ 개정" 절 + `DECISION_INDEX`.

### ④ 도구

- `spawn-probe.mjs`의 `HZ_V 0.12 → 0.26`(B가 두 라운드째 지적 — 실제 `HORIZON_V`).
- 라운드 10 프로브(`.scratch-pw/r10-{band,deep}.mjs`)는 **`captureCanvas`(비트맵)**를 쓴다 — 캔버스 *요소* 스크린샷은 그 위의 HTML 미니맵
  패널까지 찍어, 처음엔 "깊은 바다 날씨 누출 · 실행 간 비결정"으로 오판했다(변하는 화소 1,455개가 전부 패널 안이었다).

## 회귀 재검사(최종 빌드, 같은 시드)

| 항목 | 결과 |
|---|---|
| 다람쥐 spawn/path | B·C 라운드 10 검토에서 8시드·3시드 전부 통과(경로 최소 v .324 / .48~.60) — 이번 수정은 `autumn.ts`를 건드리지 않음 |
| 산 ridgeline/depth | 안개·겨울에서 순서 유지(C) · 부록 D 표준창 s11 ③↔④ 역전 닫힘 유지(B) · `r09-mtn2` 산 1초 3,337px ✔ |
| submerged rock | 민물 밑단 컷 닫힘(B 부록 A) · 계곡 후류 곡선 ✔ · **해안 노두 판은 뭍 클립이 원인으로 재귀속 → 라운드 13** |
| 간격·레이어 | 숲 나무 겹침 ≤ 40% 유지(B) · 버섯 150 크림 픽셀 ✔ · 풀 층 순서 문제는 라운드 12 입력 |
| 물/돌/파도 접촉 | P0(빛의 길) 유지: 노을 해 x 390 · 수면 띠 x 398 ✔ |
| 안개 depth | 절단선 0 유지(A·C) · 박스형 구조는 라운드 11 입력 |
| 육지 움직임 | 숲 16,285 / 10,772 · 언덕 9,958 · 산 2,159(밤) · 계곡 10,178 ✔ |
| 해·별 vs 마루 | 언덕 마루 아래 0 ✔ |
| 게이트 | tsc 0 · eslint 0 · vitest 604 · build 0 · `ambient-qa/selftest` **ALL PASSED**(④·⑨ 새 계약 포함) |

## 검증

| 항목 | before(b134f9d) | after | 기준 |
|---|---|---|---|
| 천정 L(초원 여름) 새벽/아침/점심/노을/저녁/밤 | 59.9 / — / — / 62.4 / 46.6 / 44.6 | **56.8 / 79.7 / 77.1 / 62.1 / 46.0 / 39.0** | 노을 > 새벽 ≥ 4 ✔(5.3) · 저녁 > 밤 ≥ 5 ✔(7.0) · 밤 ≤ 36(39.0, 근접) |
| 근경 크로마 비(초원 여름) 저녁 / 밤 | ×.63 / ×.40 | ×.61 / ×.46 | .70~.90 / .50~.70 — **부분** |
| 근경 크로마 비(초원 가을) 저녁 / 밤 | ×.39 / ×.18 | ×.48 / ×.33 | 같음 — **부분** |
| 깊은 바다 고유 해시 | 1(30장 동일) | **6** | 6 ✔ |
| 깊은 바다 띠별 날씨 봉인 | — | **✔**(6띠 × 5날씨) | 불변 ✔ |
| 깊은 바다 위 띠 L 점심−밤 / 아래 띠 | 0 / 0 | **10.0 / 0.0** | ≥ 8 / ≤ 1 ✔ |

크로마 비에 대한 주의: A의 지표(RGB max−min)는 **어두워지는 만큼 함께 줄어드는** 값이라 밤(휘도 ×.79)은 채도가 그대로여도 ×.79로
나온다. 채도 비로 환산하면 저녁 ≈ .56 · 밤 ≈ .42 — 그래도 규칙(.8/.62) 아래다. 남은 원인은 desat이 아니라 청회 **tint 오버레이**와
`gf`(원경 띠)·`drawDepthHaze`가 따뜻한 색을 회색 쪽으로 끌어당기는 것 — 라운드 14(날씨·시간대 채널 묶음)에서 tint 색을 띠 지면색
기준의 **채도 보존 색조**로 바꾸는 것이 다음 단계다. desat을 더 내리는 것은 효과가 없었다(.28 → .16에서 ×.26 → ×.33).

## 남긴 것(라운드 11 입력)

1. **E 안개 밀도장**(C #3 처방 — 라운드 11 첫 입구): `groundFog` 화면 그라데이션 + 고정 v .3 띠 → F(x,y) = 깊이항(단조) × 고도항(`fogFloor(x)`),
   층 슬라이스 `fogSlice`, 노이즈 윗변, 물색 혼합, `depthFade`가 F를 읽기. 수용 기준은 C 블록 그대로.
2. C 간격·레이어(B #1·#2 + 부록 B 규칙표 → `world/spacing.ts`) — 라운드 12.
3. D 해안 뭍 클립(B #3) — 라운드 13.
4. B 날씨 지면·수면·그림자 채널(C #2) + 바람 무나무 바이옴(C #4) + 스프라이트 2톤(C #1 ②) + tint 채도 보존 — 라운드 14.
5. F 갯골(B #4) · G 언덕 마루(B #5) · 구름 원반(A #4) · 물풀 섬 슬래브(A #5) · 태양 글리터(C #5) — 라운드 15.
