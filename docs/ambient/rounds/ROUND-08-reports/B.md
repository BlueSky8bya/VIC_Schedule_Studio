# ROUND-08 — Agent B(Spatial Ecology Inspector) 보고 원문(요약 보존판)

빌드 `ff70ee3` · 시드 42 · 1400×860 · `hz = h·0.26 = 224` · 도구 `.scratch-pw/r08b-*.mjs`(신규, 읽기 전용)

## 발견(P0 0 · P1 8 · P2 4)

1. **P1 S-4 — 잠긴 돌이 "접시 위 돌".** 수면선이 뒤 반원까지 그린 **닫힌 타원**이고, 그 아래가 하드 에지 평판이라 몸통이
   수평으로 잘린다. s12 x 595~685 실측: 흰 타원이 바위 몸통 **위**를 가로지르고(뒤 반원 노출), y 577에서 폭 전체 직선으로
   끊긴 뒤 y 589에서 또 직선으로 끝난다(L 편차 0.4). 원인 `art/props.ts:1129` `destination-out` 페이드가 `1 − alphaDeep`
   까지만 빼 밑단에 alphaDeep의 하드 컷이 남고, 호출부가 `ellipse(…, 0, TAU)` 링을 따로 긋는다(`coast.ts:1001`은 앞 반원이지만
   `summer.ts`·`land.ts`는 링). 계곡에도 같은 서명(s05 (870,807) 밑 **직선 세로 획 2개**). 암석해안은 접촉이 아예 안 읽힌다 —
   노두 몸통 − 조류대 **−5.4 / +8.4 / +0.9**(기준 ≥ 12).
   Fix: ① 페이드 끝을 0으로 + 밑단 6~10px 물색 100% ② 젖은 띠를 몸통색 −10L로 ③ 수면선을 함수 안에서 **앞 반원만**, 호출부 링 제거
   ④ 계곡 후류를 실제 곡선으로.
2. **P1 D-2 — 산 층 단차가 여섯 띠 중 다섯에서 8L 미만, 겨울 ②body↔③ 0.1L.** 창 `hz·0.45` 표준판으로 재측정 —
   가을 ①↔② 새벽 5.4 · 아침 7.9 · 점심 9.3 · 노을 6.9 · 저녁 6.0 · 밤 5.4. 여름이 최악(노을 6.8/**4.1**/**3.9**/6.0).
   겨울 점심 sky↔① **1.2** · ②body↔③ **0.1** · |③↔④| **5.8**. 원인: 층 색이 팔레트 상수인데 띠 조명이 **곱셈**이라
   어두운 띠에서 간격이 함께 압축된다(가을 전체 span 점심 37.9 → 밤 26.9). 클램프·평탄 run은 통과.
   Fix: **④(GROUND) 앵커 + 고정 L 스텝**, 띠 틴트는 **덧셈 오프셋**으로.
3. **P1 D-2 — 층 안에 사면 명암이 없다.** 능선 → 발치 ΔL **0.2 / −0.5 / −2.4**. 세로 프로파일이 80 → 43의 끊김 없는 단일
   램프라 "겹친 두 면"이 아니라 "한 장의 그라데이션 위 선"으로 읽힌다. `land.ts:1098` `fg`는 0~0.55가 평색이고,
   대각 광원 `lg`는 α .10/.12로 실측 1L 미만. ③ 애추 띠만 4.5L로 살아 있다.
4. **P1 S-2 — 가을 숲에 버섯이 0개.** R7의 "버섯 앵커" 수정이 버섯을 통째로 꺼버렸다 — `land.ts:470`
   `propSpots().filter(sp => sp.r >= 10)`이 도는 시점에 `claimSpot`을 부른 코드가 하나도 없다(`resetPropField()` 173행,
   숲 나무 1401·1479행) → `anchors.length === 0` → groups 0. 화면의 살빛 덩이 셋은 `shrub-autumn`이고 최근접 나무에서 121~222px.
5. **P1 D-1 — 원경 침엽수 줄에서 이웃 두 그루의 명도 방향이 반대.** (438,437) 지면 대비 **+7.8L** vs (410,443) **−6.3L**.
   원인 `land.ts:1329` 고사목 `#9aa0a0` + 안개색 0.48 혼합이 지면보다 밝아진다.
6. **P1 S-5 — 계곡 유로에 굽이 안/바깥 비대칭이 없다.** 좌·우 둔치 폭이 둘 다 7px, 소는 직선 변 3~4개의 계단 다각형,
   (607,560)에 반경 26px 소프트 원반(ADR-0017 ⑱-13 위반).
7. **P1 S-4 — 민물 물풀 섬이 잠기지 않는다.** 수면선 위/아래 몸통 L 차 **0.0**, 몸통 내부 명암 0, 뿌리 없는 줄기 획 6개가 섬 밖 물 위.
8. **P1 S-2 — 갯벌 바다 띠가 지평선 아래 땅의 28.5%**(규격 3~5% 화면). `coast.ts:32 SEA_GV_BY.tidal = 0.25` 그대로.
   물골이 x 0~430과 1260~1400(폭의 40%)에 0개, 건열은 한 구역뿐.
9. P2 S-1 — 산 중턱 침엽수 한 무리 11그루, 뒷그루 줄기 노출 0px(`claimSpot`의 stand 규칙이 반지름 기준이라 1:2.7 수관을 못 잡는다).
10. P2 S-2 — 갯벌 물골 안에 흰 조개 2개, 젖은 가장자리 획이 본류 물 위에 +3.0~5.3L로 잔존, 나란한 동차수 물골 쌍.
11. P2 S-2 — 뭍 판 열 전단으로 물가 소품이 최대 **6.1°** 기울어짐(라운드 7 조류대 추종의 부작용).
12. P2 D-1 — 원경 실루엣(34px)과 중턱 소나무(108px)가 y 30px 사이에서 **3.2배** 계단.

## (a) 라운드 7 수정 재측정

닫힘: 조류대 곡선 추종(sd 4.0) · 기슭 소품 물 안 0 · 산 봉우리 지평선 위(클램프 0.0%) · 겨울 ③ 전폭 실선 0 · 침엽수 y 구간 분리 ·
가을 다람쥐(8시드 전수, 경로 최소 v .324). 부분: 갯벌 물골(교차 0 ✓, 물 안 획 잔존) · 계곡 물 안 바위(수면선 ✓, 발밑 직선 잔존) ·
민물 물풀 섬(경계 13L ✓, 잠김 0.0) · 물가 거품(마디 ✓, 사이 24·7px). **회귀: 버섯 0개.**

## 부록 — 측정창 표준 제안

산 층은 `r08b-mrects.mjs --hz 0.26 --win 0.45`(r1 탐색을 `hz·0.45`부터). 옛 `hz−6` 창은 봉우리를 놓치고 이차 경계를 잡아
s10 노을·안개를 5.1/5.2/4.8/9.1로 **과소 보고**한다(표준판 7.8/6.9/7.8/9.1). `scripts/ambient-qa/spawn-probe.mjs`의
`HZ_V = 0.12`가 낡았다(실제 0.26). `BIOME_GRAMMAR.md` 공통 층 표의 `hz .30h`와 글머리 `.12`가 서로 다르다 → `.26`으로 통일 필요.

## 부록 C — 산 층 팔레트 목표값(렌더된 L)

④(그 계절 GROUND) 앵커 → 위로 9L씩(밤·저녁 7L), 하늘은 ① + 5 이상. 겨울만 눈 알베도 예외로 ③을 ④ 아래 11L.

| 계절 | 띠 | ⓪ 하늘 | ① 원경 | ② 중경 | ③ 자락 | ④ 발치 |
|---|---|---|---|---|---|---|
| 가을 | 점심·아침 | ≥ 87 | 82 | 73 | 64 | **55**(앵커) |
| 가을 | 노을 | ≥ 80 | 75 | 66 | 57 | **48** |
| 가을 | 밤 | ≥ 65 | 60 | 53 | 46 | **39** |
| 여름 | 점심 | ≥ 87 | 82 | 73 | 64 | **55** |
| 겨울 | 점심·아침 | ≥ 90 | 85 | 76(만년설 88) | **66** | **77**(앵커, 눈밭) |

**요약** — 가장 넓게 걸린 문제: **`drawSubmerged` + 닫힌 타원 수면선**(민물·계곡·암석해안 세 바이옴, 원인 하나).
미룰 것: 뭍 판 열 전단 6°(조류대 추종의 이득이 더 크다).
