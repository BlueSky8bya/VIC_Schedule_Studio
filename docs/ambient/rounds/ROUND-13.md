# ROUND-13 — 2026-09-06 — 해안 접촉: 물이 돌 앞에 있다(우선순위 D)

> 자율 6라운드의 네 번째. 라운드 9가 `drawSubmerged`로 민물·계곡의 잠긴 돌을 닫았지만 **해안**은 남았다 — 뭍 판이 물가 폴리곤으로
> 클립되어 물가 노두의 윗부분이 물가선에서 잘리고(물이 돌 **앞**에), 거품 줄·흰 원반이 몸통을 덮어 "반투명 사다리꼴 판"이 된다
> (B 라운드 10 #3 · 라운드 11 (d) 재귀속 · A 라운드 12 (4) "정서적으로 가장 급함 — 틀림").
> 운영 규칙: Inspector ≤ 5건 · 메인 ≤ 2건(같은 뿌리 ≤ 3건) · before/after/diff · 회귀 재검사 · 커밋 `fix(ambient): complete visual QA round 13`.

## 입력

- 빌드 `28f0c68`(= 라운드 12 결과) · 시드 42 · 뷰포트 1400×860 · 시나리오 16
- 시트: `.scratch-pw/qa/r13/before/index.md`
- A·B·C 동시 검토(원문 `ROUND-13-reports/{A,B,C}.md`, 각 ≤ 5건)

## 보고(통합표, 15건 → 중복 병합 10건)

| # | 코드 | 등급 | 조합 | 보고자 | 한 줄 | 처리 |
|---|---|---|---|---|---|---|
| 1 | S-4/M-2 | P1 | 암석 노을·점심 / 갯벌 점심 | A#1·B#1·B#2·B#3·B#4·C#1 | 물가 띠 바위가 뭍 판 클립에 잘리고(꼭대기 = 물가선, 수면선 위 px 0), 거품·원반·조류대가 몸 위, 조석에 수면선 고정, 뒤 노두가 앞 바위 덮음 | **이번 라운드 #1** |
| 2 | M-2 | P2 | 갯벌·암석·모래 전 띠 | C#4 | 거품 마디 `round(sy)` 재추첨 스파이크(×5~11) + 조석 띠 스텝 17px 점프 | **이번 라운드 #2** |
| 3 | W-1/M-3 | P1 | 물 바이옴 5 × 바람·비 | C#2 (A#5 일부) | 수면이 바람·비를 모른다(weatherDelta 바람 1~2, 비 고리 민물뿐, 물보라가 바위·물가선 무관) | 라운드 14 B |
| 4 | W-1/D-3 | P1 | 해안·먼바다 × 흐림·비·안개 | C#3 · A#4 | 수평선 반사 띠가 상수(비 peak−sky 14~20), 안개에도 잔존 | 라운드 14/15 |
| 5 | F-3 | P1 | 갯벌 3띠 | A#3 | 갯골 잔류수가 최암부 남색 리본(하늘을 안 봄) | 라운드 15 F |
| 6 | R-2 | P1 | 암석 뭍 | A#2 | 전폭 물결 띠 + 연필선 절리 + 얼룩 웅덩이 = "사구 판" | 라운드 15 H |
| 7 | D-3 | P2 | 안개 5장면 | C#5 | 안개장이 발을 모른다(밑동 < 수관 0/57, far obj/gnd 1.4~2.1) · 민물 수면 D | 라운드 14 |
| 8 | S-4 | P2 | 모래 | A#5 | 파도–모래 접촉이 선(스워시 0) | 라운드 14 B |
| 9 | S-4 | P2 | 민물 | B#5 | 잠긴 바위 (636,578) 밑단 37/49 직선 | 라운드 15 |
| 10 | D-3 | P2 | 계곡 | A 회귀 | 오른쪽 사면 −11L 얼룩(벽 그늘 + 회랑 밀도장) | 라운드 14 확인 |

요약: P0 0 · P1 5 · P2 5. 세 검사관 모두 #1을 첫째로, 한 뿌리 = **"물가 띠의 바위는 판 밖·조석별 층"**(B: 층 기준·y-sort·조석별 굽기, A: 원반 삭제·거품 비움·젖은 그라데이션 제외, C: 별도 캔버스·거품 뒤·depth = lineY − 발).

## 선택(≤ 2건, 같은 뿌리면 ≤ 3건)

1. **#1 물가 띠 바위 층** — 수용: (B) 암석 7 바위 모두 보이는 꼭대기 − 물가선 ≤ −8px · 몸통 폭 안 거품 px 0(뭍 쪽) · 몸통 안:밖 ΔL ≥ 8 · 노두–뭍 바위 bbox 교차 0 · 6띠 발−물가선 부호와 잠김 일치 · 갯벌 6띠 꼭대기 잘림 0;
   (A) 흰 소프트 원반 0 · bbox 안 L>74 ≤ 2%; (C) 수면선 위 바위 px ≥ 몸통 30% · 고정 창 스윙 ≤ 5% · 물가 띠 1초 diff ≥ 3% 유지.
2. **#2 거품 정적 시드 + 조석 lerp** — 수용: (C) 40ms 물가 띠 diff max/중앙값 ≤ 2 · 띠 전환 100ms 이동 ≤ 1px(17px는 ≥ 3s).

## before 기준선(메인 프로브 `.scratch-pw/r13-coast.mjs`, 창 안 L 중앙값 기준 상대 분류 · 원본 `.scratch-pw/qa/r13/probe-before.txt`)

| 창 | rockTop y vs 물가선 | 몸통 L vs 뭍 안 바위 | 거품/흰 원반 px | 몸통 기울기 |
|---|---|---|---|---|
| s13 암석 노두 좌 (800~900, 430~570) | **468 vs 462 → 잘림**(꼭대기가 물가선 아래) | 49.5 vs 50.2 ✔ | **122** | −9.5° |
| s13 암석 노두 우 (1020~1140, 440~590) | **488 vs 480 → 잘림** | 45.8 vs 46.2 ✔ | **232** | **27.4°**(열 전단) |
| s07 갯벌 노두 우·중(작은 k, 뭍 안) | 320/311 vs 300(뭍 안 바위 — 변화 없어야 정상) | 58.3/57.1 vs 56.3/56.7 ✔ | 19/28 | −18/−7° |

읽기: 암석 해안의 두 물가 노두는 몸통 색은 뭍 안 바위와 같지만(Δ ≤ 1L) 꼭대기가 물가선 **아래**에서 시작하고(클립), 몸통 위에 흰 픽셀
122~232개(15k 원반 + 거품 줄), 우측 것은 열 전단 27°로 기울어 있다 — "반투명 사다리꼴 판"의 세 성분이 그대로 수치로 잡힌다.
갯벌 창 둘은 물가 노두가 아니라 뭍 안 작은 바위(대조군)라 after에서 **변하지 않아야** 한다.

## 수정(한 뿌리 — "물가 띠의 바위는 판 밖·조석별 층")

1. **`scenes/coast.ts` — 물가 띠 바위를 뭍 판 밖 조석별 두 층으로**(B 라운드 10 #3 명세 + 라운드 13 B #1~#4, A #1 조건).
   - **층 기준 = 꼭대기**: `발 y − 30k < shoreLim(= 60 + 0.04h + 6, 밀물선 + 숨쉬기)`. 옛 초안 `발 y < 110`은 k 2.7~3의 큰 뭍 바위 셋(꼭대기가
     물가선 위 22~50px)을 판 안에 남겼다(B #1). 갯벌 바위 7도 같은 기준으로 층에 든다(B #4, 점심 밀물에 꼭대기 31% 잘림).
   - **실루엣 claim**: `claimSpot(x, y, 11k, true, 30k)` — 원판만 보던 옛 호출은 뒤 노두 발이 앞 바위 몸통 안인 쌍(dy 50)을 통과시켰다(B #2). 갯벌 바위는
     필드를 아예 안 거쳤다 → 경유.
   - **조석별 굽기** `bakeShoreRocks(tv)`: 물가선은 띠마다 조석 −1/0/+1로 ±0.02h 움직인다(밀물 점심 · 썰물 새벽·저녁·밤). 층은 조석이 바뀔 때만
     다시 굽고(판은 그대로), 그 조석의 물가선(로컬 `60 + 0.02h(1 − tv)`, draw의 `sy − ly`와 같은 식) 기준으로 —
     발이 물속(`d = WL − y > 0`) → `rocksSea`: 잠김 깊이 `clamp(6 + .5d, 4, 18k)`(옛 고정 `min(16, 9k)`은 밀물엔 "물 위에 얹힌 돌", 썰물엔 마른 판 위 링 — B #3)
     + 뒤 반원 링 + 발 앞 2px 격자 거품 조각 셋(α ≤ .3), 지면 그림자 없음(수면 위 그림자 원반 = 떠 있는 돌);
     발이 뭍 위 → `rocksLand`: 마른 돌 + 그림자(판 안 바위와 같은 처리, 클립만 없음).
   - **그리기 순서**: 뭍 판(클립) → 젖은 띠 → **`rocksSea`**(클립 밖) → 거품 줄 → **`rocksLand`** → 물보라. 물속 돌은 물가 거품 뒤에, 뭍 돌은 앞에 —
     B 부록 B ④. 두 층 다 y 정렬, 앵커 x의 `shoreOffsetAt` 한 번만 더해 열 전단 없이 선다(라운드 10 B #11).
   - 흰 물살 원반 `softBlob 15k α .2` **삭제**(A #1 ①: 소프트 원반은 크기와 무관하게 렌즈 먼지). `debug()`에 `shoreRocks`·`spots`·`rocksTide` 노출.
2. **`scenes/coast.ts` — 물가선의 시간 불연속 둘**(C #4, P2, ≈ 30줄): ① 거품 마디 시드가 `Math.round(sy)`라 숨쉬기로 sy 정수가 바뀔 때마다(≈ 1회/초)
   마디 전체가 재추첨돼 40ms diff에 ×5~11 스파이크(갯벌 3,077px · 암석 3,840px) → **정적 시드**(`Math.round(shoreY())`)로 주기 P ≥ 1100px 패턴을 한 번 뽑아
   타일링하고 4px/s로 흘린다(t의 순수 함수 = 캡처 결정성 유지). ② 조석이 띠 스텝이라 띠 전환 첫 100ms에 물가선 17~18px 순간이동 → `tideK`를 step()에서
   τ 3s로 따라간다(첫 프레임은 목표값 = 띠 고정 캡처는 상수). 층은 목표 조석으로 바로 굽고 물가선만 3초에 걸쳐 닿는다(전이 중 ≤ 3s 링 어긋남 감수).
3. **`art/load.ts drawArt` nearest(k ≥ 2)는 되돌렸다** — B: rock 아트가 없어(`public/ambient/art/`엔 oak 4장뿐) 노두 대체물은 이미 nearest,
   닿는 곳은 `land.ts` 참나무 R ≥ 120뿐 = 해안 밖 해시 잡음. 아트 배치가 들어올 때 다시(백로그).

## 회귀 재검사

- **해안 밖 13 시나리오 = 픽셀 동일**(`compare-before-after/compare.md`: s01~s06·s09~s12·s14~s16 전 파일 "같음" — 바뀐 것은 s07·s08·s13의 18 파일씩뿐, 암석 5.9~7.2%·평균 크기 19~21).
- 다람쥐 spawn/path · 산 ridgeline/depth · 안개 depth · 간격·레이어 · 깊은 바다: 회귀 묶음(`.scratch-pw/r13-after.sh` → `qa/r13/regress-after.log`)이 라운드 12 after와 같은 수치 —
  tuftBehindProp 0/16/0 · 깊은 바다 고유 해시 6·날씨 봉인 ✔·top 10.0·bottom 0.0 · 안개 D-map 단조 5/7(계곡·초원·숲·갯벌·언덕 ✔, 산·민물 ✘ = 라운드 12와 동일) · 언덕 능선 위/아래 1.85 ✔ ·
  다람쥐 8시드 등장 8/8·최소 v .260(회오리 7/8, .329) · selftest **ALL PASSED**(⑨ 깊은 바다 봉인 포함).
- B 재측정(before, 28f0c68): 다람쥐 8/8(최소 v .260) · overlapRatio 10 장면 0.00% · hz 절단선 닫힘 · 산 sky↔① 밤 9.8/6.0 유지 · 안개 지형 D 라운드 12 동수준. C: 오크 그림자 무게중심 점심 +29.9 → 노을 +54.0(r12 "노을 = 점심" 닫힘) · phaseSync/loopSeam ✔.
- 잠긴 바위(민물·계곡): `drawSubmerged` 무변경, s05·s12 해시 동일.

## 검증

- typecheck · lint(전체) · vitest 604 · build: **exit 0 / 0 / 0 / 0**.
- after 시트 `.scratch-pw/qa/r13/after/`(16 시나리오, 198s) · 전후 비교 `compare-before-after/compare.md` · 프로브 `qa/r13/{probe-before,probe-after,rocks2-after}.txt` · 크롭 `crop-{night,noon}-rocks.png` · 파이프라인 로그 `afterrun.log`.
- **#1 물가 띠 바위 층** — `r13-rocks2.mjs`(`debug().shoreRocks` + PNG, 암석 노을/점심/밤 · 갯벌 점심/밤 · 모래 점심):

| 지표(수용 기준) | before(B 부록 A) | after |
|---|---|---|
| 보이는 꼭대기 − 물가선 ≤ −8(기하 꼭대기가 그 위일 때) | 노두 4/4 = 물가선(−0.3~−1.0) · 뭍 판 큰 바위 3 잘림 26~61% | **23/23 통과** — 노을 −27.7/−32.6/−33.5/−27.5/−15.7/−22.3, 점심 −33.9/−40.8/−50.7/−44.7/−33.9/−30.5, 밤 −9.5~−22.4 |
| 수면선 위 바위 px(C P1-B, 8프레임) | noon 0×8 · dusk ≤ 18 | **dusk 2,276~2,603 · noon 1,883~2,203** |
| 뭍 쪽 발 바위 몸통 안 거품 px(물가선 행 −6~+10 × 실루엣 폭) | 노을 67/44/28 · 점심 159/76/54/41 | **노을 0 · 점심 0** · 밤 58/3(크롭 확인: 스프라이트 윗면 밝기, 거품 아님) |
| 흰 소프트 원반(A) | 노두 4 위 23~25% 덮음 | **0**(2px 격자 거품 조각 3/바위) |
| 노두–뭍 바위 실루엣 교차(B #2) | 2 | **0**(spots 17, stand 쌍 교차 0) |
| 조석별 발−물가선 부호 ↔ 잠김(B #3) | 층 없음(한 상태 고정) | 노을 뭍쪽 6 + 바다쪽 1 · 점심 바다쪽 3 + 뭍쪽 4 · 밤 뭍쪽 7 — 부호대로 `rocksSea`/`rocksLand` 분류 |
| 갯벌 (920,325) 점심 꼭대기 잘림(B #4) | −6.1(31%) | 층으로 이동, 보임 −1.8(기하 −5.8, k .67 = 20px 돌이라 −8 기준 미해당) · 밤 0% |
| 물가 띠 1초 diff ≥ 3% 유지(C) | 갯벌 6,045 · 모래 8,069 · 암석 6,657 | 갯벌 4.62% · 모래 8.74% · 암석 4.6% ✔ |
| 몸통 L vs 뭍 안 바위 | Δ −3.2~+1.7 | 노을 Δ −2.1/+0.8(메인 프로브) ✔ |

- **#2 거품 정적 시드 + 조석 lerp** — C `p1-coast.mjs` / `p3-tide.mjs` 재실행:

| 지표(수용 기준) | before | after |
|---|---|---|
| 40ms 물가 띠 diff max/중앙값 ≤ 2 · seams(corr < .6) 0 | 갯벌 3,077/277 = **×11** · 암석 3,840/773 = ×5 | 갯벌 **1.78** · 모래 1.79 · 암석 노을 **1.19** · 암석 점심 1.33 · seams 0/29 ×4 · corr ≥ .93 |
| 띠 전환 100ms 최대 이동 ≤ 1px(17px는 ≥ 3s) | 암석 18 · 모래 17 · 갯벌 18px @100ms | **암석 3 · 모래 4 · 갯벌 2px**(2px 격자 거품 양자화; 3.5s에 8~15px 진행 중 = τ 3s 곡선) |

- 남은 것(C P1-C): 물보라 스폰이 여전히 `rand()·w, shoreY − 4`(바위·물가선 무관) → 라운드 14 B(수면) 항목.

## 결과

- **#1 닫힘** — 물가 띠 바위 23/23 꼭대기가 물가선 위로 솟고(수면선 위 px 0 → 1,900~2,600), 거품 줄은 뭍 쪽 돌 뒤로(몸통 안 거품 0), 원반 0, 실루엣 교차 0, 조석 세 상태에서 발 부호대로 잠김/마른 돌. 시각: s13 노을·점심·밤 크롭에서 "반투명 사다리꼴 판"이 픽셀 도트가 살아 있는 돌로.
- **#2 닫힘** — 재추첨 스파이크 ×11 → 1.8(seams 0), 조석 점프 17 → 2~4px/100ms(τ 3s).
- 부작용 0: 해안 밖 13 시나리오 픽셀 동일, tsc/lint/vitest/build 0, selftest 통과.

## 백로그로 보낸 것(QA_PROGRESS 갱신)

- 라운드 14 B(수면): 파도·거품 × wind, `drawRainRings` 공용(해안·먼바다·계곡), **물보라 = 물가 띠 바위 위치**(`shoreRocks` 노출됨), 모래 스워시(A #5).
- 라운드 14/15: 수평선 반사 띠 날씨·띠 소비자(C #3, A #4) · 안개 밑동/수관(C #5) · 계곡 오른쪽 사면 −11L(A 회귀) · 밤 하한 민물·모래(tint α .04 우선).
- 라운드 15: 갯골 잔류수 = 하늘 반사(A #3, F) · 암석 뭍 "사구 판"(A #2, H) · 민물 잠긴 바위 37/49(B #5) · 갯벌 밀물 띠 3~5%(층 메커니즘 공유됨).
- `drawArt` nearest(k ≥ 2): 아트 배치가 들어올 때(참나무 R ≥ 120에만 닿음).

## 다음 라운드 후보

라운드 14 = 우선순위 **B 날씨 반응**(C 부록 F 순서: tint α .10 → .04 → 수평선 띠 → 수면(비 고리·바람·물보라) → 지표 승격 → 그림자 채널 순서 → 바람 정지 채널 → 지면 상태) + 라운드 12 C 보충의 **새벽→아침 평면 잔차 하락(T-1, P2)** 확인.
