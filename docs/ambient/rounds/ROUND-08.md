# ROUND-08 — 2026-09-06 — 시간·날씨의 두 번째 채널(그림자·바람·입자) + 산 층 팔레트

> 소유자 위임(6~10 자율)의 세 번째. 라운드 7이 **공간**(물가 접촉·간격)을 닫았으니 이번은 **시간**이다 —
> C가 세 라운드 연속 올린 P1 둘("아침 = 점심", "바람이 입자 한 열")과 산 층 팔레트.

## 입력

- 빌드 `ff70ee3`(= 라운드 7 결과) · 시드 42 · 뷰포트 1400×860 · 시나리오 16
- 시트: `.scratch-pw/qa/r08/before/index.md`
- A·B·C 동시 검토(원문 `ROUND-08-reports/{A,B,C}.md`) — 초점은 "라운드 7이 닫았다고 한 것이 정말 닫혔나" + 다음 기준선.

## 선택(입구 묶음 4)

1. **그림자 채널**(AMB-T1-03, C 3라운드 연속) — `light.ts` BAND · `art/props.ts propShadow` · 생물 그림자.
   대상: 소품·나무가 있는 s01 · s02 · s03 · s04 · s05 · s06 · s15 + 생물이 있는 s03 · s12.
2. **바람 삼단 반응**(AMB-W1-03 = M3-02 = M3-03 = M1-01) — `land.ts drawTree`·언덕 억새 층 · `coast.ts` 물보라.
   대상: s01 · s02 · s13 · s15(+ 바람 시트 전부).
3. **입자 원근 + 밤 지면 하한**(C P2 둘) — `world/particles.ts` · `world/light.ts` 밤 `mul`.
   대상: 비·눈 시트 전부 + 밤 띠 전부.
4. **산 층 팔레트**(라운드 6 결정 1 + B·C) — 목표값은 B의 라운드 8 보고를 받아 정한다.

## 수정

### ① 그림자 채널

- `light.ts BAND`: 새벽·저녁 `shadow.alpha` .55 → **.80**(C 실측: 코어 하강 1.98L, 기준 4L) · 아침 `len` 1.2 → **1.3**, `alpha` .9 → .95.
- `art/props.ts propShadow`: **면적 보존** — `a = a0 · s.alpha · (0.55 + 0.45·stretch)`. 늘어난 그림자는 같은 알파로는
  단위 면적당 농도가 떨어져 코어가 4L을 못 넘겼다.
- `scenes/util.ts drawCreatureShadow` 신설 — 생물 그림자도 소품과 **같은 계약**(발치 고정 · 끝만 dx · 길이 stretch · 면적 보존).
  라운드 4가 소품·나무만 옮겨서 "새벽 한 화면에 해가 둘"이었다(C). 다람쥐·봄 생물부터 적용.

### ② 바람 삼단 반응

- `land.ts drawTree`: `wind ≥ .15` **계단 제거** → `max(.05, wind)`. 진폭·주기·지연이 **크기의 함수**가 됐다
  (작을수록 크게·빠르게·먼저: `amp = wind·(3.4 − 2.4·sz)` · `freq = 1.7 − .9·sz` · `lag = (.2 + .3·sz)·freq`).
- **언덕 억새를 자기 층으로** — 바탕에 구우면 정지판이다. `grassC`에 굽고 `draw()`에서 세로 띠 40px마다
  `sin(u·0.008 − t·spd)` 진행파로 흘린다(속도·진폭은 바람 비례, 맑음에서도 멈추지 않는다).
- `coast.ts` 물보라: 발생률 × (1 + 2·wind) · 개수 5 → 5+5·wind · 초기 속도 × (1 + .6·wind).

### ③ 입자 원근 + 밤 하한

- `particles.ts`: `d`를 **화면 y의 함수**로(`depthOf`) — 지평선 위는 `d ≤ .2`(먼 비 베일), 아래는 .15 → 1.
  떨어지며 굵어지도록 `step`에서도 갱신. 지평선이 .26으로 오르며 하늘에 떨어지는 비가 12% → 26%로 배증했다.
- `light.ts` 밤 `mul` 184 → **194**: 밤은 곱셈이라 ΔL이 바탕 L에 비례해 **밝은 바이옴이 먼저** 하한을 깬다
  (민물 봄 −18.74 · 초원 여름 −16.3 · 언덕 가을 −16.06, 하한 −16).

### ④ 산 층 팔레트 — **상수 hex를 버리고 ④ 앵커 + L\* 스텝으로**

B 부록 C의 처방을 그대로 받되, 그가 지적한 압축(띠 조명이 곱셈이라 어두운 띠에서 간격이 함께 눌린다)을 감안해
스텝을 표(9L)보다 크게 잡았다. 새 헬퍼 `labL`/`setLabL`(land.ts) — 색상·채도는 두고 **L\*만** 목표로 옮긴다.

- `anchorL = labL(GROUND[kind][season] @ v .76)` = ④ 발치 땅.
  **① = min(anchorL + 31, 84) · ② = ① − 12.5 · ③(애추 띠) = max(anchorL + 9, ① − 23.5)**.
  겨울만 눈 알베도 예외(MOUNTAIN §4): ① = ④ + 6 · ② = ④ − 6 · ③ = ④ − 17.
- **①에 천장 84가 필요했다.** 밝은 지면(여름 L\* 58 · 봄 64)에 앵커 + 31을 그대로 주면 ①이 89~95로 올라 하늘을 뚫는다
  (실측 여름 하늘↔① 6.8 → **1.7~4.6**). 천장을 걸자 4.9~11.1로 돌아왔다.
- **봄·여름 산자락(④)을 어둡게** — `GROUND.mountain.spring/summer`를 가을(L\* 50) 수준으로 내렸다
  (`#dce1d6→#ccd2c6` / `#d1dacc→#c2ccbd`, 하단도 같이). 발치 L\* 58~64에 하늘까지 20~26L밖에 없으면
  **≥ 8L 단차 넷(= 32L)은 물리적으로 안 들어간다**. 이 한 줄이 여름 ②↔③를 1.9 → 6.5로 바꿨다.
- **사면 명암**(B #3): `fg` 램프를 **보이는 구간 안으로** 옮겼다(0 → +4L · .30 → +1 · .62 → −7 · .80 → −9 · 1 → 땅색).
  옛 평색 구간 0~.55가 하필 화면에 남는 띠 전체였다(①의 발치는 ②·③이 덮는다) — 그래서 층이 "선 하나"였다.
  대각 광원 α도 .10/.12 → **.20/.22**(실측 1L 미만이었다).
- **침엽수 실루엣은 언제나 그 y의 지면보다 어둡다**(B #5): 절대 hex를 버리고 `setLabL(hazed, labL(지면색) − 7)`,
  고사목은 색이 아니라 **얕은 ΔL(−3)**로 구분. 이웃 두 그루가 +7.8 / −6.3으로 갈리던 것이 원인이었다.

### ⑥ 라운드 7 잔여를 닫는다(하늘 판 · 그리기 순서 · 회귀)

- **흐림 덮개 부호**(C #2) — `bakeClouds`의 덮개를 지평선 회색 평면이 아니라 **그 y의 하늘색 −6L 세로 그라데이션**으로.
  팔레트는 R7에 고쳤는데 덮개가 되돌리고 있었다. 어두운 띠는 오프셋도 크게(`skyPalette` cloud: 밤 −40 · 저녁 −22 · 새벽 −13).
- **안개 천정을 천정에서 만든다**(C #3) — 옛 `add(z, −13)`은 **지평선**에서 파생해, 밤처럼 "천정은 어둡고 지평선은 밝은"
  설계에서 안개 하늘이 맑은 밤보다 +19.9L 밝아졌다. 이제 `mix(top, [200 206 212], fkTop)`(밤 .04 · 저녁·새벽 .1 · 낮 .36),
  지평선은 `mix(hz, …, fk)`(밤 .34 · 저녁·새벽 .56 · 낮 .8) → 세로 폭이 저절로 생긴다.
- **바람 새털구름을 두 층 모두**(C #5) — `pal.cirrus && share < 0.5` 게이트 탓에 가까운 층이 빈 캔버스라
  가장 바람 센 날씨에 시차가 없었다. 가까운 층은 더 굵고(th0 5~8) 적게(2~3) 낮은 고도에.
- **해·달·별 → 지평선 띠 순서**(C #1) — `land.ts`가 R7 수정에서 빠져 있었다. `drawSkyLive`를 `drawImage(horizon)` **앞**으로,
  상한도 `summer.ts`와 같은 식(`min(hz·0.92, hillCrestY − 4)` / `hillCrestY − 14`)으로 통일.
- **육지의 정지**(C #4) — `bakeGrass(n, sizeK, alphaK)` 헬퍼를 만들어 **숲·언덕·산**의 풀포기를 전부 움직이는 층으로.
  라운드 8 초반에 언덕만 옮겼던 것을 세 바이옴으로 넓혔다.
- **버섯 회귀**(B #4) — 버섯 블록이 `resetPropField()` 직후·나무 배치 **전**에 돌아 앵커가 0개였다.
  나무 목록(`trees`)이 채워진 **뒤**로 옮기고 앵커를 `trees` + `propSpots().stand`로.
- **여울이 차선 대시**(A) — `iA ± 3`에 16획이 몰려 폭 방향 일렬이었다. 상·하류 ± 14 마디에 종 분포로 22획, 중심에서
  멀수록 옅고 짧게, 획 자체도 곡선으로.
- **물 위 빛의 길 14행 사다리**(A) — 등간격 16.8px 하드 에지 사각 14장을 **스프라이트 한 장**(사다리꼴 × 가로 페이드 ×
  세로 감쇠를 128×256에 굽는다)으로 바꾸고 살아 있음은 전단 한 번으로.
- **봄 참나무 채도**(A) — 최대 .92 · 픽셀 57%가 .35 초과로 여름(.59 / 28.7%)·가을·겨울과 다른 세계였다.
  새 `scripts/ambient-art-satcap.mjs`(무릎 위만 비율로 압축)로 **최대 .45 · 25.6%** — 네 그루가 한 세트가 됐다.
- **봉인 장면 자체 검사**(도구) — `selftest.mjs` ④가 깊은 바다에서 늘 FAIL이었다(설계상 시간대를 안 받는다).
  `sealed` 분기를 넣어 **"픽셀 불변"을 반대로 검사**한다.

## 검증

게이트: tsc **0** · eslint **0** · vitest **604** · `next build` **exit 0** · `ambient-qa/selftest` **ALL PASSED**(16 시나리오).

| 항목 | before(ff70ee3) | after | 기준 |
|---|---|---|---|
| 흐림 천정 Δ맑음(초원·여름 6띠) | +7.50 / −0.20 / +2.45 / +3.60 / +13.04 / +14.67 | **−3.29 / −7.66 / −4.34 / −4.80 / −3.55 / −0.68** | ≤ −3(밤만 미달) |
| 안개 하늘 세로 ΔL(6띠) | 1.48 ~ 2.99 | **6.22 ~ 18.44** | ≥ 6 ✔ 6/6 |
| 안개 천정 Δ맑음 | +8.0 ~ +27.4 | **+3.30 ~ +8.56** | ≤ +6(4띠 초과, 최대 +2.6) |
| 산 s10 가을·안개 sky↔① | 4.6 ~ 8.8 | **6.7 ~ 8.9** | ≥ 4 ✔ |
| 산 s10 ②↔③ / ③↔④ | 3.3 ~ 11.8 / 8.7 ~ 10.3 | **6.7 ~ 11.7 / 8.1 ~ 11.9** | ≥ 8(밤 6) |
| 산 s11 여름·흐림 ①↔② | 3.9 ~ 5.5 | **6.4 ~ 10.6** | ≥ 8(밤 6) |
| 산 s11 sky↔① | 6.8(노을) | **2.4 ~ 11.1** | ≥ 4(저녁 미달) |
| 가을 숲 버섯 | 0개(크림 픽셀 0) | **161 크림 픽셀** | 4~6개 ✔ |
| 언덕 해·별이 마루(y 153) 아래 | 해 원반 2/3 · 별 12점 | **해 0 · 별 0** | 0 ✔ |
| 지평선 아래 1초 변화 | 숲 0 / 0 · 산 0 · 언덕 0 | **숲 16,398(점심) · 10,058(밤) · 산 1,539 · 언덕 12,580** | 맑음 ≥ 3,000 · 밤 ≥ 1,000 ✔ |
| 봄 참나무 채도 | max .92 · >.35 57% | **max .45 · 25.6%** | 여름(.59 / 28.7%) 이하 ✔ |
| 깊은 바다 봉인 | 해시 동일 | **동일 유지**(selftest ④ 봉인 분기) | 불변 ✔ |

## 남긴 것(라운드 9 입력)

1. **`drawSubmerged` + 닫힌 타원 수면선**(B #1, 세 바이옴 여섯 곳, 원인 하나) — 라운드 9의 **첫 입구**.
2. **띠 틴트를 덧셈 오프셋으로**(B #2 처방의 나머지) — 지금은 곱셈이라 새벽·밤에서 ①↔②가 5.4로 눌린다.
   실측으로 확인한 구조적 사실: **가을 새벽의 하늘↔④ 폭이 26.6L**이라 ≥ 8L 단차 넷(32L)이 들어갈 자리가 없다.
   폭을 넓히거나(어두운 띠의 하늘을 올린다) 단차 기준을 띠별로 나눠야 한다.
3. 겨울 ②body↔③ 1.4~2.4(만년설·눈밭 사이) · `r08b-mrects.mjs`의 ③ 창이 침엽수 벨트와 겹쳐 계절에 따라 나무를 잰다 —
   **측정창을 나무 제외로 다듬는 것이 먼저**다.
4. 갯벌 바다 띠 28.5%(규격 3~5%) · 물골이 화면 폭의 40%에 없음(B #8).
5. 계곡 굽이 안/바깥 비대칭 · 민물 물풀 섬 잠김(B #6·#7) · 눈 쌓임 상태 변수 · 비의 젖음 채도(C #7·#9) ·
   달을 구름 뒤로(C #12) · 나무 밑동 고정 전단(C #8).

### ⑤ 라운드 7 잔여 · 검토 요청 반영(예산 밖 소품)

- **노을 채도 예산**(A #10 · C P2) — `BAND_SKY.dusk` 천정을 회장미(hue 330° · 채도 .17)로. 렌더된 천정 채도가
  .06~.09로 규칙 상한(.3)의 1/4만 써서 "하늘이 가장 색을 갖는 시간"이 가장 무채색이었다. A는 .18~.30, B·C는
  절제를 권해 **중간값 .17**로 잡고 테스트(`ambient-sky.test.ts`)로 못박았다(채도 .12~.26 · R − G ≤ 45).
- **지면 리듬**(A #7) — 가을 초원의 잔가지·조약돌 y 분포를 `pow(u, .6)`로 눌러 근경이 원경의 3배 이상
  촘촘해지게 하고, 저주파 함수로 **빈 구간**을 남긴다(8밴드 최대/최소 1.7배 = 벽지였다).
- **바닥 소프트 원반**(A #9) — `softBlob`이 **누운 얼룩**(sy ≠ 1)이고 반경 ≥ 36px이면 3단 알파 + 굴곡진
  가장자리(2px 격자) 폴리곤으로 그린다. 빛무리(달·해·해파리 — sy = 1)는 그대로 매끈하다.
  ⚠ 흔들림은 좌표 해시로 만든다 — 여기서 `rng()`를 부르면 호출 쪽의 결정적 난수 흐름이 밀린다.
- **심해 실루엣 평탄화**(A #8) — `loadSprite(..., flatten)`가 알파를 3단으로. 해파리 60×60에 고유색 503개였다.
- **언덕 띠**(B 라운드 7 잔여) — `hillRidge`와 실제로 칠하는 `ridge`가 5.5px 어긋나 있었다(비례 식 vs 절대 26/9) →
  **한 식으로** 통일하고, 띠마다 파장·진폭을 다르게(뒤 띠일수록 길고 낮게).
- **눈 하늘**(C P2) — 눈이 하늘을 +0.8L 밝히고 있었다(규칙 −6) → 천정 −7 · 지평선 −6.

## 검증

(after 캡처 뒤 기록)
