# ROUND-23 — 초원 계절 해 높이·근경·세로선

Status: 로컬 구현·검증 완료, 소유자 시각 확인 대기 · 2026-09-11 KST

소유자 요청: 기존 코드의 계절별 태양 고도를 실제 초원 화면에 반영하고, 풀 몇 조각뿐인 근경을 보강하며, 왼쪽 약 10%의 세로선을 조사·수정한다. 기존 초원 사계절 소스, .35 하늘 비율, 넓은 공간과 중앙 빈터, 등비율 풀, 감상 모드/모바일/자동조절을 유지한다. 다른 바이옴 아트 생성 및 푸시는 범위 밖이다.

세 수정 묶음: (1) 날짜 기반 낮 태양 표시와 고도 투영, (2) 기존 생성 근경을 비대칭 여러 무리로 배치하고 가까운 크기/화면 하단 가림 강화, (3) 재현한 지면 세로 경계 제거. 코드로 배치와 합성을 개선하며 원본 PNG는 보존한다. 읽기 전용 A/B/C 독립 검토 및 고정 빌드 전후 캡처, 관련 단위/타입/린트/빌드와 화면 검사를 수행한다. 서버 권한·공개 DTO·DB·KST 날짜 선택·심해 봉인은 변경하지 않는다.

## 결과

- 초원만 `solarPath`를 켜 실제 날짜의 절대 고도 0–90°를 하늘에 투영한다. 봄/여름 초원·가을·겨울의 고정 `sunY`를 제거했다. 아침·낮에도 해가 보이며 일출 부근 투명도를 부드럽게 변화시킨다. 상단 여백은 원반보다 크고 실제 지평선으로 클립한다. 하늘→천체→구름→먼 능선 순서라 구름 뒤에 놓인다. 흐림·비·눈의 기존 원반 숨김과 안개의 빛무리를 유지한다. 다른 바이옴과 심해는 새 경로를 사용하지 않는다.
- N은 기존 원본으로 좌우 각각 세 무리를 높이·크기·간격을 달리해 한 번 굽는다. 밑동은 화면 하단에 걸리고 안쪽 48px은 서서히 옅어진다. 중앙 절반 이상은 열린 상태다. N 캐시 높이는 192px이지만 할당 전 기존 1MiB 상한을 적용한다. 추가 원본·대형 캐시·매 프레임 합성은 없다.
- 세로선 조사: 5120 화면의 기존 M 반사축 x436(8.52%), N 잘림 x499(9.75%). 원본 캡처에서 빈 픽셀 틈은 재현되지 않았으며 문양의 반사 접힘이 유력했다. 반사 타일을 같은 방향의 타일로 바꾸고 12% 여백을 32단 smoothstep으로 겹쳐 기존 지면 굽기 때 합성한다. 풀 등비율과 전체 원근 진행을 유지한다. N의 직선 잘림도 안쪽 페이드로 완화했다.

## 검증

- 통합 단계 전체 테스트 73파일/809개 통과. 최종 소스 관련 테스트 13개, typecheck, lint, build 통과.
- 지면/N 확정 빌드 `O-vD_P9IZtpWMCyRRQqTJ`: 사계절 4비율 소스/색경계/오류/실패굽기 26검사 통과 (`.scratch-pw/qa/r23/final/season-check.json`). 초원 9화면비·날씨·모드·모바일 35검사 통과 (`r23/responsive/results.json`). selftest 3시나리오 결정성·정지·시간 전환·심해 봉인 전체 통과. 이후 변경은 태양 상단 여백과 일출 투명도뿐이다.
- 최종 빌드 `f8aW__NliQ3_ifzkKWI-I`: `meadow-sun-check.mjs` 실제 화면 검사 10개 통과. 2026년 각 계절 대표 달 15일 12 KST에서 사계절 해 픽셀 확인, 여름 고도와 화면 위치가 겨울보다 높음 확인. `.scratch-pw/qa/r23/sun/{results.json,*-12.png,winter-*.png}`. 첫 검사에서 봄 해가 파란 하늘에 비쳐 노란색 기준에 잡히지 않는 오탐이 있었고 실제 픽셀을 확인하여 흰색 원반을 측정하도록 수정했다. 눈 능선을 포함하지 않도록 탐색 범위를 하늘 상부로 제한했다.
- 고정 seed42 전후 여름 6시간대 캡처: `.scratch-pw/qa/r23/{before,final}/s16-meadow-summer-noon-clear/`. 전체 계절·화면비 지면/N 캡처: `r23/final/final/<season>/` (태양 여백 최종 조정 전). 최종 태양 캡처는 위 `sun/` 및 최종 시간대 폴더 사용.
- A: 양쪽 근경 강화 및 중앙 빈터 유지 확인. B: 여섯 장 비교에서 약10% 세로 접힘 해소, 새 세로 띠·빈틈 없음 확인. C: 초원만 적용, 구름/능선 가림 및 기존 제어 유지, 대형 캐시/루프 증가 없음 확인. C의 상단 여백·일출 급변 P2를 최종 수정했다.

남는 시각 한계: 같은 지면 원본을 이어 쓰므로 일부 대각선 풀무늬 반복은 남는다. 봄 N은 지면보다 옅어 별도 무리 느낌이 강하고, 다른 계절은 더 은근하다. 실제 저사양 기기·OBS/GPU 비용을 새로 측정하지 않았다. 소유자 최종 시각 수용 및 푸시는 별도이며 이번에는 푸시하지 않았다.
