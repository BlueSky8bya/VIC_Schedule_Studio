# ROUND-11 — Agent A(Art Mood Director) 보고 원문(요약 보존판)

빌드 `6e7d193` · 시드 42 · 1400×860 · 도구·크롭 `…/scratchpad/a11/`

## 발견(P0 0 · P1 2 · P2 3)

1. **P1 D-1 — 안개가 화면 전폭 세로 그라데이션 + 균일 탈채도 한 장 = "안개 필터"**(16/16 안개 칸).
   ΔC(채도) 하늘~발치 **어디서나 −2~−7**(s12 근경 −5.2 vs 원경 −0.3~−1.7 — 근경이 더 빠짐; 원인 `light.ts` fog `desat += 0.2`).
   ΔL 최대 +3~+8인데 **깊이 순서가 아니다**: s10 x=700 열 ① +2.5~4.0 · ② **+0.5~1.4** · ③ +2.4~3.4(② < ③ 역전, 층 사이 누적 0).
   **수면 무반응**: s12 물 ΔL −0.2~+0.4 · s13 수평선 −0.5 · s07 −0.6 — 안개인데 수평선·먼 섬이 선명. 지면 안개 띠는 `hz + .3(h−hz)`
   고정 y 전폭 직선 — 계곡 바닥·연못·오목부에 고이지 않고 능선·마루 무관(s06 상류 첨점이 맑음과 같은 자리).
   Fix = 라운드 10 C #3 처방(밀도장 F(x,y) 깊이항 × 고도항 `fogFloor(x)` + 노이즈 윗변, `depthFade`가 F를 읽기, `desat` .2 → ≤ .06,
   수면을 far edge로 갈수록 hazeRgb로 mix, 산 ①②③ 층별 누적, 뭉치를 저해상 노이즈 덩이로).
   Acceptance: ΔL y 단조(hz ≥ +8 · mid +3~5 · near ≤ +1.5, ≥ 13/16) · ΔC(near) ≥ −1.5 · s10 열 ① > ② > ③ > ④ · s12 수면 상반 ΔL ≥ +3 ·
   수평선 대비 ≤ 4L · 안개 윗변 x 방향 sd ≥ 20px · 뭉치가 원반으로 안 읽힘.
2. **P1 F-1 — 안개 날 하늘에 낱개 구름 원반이 그대로**(새벽·저녁·밤엔 하늘보다 어두운 얼룩), 해·달은 그냥 사라진다 = "맑은 날 + 회색 필터, 해 지움".
   `skyPalette` fog `cover` .12 > 0 → `bakeClouds`가 낱개 구름을 그리고 밑면을 −12로 어둡게. `drawSkyLive`는 안개면 halo 없이 소실.
   Fix: fog `cover` 0 + 하늘 판 저대비 단일 그라데이션(ΔL ≤ 6) + 저주파 밝기 얼룩 한 층(α ≤ .08) · 해·달은 **큰 저채도 halo**(반지름 ×3, α .12~.2,
   원반 없음) — "빛은 있는데 방향이 없다"가 안개의 정서. **안개만** 손댄다(라운드 15 구름 형태 항과 분리).
3. P2 T-2 — 따뜻한 팔레트(가을) 저녁·밤 근경 크로마 ×.51~.55 / ×.37~.41 잔존(초록은 ×.67/×.54로 규칙권 진입). 원인 = 청회 tint·원경 띠 gf·drawDepthHaze.
   라운드 14 예정 유지.
4. P2 S-3 — 산 ③ 자락 "바코드 줄"(높이 4~6px 세로 획 한 줄 정렬, s09·s10·s11) + 겨울 10px 주기 세로 빗살(y405~470). 라운드 10과 픽셀 동일(잔존).
5. P2 T-1 — 육지 "아침 = 점심"(천정 아침 79.3 > 점심 76.9 역순, 서 있는 물체 없는 바이옴은 그림자 채널 안 보임) · 깊은 바다 "새벽 = 노을"(Δ>12 픽셀 **0**).
   Fix(C 몫): 아침 천정을 점심보다 2~4L 낮게, 가로 광 하한을 아침에도 · `deep.ts` 빛줄기 기울기 부호를 해 방위로(새벽 +, 노을 −).

**요약** — 가장 넓게 걸린 문제: **#1 안개 밀도장 부재**(#2 안개 하늘은 같은 라운드에 닫히는 형제). 안 고쳐도 되는 것: #3(라운드 14) · #4(잔존, 회귀 아님).

## (1) 라운드 10 판정

천정 L 순서(s16): 새벽 56.5 · 아침 79.3 · 점심 76.9 · 노을 61.8 · 저녁 45.7 · 밤 38.7 → 점심 > 노을 > 새벽 > 저녁 > 밤 ✔(노을−새벽 5.3 · 저녁−밤 7.0), 밤 ≤ 36 근접 미달, 아침 > 점심 역순(#5).
**새벽↔노을이 갈린다 ✔**(색상각 h273 vs h350 — "존재"가 아니라 "읽힘"). 저녁↔밤은 하늘로만(Δ7 + 별); 지평선 잔광이 화면에 안 남는다(원경 띠·안개가 덮음).
밤 = 하늘은 "깊음"(남청 판 + 별 + 림 광), 땅은 아직 "어두운 저녁". 크로마: 초록 규칙권 진입, 가을 미달(#3). 가로 광: s13·s04 노을 지면 좌우 기울기 **육안으로 읽힌다** — 첫 공간 채널로 작동.
**깊은 바다 6띠는 시간을 말한다 — 세 단계로**(점심/아침 빛줄기 4 · 새벽/노을 기울어진 2 · 저녁/밤 0~미세 + 발광 ↑). 상단 점심−밤 7.9~10.0. 새벽=노을은 P2.

## (2) 안개 — 박스형 필터다(세 시나리오 + 16칸 전부)

s06 계곡: 상류가 맑음과 같은 자리, 띠는 강 골이 아니라 y .3 전폭 · s10 산: ①②③ 어디에도 안 쌓임(② +1 · ③ +3 역순), 해가 없어져 방향 없는 회분홍 판 ·
s12 민물: 물이 가장 안 바뀐 표면(ΔL 0). **공간으로 읽히게 하는 우선순위**: ① 깊이 단조 + 원경 소실(실루엣 ≥ 10L은 남김) ② 저지대 체류(윗변 노이즈 sd ≥ 20px)
③ 수면 반응(연못 상반 ≥ +3 · 수평선 소실 ≤ 4L) ④ 근경 채도 보존(ΔC ≥ −1.5) ⑤ 하늘 정서(구름 원반 0 · 해·달 halo) ⑥ 뭉치 = 저해상 노이즈 덩이.

## (3) 회귀 — 없음

스캔라인 0 · 산 ① 불투명 · 안개 절단선 0 · 잠긴 바위 · 롤리팝/봄 참나무 · 라운드 10 밤 천정 값 유지. #4는 라운드 10과 픽셀 동일(잔존).
