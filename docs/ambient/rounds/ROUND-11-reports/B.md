# ROUND-11 — Agent B(Spatial Ecology Inspector) 보고 원문(요약 보존판)

빌드 `6e7d193` · 시드 42 · 1400×860 · 도구 `.scratch-pw/r11b-{fogprof,hgrad,treehalf,deepbands}.mjs`(신설) + r08b/r09b/r10b 재사용

## 발견(P0 0 · P1 3 · P2 1)

1. **P1 D-1 — 안개 밀도가 지형을 모른다: 세 안개 시나리오 전부 F(x,y) = F(y).** 행마다 7열 ΔL sd ≤ 1.5L. 산 ①②③④ +2.6/+2.9/+2.3/+1.2
   균등(규칙 .55/.3/.1) · 민물 물 위 ΔL 0, 기슭 띠 +3(거꾸로) · 계곡 물길 회랑 +0.8~1.2 vs 같은 v 둔치 +2.4~3.6(거꾸로), 물이 안개 속에서
   가장 채도 높은 물체(크로마 29 vs 둔치 5). 코드: `groundFog` 세로 그라데이션 두 장 + 고정 띠 · `drawDepthHaze` 세로 한 장 · wisp y 자유 — 셋 다 x·지형 항 없음.
   Fix: `Scene.fogFloor(x, f)` + F(x,y) = D(y) · [1 + P(x,y)], P = Σ poolᵢ·exp(−max(0, floorᵢ(x) − y)/τᵢ) + water(x,y). 안개색은 상수가 아니라
   **그 띠 렌더 하늘의 지평선 스톱 − 4L**(그래야 ①이 하늘을 안 뚫고 D①을 키운다). 장면별 floor·목표 비는 부록 A.
   Acceptance: s12 물 ΔL ≥ 2× 기슭 띠 · s06 회랑 ≥ 1.5× 둔치(상류 ≥ 2.5×), 안개 속 물 크로마 ≤ 둔치 + 4 · s10 부록 A 산 표 · 지형선을 지나는 행 ≥ 60%에서 7열 sd ≥ 2L.
2. **P1 D-1 — 라운드 10 ②(지면 가로 광) 회귀**: `fillRect(0, hz, w, h − hz)`에 세로 램프가 없어 y = hz에서 풀 세기로 켜진다 → 그늘 쪽 화면 40~50%에
   지평선 절단선(한 행 −2.5~−5.1L: s04 dusk −2.5/−5.0 · s16 −2.8/−5.1 · s02 −2.6/−4.7 · s10 −6.0/−8.8), 해안 그늘 쪽 수평선 대비 +5.9 → **+0.1**.
   라운드 3 C#6이 잡았던 "지평선에서 곧장 켜짐"이 반폭으로 되돌아온 것. 부작용 없는 것: 좌우 끝 경계 띠 없음 · 수관 비대칭 없음(필드 채널로만) ·
   지면 끝차 −3~−4L 의도대로. Fix: (a) 화면 전체에 걸고 skyK ×.6 (b) aboveHz(.04) → groundYAt(.06) 세로 알파 램프.
   Acceptance: 그늘 쪽 두 구간 − 해 쪽 두 구간 ≤ 1L · hz±20 행간 최대 계단 ≤ noon + .5 · 해안 그늘 쪽 수평선 계단 ≥ noon의 60%.
3. **P1 D-2 — 라운드 10 하늘 프로파일 뒤 산 ⓪↔① 단차가 밤 8.4 → 1.2(s10) · 7.1 → 1.9(s11), 새벽 5.8 → 3.3 · 6.9 → 4.9(s09).**
   원인: 밤 지평선 "246 248 252" → "190 200 216"(−17L)로 **렌더 하늘**이 내려왔는데 ①은 굽기 때 GROUND 앵커(띠 무관 `min(anchorL+31, 81)`)로
   정해지고 `gf` 원경 띠가 옛 α 그대로. 밤 s10: sky 55.1 / ① 53.8 / ② 50.7 / ③ 48.1 / ④ 41.7.
   Fix: ①·②를 **렌더 하늘 기준**으로 — `shadowKey` 재굽기에 `skyPalette(season, f.weather.now, f.time.band)` 지평선 스톱 L을 넘겨 ① = sky_h − max(6, …).
   라운드 9의 실패는 `lt.sky`(틴트 색)를 읽은 것이지 띠 굽기 자체가 아니다. 하늘 값을 되돌리는 것은 금지(라운드 10 결정).
   Acceptance: s09/s10/s11 × 밤 sky↔① ≥ 6, 새벽 ≥ 4 · ①↔② 현행 이상 · 점심 불변(±.5).
4. P2 R-2 — 갯벌 바다 띠가 점심(밀물) 81px = **9.4%**(규격 3~5%), 저녁(썰물) 5.9% — 라운드 10의 "5.9% 근접"은 썰물만 잰 값.
   Fix: `SEA_GV_BY.tidal` .08 → .05 또는 tidal 조석 진폭 .02h → .01h; 밀물은 물가선이 아니라 갯골 하구 채움으로(라운드 15 F와 한 벌).

**요약** — 가장 넓게 걸린 문제: **안개장이 지형을 모른다**(#1) — #2 hz 이음매는 "화면 전체 그라데이션 한 장"이라는 같은 뿌리, #3은 ①이 렌더 하늘을
모르는 같은 종류의 "굽기 시점 무지". 안 고쳐도 되는 것: #4(라운드 15) · 물풀 섬 잠김 단차(15) · 산 ②↔③ 밤·안개 3~4L(구조 한계).

## 부록 A — 안개 × 지형 목표(fogFloor 후보 · 체류:노출 비)

| 장면 | fogFloor(x) | 체류(고밀) | 노출(저밀) | 목표 비 |
|---|---|---|---|---|
| 언덕 | `hillRidge(i, x)` i = 0,1,2 | 능선 i 바로 위(뒤) 0~.05h 띠 | 능선 마루 아래 사면 0~20px | 3:1 / 2:1 / 1.3:1 |
| 산 | `ridge1` · ② 능선(저장 필요) · `screeBand.top/bot` | ① 창 + ②의 발(yTop 위 40px) | ② 능선 아래 40px · ③ · ④ | 4:1(④ ≈ 0) |
| 계곡 | `stream[i]` 최근접점 + `halfAt` | 물길 회랑(≤ 2.6·halfAt) + 상류 끝 | 사면 벽 v > .3 | 1.6:1(상류 2.5:1) |
| 민물 | `waterTopAt(x)` ~ `waterBottom()` | 물 위 먼 절반(v .3~.5) | 기슭 띠·가까운 기슭 | 2:1 |
| 해안 3 | `shoreY() + shoreOffsetAt(x)` + tide | 바다 띠 + 젖은 전빈 | 후빈·사구·방풍림 | 2:1, 수평선·먼 섬 먼저 소실 |
| 초원·숲 | `groundYAt(v)` 평지 | 숲: 나무 줄 뒤 v .3~.45 | v > .6 | 1.5:1 |

산 다섯 층 목표 D(노을 s10): ⓪ +2~4 · ① min(+8, sky_fog − 4 − ①clear) = 노을 +4.1 / 아침 +8 · ② 능선 ≤ +2 · ② 발 ≥ +5 · ③ ≤ +2.5 · ④ ≤ +1.5. 비 (① + ②발) : (②능선 + ③) : ④ ≈ 4 : 1 : 0.3.

## 부록 B — 라운드 10 재측정(표준창)

산 sky↔①: 밤 s09 4.7 · s10 **1.2** · s11 **1.9**(r10 before 5.7 / 8.4 / 7.1) · 새벽 4.9 / 3.3 / 3.3(6.9 / 5.8 / 4.0) → #3. 그 밖은 라운드 10과 동일
(s11 ③↔④ +4.6~6.4 유지 · s09 cap↔③ 5.1~7.4 · s10 ③↔④ 6.5~11.1).
`drawSubmerged` (a) 밑단 최장 수평 직선 15/45 · 11/51px(옛 창 오판 교정, 닫힘 유지) · (b) −10.1/−13.3L ✔ · (c) ✔ · (d) 해안 열림(라운드 13) · (e) 섬 단차 1~2L 미달(15).
다람쥐 8/8 · 경로 최소 v .324 · 숲 나무 간격 r10과 동일 세 쌍 · 깊은 바다 3채널 층 질서 유지 ✔(바닥 1/3 med 4.8 전 띠 동일, 위 1/3 28.9 → 22.6).

## 부록 C — 라운드 12(C 간격·레이어) 사전 명세

**C-1 `world/spacing.ts`**: `createSpacing()` → `{ reset, claim(x, y, r, kind, hy?, group?), probe(...), behindStand(x, y), spots() }`, `PAIR[A-B] = [a, b]`(d_min = a·r_A + b·r_B, dy /0.7).
필드는 **장면 인스턴스**(지금 `propField`는 모듈 전역, 민물은 `resetPropField`를 안 불러 이웃 바이옴 자리를 본다). 세로 실루엣 규칙: `|dx| < .8(r_far + r_near)` 이고
`near.y − .9·near.hy < far.y ≤ near.y + 4`면 거부; hy = 슬롯 높이 × k(rock 30 · shrub 66 · stump 28 · log 26 · oak 150 · pine 168 · reed 70 · tuft 18).
PAIR: tree-tree [.85,.85](무리 [.6,.6], ≤ 4) · tree-rock [.9,1.0] · tree-shrub [.5,1.0] · tree-stump/log [0,2.4] · rock-rock [1.1,1.1](outcrop [0,0]) · rock-shrub [1,1] ·
shrub-shrub [.9,.9] · island-* [1,1] · water-* 폴리곤 거부 · mushroom probe(90px) · tuft = behindStand만.
**C-2 그리기 순서**: 바탕(땅·물·능선·애추·원경 침엽수·납작한 것·**소품 그림자**) → 풀 → **`standing[]` = 소품 + 나무 + 생물 한 목록 y-sort**(L1718 `trees` 루프 대체). 초원 세 장면도 같은 순서.
**C-3 미경유 루프 삽입 지점**: 산 큰 바위 6(land L1421) · 계곡 수변 관목(L1044~1059) · 민물 `stand()`(summer L465~476, 물 폴리곤 먼저 등록) · 민물 물 앵커 바위(L564) · 물풀 섬(L496) · 초원 관목 무리(spring L293~302) · 풀 전부(`tuftAt` 첫 줄 · spring blades L331) · 기존 `claimSpot` 호출 치환.
**C-4 수용 기준**: overlapRatio 16 시나리오 0 · `tuftBehindProp` 0 · 무리 밖 수관 겹침 > 15% 쌍 0 · 바위 발이 다른 바위 몸통 안 0 · 민물 기슭 소품 물 안 0 · 필드 장면별 독립.
