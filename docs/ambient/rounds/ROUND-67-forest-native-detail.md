# R67 — Match foreground tree detail to the floor

2026-09-16 KST. Owner clarified that tree detail must match the ground at the root location, especially in the foreground. Ground quality, geometry and the R66 fog/root changes stay intact. Owner authorized push to main on 2026-09-16; deployment unverified.

Cause: R66 made mature trees larger, but all 25 per-tree image copies still shared a global sampling cap, with an additional distance multiplier. This downsampled foreground copies even though higher-detail original sprites were already decoded.

Near trees now reference the existing native sprite and alpha map directly. Multiple trees share that immutable source; no duplicated large foreground bitmap. Far trees retain their previous sampling policy. Full tier uses native for root distance >=.68; lite/still preserve it in the nearest >=.82 region. Roots remain authoritative, so a tall crown does not accidentally get distant sampling merely because it is high on screen. No new detail is fabricated beyond the existing native artwork.

Fog masks deduplicate by actual source canvas. Reserve their cost before allocating remaining instance detail copies. Cache clearing releases owned copies only, never shared original sprites; scene disposal releases originals once. Draw/contact/picking use the same selected source. Existing tree-detail+fog budgets remain 8/3/1.5 MiB. No asset/network/dependency changes, no ground downsampling, no server/role/auth/KST changes.

Checks: 8 forest unit tests; four-season direct browser probe cycles full/lite/still/full, confirms native originals survive rebuild, foreground uses original image, budget limits and zero retained bytes after disposal. Full summer detail+fog 3,071,164 bytes vs R66 8,174,399 bytes; this excludes separately counted original atlases and contact patches. Production captures four seasons clear/fog in `.scratch-pw/qa/r67`. B lifecycle/cache/picking review found no blockers. Typecheck, lint and final production build passed. A visual review confirmed clearer nearby crowns, branches and bark with intact ground joins; original ground still has richer fine texture, so this is source-detail recovery, not newly generated artwork. C confirmed seasonal fog/winter branches and flagged a thin existing mauve key fringe exposed by native detail: edge-only RGB despill was added without changing alpha or cutting new canopy holes. Final production fixtures passed: four-season clear/fog captures, four-season night/noon material grab/drag/release, 900/2560-wide layouts, full/lite 90-frame stable bake counts and memory caps. Direct summer/winter probe passed front/back occlusion, canopy-hole picking, parallax and disposal. No browser errors. Final harness and diff whitespace checks passed.
