# ROUND-10 — Agent B(Spatial Ecology Inspector) 보고 원문(요약 보존판)

빌드 `b134f9d` · 시드 42 · 1400×860 · hz 224 · 도구 `.scratch-pw/r09b-mstd.mjs`·`r09b-l34.mjs`·`r10b-{col,hill,tip}.mjs`(신설, 읽기 전용)

## 발견(P0 0 · P1 5)

1. **P1 A-3+S-1 — 풀 층(`grassC`/`blades`)이 구운 소품 위에 그려져 풀포기가 바위·관목·소나무 몸통을 뚫고 올라오고,
   풀 배치는 `claimSpot`을 거치지 않는다** — 언덕·숲·산(`land.ts`) + 초원 봄·여름(`spring.ts`) 네 바이옴 공통.
   `land.ts draw()`: `ground`(바위·관목·그루터기·통나무·벨트·노두·능선 나무 전부 구움) → `grassC` 타일을 그 **위에** →
   `trees`. `tuftClump → tuftAt → drawProp`은 `claimSpot` 미호출. `spring.ts`도 `blades`를 따로 구워 위에 그린다.
   관찰: s15 바위(838,462) 왼쪽 위로 억새 가닥 · s16 관목 3그루 실루엣 위 풀 획 10+ · s10 소나무(1170,690) 단 위 마른 풀 3포기.
   **계곡만 멀쩡**(풀을 바탕 `g`에 직접 그림) — 원인 확인.
   Fix: 풀 루프에 **조회 전용** 점유 검사(`propSpots()` 대해 `|dx| < r_prop && y < prop.y + 4`면 스킵 — 소품 뒤에 서는 포기만) +
   큰 stand 소품을 `ground`가 아니라 **풀 뒤에 그려지는 층**(`propsC` 또는 y-sort 목록)으로.
   Acceptance: `debug().tuftBehindProp` 16 시나리오 0 · 크롭에서 소품 몸통 위 풀색 획 0.
2. **P1 S-1 — `claimSpot`이 실루엣 높이를 모르고(바위·관목이 stand=false), 입구를 안 거치는 루프 다섯 곳.**
   일반 소품은 눌린 발자국 거리만 본다 → dy 17·dx 5인 두 바위가 통과해 위 바위 발이 아래 바위 몸통 안(s15 (843,447)/(838,462) ·
   s09 (1212,547)/(1188,570)). 미경유: 산 큰 바위 6개 · 계곡 수변 식생(둔치 바위 위 관목 2) · 민물 기슭 `stand()`·물 앵커 바위·물풀 섬 ·
   초원 관목 무리.
   Fix: `artSlot(id).view === "stand"`면 stand 자동 + 세로 여유를 **슬롯 높이**(`px[1]·k`)로 · 미경유 다섯 루프에 삽입 · 부록 B를 `world/spacing.ts`로.
3. **P1 S-4+A-3 — 해안 뭍 판이 물가 폴리곤으로 클립되어 물가 노두 윗부분이 물가선에서 잘리고(물이 돌 앞), 거품 줄·흰 원반(15k)이 몸통을 덮어
   "반투명 사다리꼴 판"** — 라운드 9 (d) 미달의 진짜 원인(depth가 아니었다). `draw()`: 물 → 파도 → 빛의 길 → **`shorePath(0)` clip 안에 `land`**
   (노두 포함) → 거품 두 줄. 세 노두 열 x785/903/1067에서 몸통 시작 y 474/488/490 = 그 x 물가선, 물가선 위 몸통 픽셀 **0**.
   `softBlob(15k, white, .2)`(반지름 45~70px)이 몸통 전체를 덮고 `drawArt`가 smoothing을 안 꺼 3~4.8× 확대가 흐릿한 판.
   Fix: `lateRocks`를 `land` 굽기에서 빼 **거품 줄 뒤** 별도로(y-sort) · 흰 원반 ≤ 6k 또는 삭제 · `imageSmoothingEnabled = false` · 갯벌 하구도 같은 클립.
   Acceptance: 몸통 최상단 y < 물가선 − 8 · 몸통 L이 뭍 바위와 ±4 · bbox 안 거품 픽셀 0.
4. **P1 S-5 — 갯골이 "검은 뿌리".** 하구 행 y306: 바다 84~88 · 물골 **46** · 뻘 64 → 흘러드는 바다보다 38L 어둡다(수리 연속성 0).
   본류 코어 72px 평탄, 가장자리 4px 하드, 둔치 대칭. 지류 말단 폭 4~7px가 30px = 바늘. 색은 `drawChan(1, "rgb(66 84 96 / .62)")` 상수 하나.
   Fix: 물색을 t로 바다 팔레트에 보간(하구 = 바다색, 육지 끝 = 젖은 뻘 −8L) · 말단 폭 하한 6~8 + 둥근 머리 · 가장자리 두 단(바깥 절벽 −12L 1~2px,
   안쪽 둔치 젖은 뻘 −4L 6~14px, 곡률 부호로 좌우) · 하구는 `shorePath` 클립 밖으로 바다 위 4~8px.
5. **P1 D-1+S-2 — 언덕 띠 셋이 평행한 색 띠.** `hillRidge` 파장 2856/2094/1532px(화면에 마루 1개 미만) · 진폭 18/22/26 · 인접 능선 최소 간격
   108/102px · **겹치는 열 0** · 능선 ΔL 점심 3.3/6.2/5.2 · 사면 sd 1.7~3.3. crest·saddle·골·"능선 너머"가 없다.
   Fix: 띠별 **마루 항**(가우시안 봉 1~2, 50~90px) + 기준 v 시드 흩음 + 한 곳은 앞 능선이 뒤 띠를 가리게 + 사면 −4~−6L 그라데이션.
   Acceptance: 능선 y 범위 ≥ 60 · 간격 < 30px 열 ≥ 60 · ΔL 점심 ≥ 5 노을 ≥ 8 · 사면 sd ≥ 4 · 마루 x 서로 ≥ w·.12.

**요약** — 가장 넓게 걸린 문제: **풀 층이 구운 소품 위**(#1) — 소품 배치·그리기가 "한 필드(claimSpot) · 한 순서(y-sort)"를
거치지 않는 것이 #1·#2의 공통 뿌리. 안 고쳐도 되는 것: 민물 잠긴 바위 밑단 기하 평탄(소프트 컷은 닫힘) · 산 ②↔③·③↔④(구조 한계).

## 부록 A — 라운드 9 재측정

`drawSubmerged` (a) 밑단 컷 **닫힘**(행간 최대 2.1~2.6L) · (b) 젖은 띠 −10L 2/3 · (c) 앞 반원 닫힘 · **(d) 노두 판 열림 — 원인 재귀속(#3)** ·
(e) 물풀 섬 잠김 단차 ≈1L **미달**(그라데이션 어두운 끝 `cy3+.9R`이 폴리곤 밖), 갈대 22획 전부 경계 안 ✔.
산 다섯 층(표준창): s11 ③↔④ **−1.7~−2.2 → +4.6~5.9**(역전 닫힘) · s09 ②cap↔③ 0.3~0.9 → **5.1~7.1** · s10 ③↔④ 6.2~10.7 ·
s10 sky↔① 3/6 < 4 · ②↔③ 12/18 < 8(구조 한계, 이슈 아님).
회귀: 다람쥐 8/8 · 경로 최소 v .324 · 갯벌 바다 띠 **5.9%**(규격 근접) · 숲 나무 간격 정상 · 물골 안 소품 0.

## 부록 B — 간격·군집·레이어 규칙 표(`world/spacing.ts` 초안)

| 엔티티 | 같은 종 | 다른 종 | 스폰 금지 | 레이어 | 지금 입구 |
|---|---|---|---|---|---|
| 나무 | ≥ .85(R1+R2), 무리 안 ≥ .6, 무리 ≤ 4, **stand 세로 검사** | 바위 ≥ .9R+W/2 · 관목 ≥ .5R+W/2 | 지평선 띠 · 물 · 산 ② · 빈터 중심 | y-sort `trees[]` | 숲·계곡·산 ✔ · **언덕 능선 나무 ✗** |
| 바위 | ≥ 1.1(W1+W2)/2 · **stand=true** · 노두 예외(큰 1 + 작은 ≤ 5) | 관목 ≥ (W1+W2)/2 | 물(잠긴 규칙 제외) · 지평선 띠 | **풀 뒤 소품 층** | 언덕·해안·scatter ✔(stand ✗) · 산 큰 6 ✗ · 민물 ✗ |
| 관목 | ≥ .9(W1+W2)/2 | 바위 몸통 사각 안 금지 | 물 · 지평선 띠 | 소품 층 | scatter ✔ · 계곡 수변·초원 무리·민물 기슭 ✗ |
| 풀포기·억새 | 겹침 허용(무리 1~7) | **조회 전용**: stand 소품 뒤면 스킵 | 지평선 띠 · 물 | **소품 층 뒤** | 전부 ✗ |
| 버섯 | 무리 2~3 ≤ 24px | 나무·그루터기 90px 안만 | 빈터 중심 · 물 | 소품 층 | 앵커 ✔ |
| 갈대·사초 | 겹침 허용 | 물가 40px 띠, 발은 물가선 위 | 물 폴리곤(섬 예외) | 기슭 층 | 민물·계곡 ✗ |
| 잠긴 돌·노두 | 바위 규칙 | 웅덩이 ≥ 14px | 뭍 위 | **물 → 돌 → 거품**; 뭍 판 클립 밖 | 민물·계곡 ✔ · **해안 ✗** |

적용 순서: ① stand 슬롯은 `claimSpot(…, true)` + 슬롯 높이 → ② 미경유 다섯 루프 삽입 → ③ 풀 층 조회 검사 + 큰 소품을 풀 뒤 층으로 →
④ 해안 `lateRocks`·갯골 하구를 물가 클립 밖으로. ①~③은 한 벌.
