# ROUND-09 — Agent C(Season·Weather·Time·Motion Director) 보고 원문(요약 보존판)

빌드 `efab217` · 시드 42 · 1400×860 · 도구 `…/scratchpad/r09c/*.mjs`

## 발견(P0 0 · P1 6 · P2 6)

1. **P1 W-1 — 라운드 8이 `sky.ts` 판에서 만든 띠별 부호·폭을 `light.ts`의 띠를 모르는 하늘 오버레이가 한 겹 위에서 되돌린다.**
   밤 흐림 **판 −17.5L → 화면 −0.6L**, 밤 안개 판 +1.7L → 화면 +8.7L. 천정 어두운 10%(구름 없는 판 부분)에서
   밤 맑음 42.55 vs 흐림 **42.45**(Δ −0.10). 원인 셋 — ① `lightOf()`가 `L.sky`를 **띠와 무관한 밝은 회색**으로 섞고
   `skyAlpha`가 밤 .46 · 저녁 .34 ② `skyPalette`의 띠 버킷에서 **노을이 '낮'**이다(맑은 노을 천정 54.09 = 새벽 54.92인데
   낮 오프셋을 받아 안개 판 Δ +10.32로 유일 초과) ③ 안개·흐림·눈의 **덩이 색이 지평선에서 파생**돼 밤 안개 덩이가
   그 띠 천정보다 **+52.6L** 밝다.
2. **P1 W-1 — 눈이 5/6 띠에서 하늘을 밝게 만든다**(천정 Δ맑음 [+8.44 −2.02 +2.91 +7.44 **+16.49 +14.67**], 규칙 −6).
   `skyPalette` snow가 상수 혼합 `mix(top, [200 206 214], 0.7)` 뒤에 −7을 빼므로 −7이 반올림 오차가 된다.
   같은 상수 혼합이 rain에도 남아 저녁·밤 비 하늘 Δ −0.77 / −1.48(목표 −14).
3. **P1 T-1 — 아침↔점심이 여전히 한 채널**(3/3 조합에서 bandDelta 1): 지면 ΔL 0.63 / 0.90 / 1.41 ·
   그림자 면적비 1.00~1.04 · 하늘 ΔE만 통과. 나머지 네 쌍은 전부 통과.
   Fix: `BAND.morning.mul`을 목표 −2에 맞춰 내리고 두 번째 채널을 그림자가 아니라 glint/hazeK로.
4. **P1 W-1 — 바람이 나무 없는 다섯 바이옴에서 하늘 한 열.** 지면 ΔL −0.18~+0.16(10/10) · 지면 Δ채도 **0.000**(10/10).
   구조 잔차 · 1초 배율: 숲 4.39%·4.6× | 계곡 2.80%·3.6× | 언덕 2.00%·1.9× ‖ **암석 0.06%·1.14× · 갯벌 0.02%·1.08× ·
   모래 0.02%·1.09× · 초원 0.02%·1.06×**. 라운드 8의 삼단 반응이 나무 있는 넷에만 닿았다.
   Fix: `coast.ts`·`sea.ts`의 파도 진폭·거품·너울을 `wind`의 함수로 · 초원 계열 풀 층을 `bakeGrass`와 같은 입구로.
5. **P1 M-1 — 새 풀 진행파가 x만의 함수라 원근을 무시한다.** `off = sin(u·0.008 − t·spd)…`에 **y가 없다** →
   인접 40px 띠 시계열 상관 **0.918**(임계 .9) · 1초 변화가 **원경 > 근경**(숲 봄 5.54% vs 1.71%, 가을 바람 22.33% vs 8.26%) ·
   억새 크롭 3프레임에서 포기가 **휘지 않고 밑동째 평행이동**, 40px 경계마다 최대 4.2px 어긋남.
6. **P1 D-3 — 안개 하늘에 또렷한 흰 뭉게구름**(여섯 띠 전부, 노을에선 화면에서 가장 밝은 물체).
   `skyPalette` fog가 `cover 0.3`에 지평선 `z`에서 파생한 밝은 적운 덩이를 만들고 `bakeClouds` FAR 티어가 천정에 얹는다.
   주석("아주 옅은 저층운 한 겹")과 렌더가 어긋나 있다.
7. P2 M-3 — **산만 지평선 아래가 정지**(맑은 낮 1초 2,103px · 0→4초 0.36%, 하한 3,000 / 0.5%).
   `bakeGrass(w·h/7000, 0.8, 0.5)`로 숲(/3400)·언덕(/3200)의 절반 이하.
8. P2 W-1 — 비가 땅을 **탈채도**(Δ채도 −0.05 ~ −0.07, 규칙 +10%). `lightOf` rain의 `desat += 0.08`.
9. P2 W-1 — 눈 쌓임 상태 변수 없음(3분 뒤 지면 ΔL +0.02 / −0.01 / −0.02). 가장 크게 보이는 곳은 **가을 11월 눈**.
10. P2 T-2 — 밝은 두 바이옴이 밤 하한 초과: 민물 **−17.74** · 언덕 **−17.04** · 갯벌 −16.03(하한 −16).
    라운드 8의 `mul 184 → 194`가 초원·숲·산은 들였으나 곱셈 구조는 그대로.
11. P2 W-1 — 비 입자가 희박·수직(500×320 크롭에 6~8줄, 대비 2~4L, 기울기 ≈ 0°).
12. P2 M-2 — 달·해가 언제나 구름 앞. 노을 해는 지름 18px(R 하한 9)에 글로우가 거의 없다.

## 요청 판정

**(a) 하늘 판 — 부분.** 흐림 ≤ −3 **5/6**(밤 −0.60) · 안개 세로 ≥ 6 **6/6 ✔ 닫힘**(6.45~18.03) · 안개 Δ ≤ +6 **2/6**.
남은 원인은 전부 **판 밖**(위 #1의 셋).
**(b) 육지의 정지 — 계곡 포함 닫힘, 산만 미달.** 숲 17,751 / 12,279 · 언덕 16,218 / 14,274 · **계곡 9,765 / 7,926 ✔** ·
산 겨울 밤 2,265 ✔(0→4초 0.34% ✘) · **산 여름 점심 2,103 ✘**. 위상 정렬은 실제로 보인다(#5).
**(c) 해·달·별 순서 — 닫힘 ✔.** 숲·언덕·계곡 노을 해 원반이 마루(153.08) **위**, 마루 아래 화소 0. 밤 별 최대 y 150.08 <
153.08로 구조적으로도 보장. 산의 `clipAboveRidge1`은 설계대로.
**(d) 바람 — 층은 닫힘, 열은 미달.** far ×0.45 / near ×1.0으로 **비율 0.45 ∈ [0.4, 0.6] ✔**, near 층 비어 있지 않음
(하늘 3초 변화 맑음 1.62% → 바람 **10.81%**). 그러나 weatherDelta ≥ 3은 나무 있는 네 바이옴뿐(#4).
**(e) 그림자 채널 — 방향·길이는 실제로 생겼다(이번 라운드 최대 성과).** 참나무 코어 하강 **새벽 −7.6L · 노을 −7.7L**
(기준 4) · 점심 −5.4L(기준 6, −0.6 미달). 좌우 비대칭 지수 새벽 −2.79 · 아침 −1.45 · 점심 0 · 노을 **+6.17** ·
저녁 +1.71 · 밤 +0.33 — `light.ts` dx와 부호·순서 일치. 여섯 띠 크롭을 쌓으면 한눈에 갈린다(라운드 8 이전엔 4띠가 픽셀 동일).
전역 dip 면적비는 1.4배 기준을 언덕만 통과 — **다음 라운드는 국소 지표(참나무 좌우 비대칭 지수)로 바꿀 것**.
**(f) 회귀 없음.** 다람쥐 3시드 전부 22.0~22.5s 등장 · 최소 v 0.48~0.60 · 순간이동 0 · 화면 밖 퇴장 ·
`drawCreatureShadow` 근단 이동 2px. **깊은 바다 봉인 유지 ✔**(9프레임 해시 동일 `9035e342`). 안개 가로 절단선 0.

**요약** — 가장 넓게 걸린 문제: **띠를 모르는 밝은 회색 혼합** — `lightOf()`의 하늘 오버레이(흐림·안개·눈·비) ·
`skyPalette`의 눈·비 분기 · 지평선에서 파생한 구름 덩이 색. 셋이 같은 어법이고 흐림 밤·안개 4띠·눈 5띠·비 2띠를
동시에 붙잡고 있다.
안 고쳐도 되는 것: 달·해가 구름 앞(M-2) — 관찰까지 수십 초, 하늘 판을 여는 김에 함께 처리하면 되지만 단독으로는 급하지 않다.
