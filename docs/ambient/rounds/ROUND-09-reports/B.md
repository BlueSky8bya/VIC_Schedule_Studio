# ROUND-09 — Agent B(Spatial Ecology Inspector) 보고 원문(요약 보존판)

빌드 `efab217` · 시드 42 · 1400×860 · hz 224 · 도구 `.scratch-pw/r09b-*.mjs`(신규, 읽기 전용)

## 발견(P0 1 · P1 6 · P2 3 · P3 1)

1. **P0 S-2 — 물 위 "빛의 길"이 해·달과 다른 x에 있다.** `scenes/water.ts drawWaterLight()`의
   `g.transform(1, 0, shear, 1, x − shear·top, 0)` e항에 `x`가 들어 있는데 다음 줄이 다시 절대좌표
   `drawImage(beam, x − w1, …)`로 그린다 → **이중 평행이동**, 띠 중심 = `2x`.
   실측: s13 노을 해 원반 **x 391** vs 수면 띠 **x 775~787**(예측 784, 오차 9px) = **Δ 383px** ·
   저녁 띠 x 1025(예측 1030) · **새벽·밤은 수면 프로파일이 평탄**(띠가 화면 밖 2016 / 1624) — 라운드 4가 넣은
   달빛 띠가 한 번도 보인 적이 없다. s07 갯벌도 375px로 동일(시나리오 독립).
   Fix: e를 `−shear·top`으로. 잔 글린트는 이미 절대 x라 같은 수정으로 함께 맞는다.
2. **P1 S-4 — `drawSubmerged` "접시 위 돌"이 그대로**(민물·계곡·암석해안 11곳).
   s12 민물 바위: 몸통 59 → 젖은 띠 44.1 → 잠긴부 54→64 → **y585→586에서 6L 하드 컷**, 밑단 **61/61열이 ±1px**
   = 65px 폭 완전 수평 컷. 수면선 흰 타원이 몸통 밑단보다 **위**를 지나 접시 테가 된다.
   s05 계곡: 밑단 20/25열이 ±1px, 그 아래 **직선 세로 흰 획 2개**(길이 17px)가 다리로 읽힌다.
   s13 암석: 물가 노두 3개가 **좌우 변 수직·밑변 수평인 반투명 판**(폭 75~95px), 몸통−조류대 ΔL 14.6 / 11.5 / **−1.5**.
   원인 넷 — `props.ts:1130~1134` 페이드가 `1 − alphaDeep`까지만(민물은 `alphaDeep` 미전달로 기본 .42, 가장 두꺼움) ·
   `props.ts:1145`가 접지점을 `yWater + depth`에 찍어 호출부 링(`yWater`)이 몸통을 가로지름 ·
   `props.ts:1141` 젖은 띠가 절대 남색 · `coast.ts:998~1001`의 **뒤 반원 호가 `drawSubmerged` 뒤**에 그려짐.
   Fix(한 함수): ① 밑단 페이드 알파 0으로 끝내고 `alphaDeep`은 중간 기울기만 ② 젖은 띠를 몸통색 −10L
   ③ **앞 반원 수면선을 함수 안에서** 소품 폭에 맞춰 ④ 호출부 링 제거 — `summer.ts:569` · `land.ts:894~898` ·
   `coast.ts:998~1001`(또는 `drawSubmerged` 앞으로) ⑤ `land.ts:905~912` 후류를 실제 곡선으로.
3. **P1 D-2 — 산 다섯 층 중 한 단이 계절마다 무너진다.** 나무 제외 창(부록 D)으로:
   s11 여름 **③↔④ −1.7 ~ −2.2(여섯 띠 전부 역전)** · ②↔③ 3.6~8.0(5/6 미달) · 하늘↔① 4.0~8.4 ·
   s09 겨울 **②cap↔③ 0.3~0.9** · ②body↔③ 2.2~4.8 · ①↔② 3.1~6.4 ·
   s10 가을 ②↔③ 5.9~9.8(3/6 미달) · **③↔④ 5.7~9.4**(라운드 8이 "8.1~11.9 닫힘"으로 본 값은 ④ 창의 **23~57%가
   침엽수**여서 5~7L 과대평가된 것).
   원인: 스텝을 색으로만 정하고 **엔진 안개 구간·곱셈 조명을 계산에 넣지 않았다**(여름 설계 ③ 61.2 → 렌더 55.8).
   Fix: 목표 L\*을 **화면 L 기준으로 역산**(층의 대표 y에서 haze 혼합비와 그 띠의 `light.mul`을 미리 적용).
4. **P1 D-2 — 층 안 사면 명암이 목표의 절반, 겨울은 부호 역전.** ① 능선→발 s10 2.0/3.6 · s11 2.9/5.2 ·
   **s09 −3.7/−4.6** · ② 1.5~4.1. 8조합 중 **1조합만 ≥ 5**. 클램프열 0.0% ✔ · 전폭 계단 0 ✔ ·
   **최장 평탄 run s09 능선② 378px · ① 220px**(기준 ≤ 168).
5. **P1 D-1 — 겨울 원경 침엽수 30그루가 땅보다 밝다**(ΔL 중앙 **+9.3**, 최대 +17.0). 가을 −3.9 · 여름 −4.2는
   방향은 맞으나 목표(−5~−9)에 못 미친다. 원인 `land.ts:1371~1374`가 기준색을 **원래 땅 그라데이션**으로 잡는데
   나무가 서는 면은 그 위의 **③ 애추 띠**다(겨울 ③ 62.9 vs 원지면 89.4 → 나무 82.4 = 띠보다 +19.5).
   Fix: 기준색을 ③ 띠 안이면 `tone`, 밖이면 `groundHere`로.
6. **P1 S-2 — 갯벌 바다 띠가 화면 22.0% / 18.3%**(규격 3~5%). `SEA_GV_BY.tidal = 0.25` 그대로. 라운드 8에서 변화 없음.
7. **P1 S-4 — 민물 물풀 섬 둘이 전혀 잠기지 않는다.** 내부 명암 5L(그냥 땅 그라데이션) · 잠김 표현 **0** ·
   밑단 81열이 3px 안 = 수평 컷 · 줄기 획 8개 중 5개가 섬 **밖** 물 위에서 시작.
8. **P1 S-4 — 암석해안 물가 노두 3개가 반투명 사다리꼴 판**, 하나는 조류대보다 밝아 접촉 역전(−1.5).
   `depth: 9·k`가 k 3~5에서 27~45px = 몸통 높이의 1/3.
9. P2 S-2 — 버섯 회귀는 닫혔으나 한 무리가 근경 소나무 **첨두 2px 위**에 얹혀 보인다. 무리 폭 33px(기준 ≤ 24).
10. P2 S-2 — 갯벌 배수망이 양끝 200~280px을 비운다(구역별 물골 픽셀 0.2~0.9%, 최대/최소 130배).
11. P2 S-5 — 계곡 굽이 안/바깥 둔치 비대칭이 곡률과 무관하게 좌우로 튄다(한 굽이 안에서 3회 반전).
12. P3 A-1 — 가을 다람쥐 8시드 전수 통과(경로 최소 v **0.324**). ⚠ `spawn-probe.mjs`의 `HZ_V = 0.12`가 아직 낡았다.

## 부록 D — 라운드 9 표준 측정창(산 ③·④, 나무 제외)

라운드 8의 창은 침엽수 벨트를 함께 잰다(벨트는 `groundY(.44~.62)`·`groundY(.60~.94)`). 오염도(mode −5L 미만 비율)
**③ 3~13% · ④ 23~57%** — ④가 5~7L 어둡게 나와 ③↔④가 과대평가됐다.

| 층 | 창 | 추정량 |
|---|---|---|
| SKY / ① / ② | 라운드 8과 동일(열별 r1·r2 기준) | 열 중앙값 |
| **③** | 열별 `[yTop(x)+8, min(yBot(x)−6, groundY(.42))]` — 코드의 실제 폴리곤 | **열 p75 → 중앙값** |
| **④** | `groundY(.66)~groundY(.86)`, 전폭 x step 2 | **열 p75 → 중앙값** |

교차검증은 전폭 pooled **mode**(0.5L 빈 + 3빈 평활), p75와 ±1.5L 안이면 채택. 오염도를 항상 병기한다.

## 부록 E — 라운드 8 수정별 재측정(요약)

닫힘: s11 ①↔② 8.2~10.6 · 전폭 계단 0 · 클램프열 0% · 가을 다람쥐 · 버섯 회귀.
부분: s10 sky↔①(저녁 3.5) · s10 ②↔③(저녁 6.1) · 층 안 사면(1/8) · 최장 평탄 run(s09) · 침엽수 방향(겨울 열림) ·
버섯 무리 폭 · 갯벌 물골 분포 · 계곡 둔치.
**되돌아감**: s10 ③↔④(옛 창 9.8~11.9 → 나무 제외 5.7~9.4).
열림: s11 ③↔④ 역전 · s09 ②cap↔③ 0.3~0.9 · s11 sky↔① 1.4(저녁) · `drawSubmerged` · 갯벌 바다 띠 · 물풀 섬.

**요약** — 가장 넓게 걸린 문제: **`drawWaterLight`의 이중 평행이동**(물 있는 다섯 바이옴 × 네 시간대, 원인 한 줄).
안 고쳐도 되는 것: 계곡 둔치 비대칭(폭 비대칭은 생겼고 잔여는 곡률 연동뿐).
