# R35 — Meadow joins, seasonal interaction and loading

Owner request (2026-09-11): hide the far-ridge/ground horizontal join; unify seasonal meadow interactions around individually draggable wind material; keep moving material/animals out of the distant upper third of the ground; eliminate legacy backdrop/prop flashes during loading.

Plan: align the opaque ridge foot over the sky/ground cutoff, then fade below it. Share a horizon-derived activity boundary and smooth fade. Preserve autumn's leaf/acorn/squirrel interaction; add graspable spring petals, summer seed fluff and winter flakes, replacing empty-ground click bursts/footprints. Keep animal routes in the lower two thirds. Publish backdrop+optional detail atomically; distinguish pending assets from failed assets so stand-ins appear only on failure. Verify four seasons, drag/fade and delayed asset loading, plus engine gates. No changes to server DTOs, roles, KST season selection, mobile or deep exposure. No new push authorized for R35.

Season choices are artistic representation, not claims of exclusive seasonal occurrence. Spring blossom supported by [RHS cherry guide](https://www.rhs.org.uk/fruit/cherries/grow-your-own). Summer dandelion seeds supported by [RHS dandelion profile](https://www.rhs.org.uk/plants/76282/taraxacum-officinale-agg/details): April–June flowering followed by wind-dispersed seed heads. Autumn retains accepted leaves; winter uses snow crystals. No new biological simulation or artwork generation requested/needed.

Final implementation: ridge whole-image height scale is `max(.8, k + .35)`, foot at local94, bottom fade78→94 (actual horizon local72). A trial single-row extension created visible streaks and was removed before delivery. Ridge height increases slightly to cover the underlying cutoff in all seasons. Motes keep their variant for life and use per-item smooth dragging; autumn retains its own richer leaf/acorn behavior. Rabbit travel is incremental so direction changes at the boundary cannot reapply accumulated hop distance. Squirrel escape waypoints also respect the activity boundary. ArtSet completion emits the existing still-frame refresh event.

Verification on final fixture build `E86IN42bYHDVuLbM-zs_-`: typecheck, lint, build and 82 test files / 855 tests passed. Four seasons × noon/night plus 3440×1440 and 900×1200 winter captures completed without browser errors (`.scratch-pw/qa/r35/final/`). Independent A visually confirmed no sharp ridge join or vertical stretch streaks in autumn noon, summer night and full-size winter ultrawide. B/C code findings (late still refresh, rabbit trajectory, particle variant identity, butterfly bounds) integrated.

Actual mouse grab/move/far-fade passed for spring/summer/winter; autumn grab and far removal passed separately. 120-second sampled creature runs observed squirrel minimum y751 and rabbit y550 at h860 (activity top487.33); this is sampled fixture evidence, not an exhaustive biological/path simulation. Delaying detail kept backdrop ready=false/version0 and zero display cache bakes until atomic ready/version1; missing optional detail recovered to original three layers with expected injected network error only. Delaying eight rock assets 3.2 seconds in still hill mode repainted after arrival, with no page errors. No real database writes or production deployment performed. No numeric FPS improvement claimed.

Final engine checks: still/cache/mobile 33 passed, 0 failed; fixture selftest all passed, including deep seal and deterministic time. Harness passed (81 documents; startup brief 7951 bytes). Local preview server remains on port3100.
