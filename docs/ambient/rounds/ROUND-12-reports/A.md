# ROUND-12 — Agent A(Art Mood Director) 보고 원문(요약 보존판)

빌드 `daab140` · 시드 42 · 1400×860 · 도구·크롭 `…/scratchpad/a12/`

코드 진실에서 확인: 숲 나무는 `claimSpot(…, stand=true)`를 지나지만 세로 여유가 `0.55·(r1+r2)`(수관 반지름)라 **실루엣 높이(소나무 3~4R)를 모른다** ·
계곡 벽 위 나무(`land.ts` L1621)는 미경유 · 산 침엽수 띠 r = 26·k(폭 기준) · 초원 풀포기는 흔들림 스트립(`spring.ts` `b.g`)에 굽혀 바탕의 바위·관목 위에 매 프레임 합성.
r11 → r12 정적 diff 12/16 픽셀 동일 — 아래 배치 문제는 전부 기존 조건.

## 발견(P0 0 · P1 3 · P2 2)

1. **P1 S-1 — 서 있는 것끼리 같은 열에 세로로 쌓여 실루엣이 융합한다**(토템). s10 (140–290, 450–690) 소나무 4그루 한 열 · s05 (1050–1170, 400–540)
   소나무 앞·참나무 뒤 같은 x("참나무가 소나무 위에 앉은" 형태) · s02 참·소·참 스택 + 참나무 4그루 세로 열 · s09 소나무 8그루 수관 융합 + **소나무 꼭대기 위에 바위 둘** ·
   s11 부분. 원인: `claimSpot(stand)`의 세로 검사가 `|dy| < .55·(r+o.r)`(수관 반지름)라 높이 100~150px 소나무를 30px 간격으로 적재 허용 · 산 벨트 r = 26·k · 계곡 벽 위 나무 미경유.
   Fix: 자리마다 **hy**(발~꼭대기) 등록, 같은 열(|dx| < .6·max R)이면 "앞 것의 꼭대기 y < 뒤 것의 수관 밑변 y" 금지, 아니면 x를 ≥ .5·(R1+R2) 밀어 재추첨. 바위는 몸통 상자 안 금지.
   Acceptance: |dx| < .4·(R1+R2)인 쌍 중 앞 꼭대기가 뒤 수관 밑변보다 위인 쌍 **0** · 무리 밖 나무 발·줄기 ≥ 6px 보임 · 무리 안 크기 편차 ≥ 1.25× · 바위 중심이 나무 실루엣 상자 안 0 · 숲 원경 밀도 유지.
2. **P1 S-3 — 소품이 한 y 띠에 "선반"처럼 줄지어 놓인다.** s02 y 320~390 띠에 바위·통나무·그루터기 6개 같은 발높이 등간격 · s03·s04는 서 있는 소품 전부 아래 12% 띠(위 88%엔 소나무 1) ·
   s12 먼 기슭 한 줄 · s06 같은 크기 관목 셋 대각선. 원인: `scatterProps`가 band "edge"/"any"에서 y를 좁은 띠에서 뽑고 edge = 아래 띠가 사실상 기본.
   Fix: 무리 중심 포아송 → 무리 안 흩기 · y 지터 ≥ .15·(h−hz) · edge 띠를 좌·우·위에도 배분. Acceptance: 발 y sd ≥ .12·(h−hz) · 40px×50%w 창에 ≥ 4개인 띠 0 · 아래 12% 띠 ≤ 40%.
3. **P1 S-1 — 풀 층이 바위·관목의 몸통을 뚫고 올라오고, 여름 초원 바위는 풀에 덮여 라벤더색 반투명 유령.** s16 바위 (430–460, 760–790) · 관목 셋 · s12 근경 기슭 바위 ·
   s05 둔치 바위 위 풀포기 + 관목 받침이 바위를 덮음. 원인: `spring.ts bakeGround`가 관목·바위를 바탕에, 풀포기·키큰 풀은 스트립 `b.g`에 굽혀 매 프레임 위에 합성.
   Fix: 풀을 놓기 전에 서 있는 소품의 몸통 상자를 물어 안이면 건너뛴다(`behindStand`) 또는 바위·관목을 스트립에 y-sort로. 바위 알파 1.
   Acceptance: 바위·관목 발선 위로 지나가는 풀 획 0 · s16 바위 L·C가 숲 바위와 ±4 · 풀 밀도 유지 · 관목 앞(발 y+4 아래)의 풀은 남긴다.
4. P2 S-3 — 바위가 "쌍둥이"(같은 스프라이트·같은 크기 둘 맞닿음, 회+갈 한 벌 반복). s13 · s15 ×3 · s10 · s03/s04. Fix: 이웃 안 크기비 ≥ 1.4 강제 · 같은 변형 인접 금지 · 결 한 방향. (라운드 15 노두와 묶어도 됨)
5. P2 D-1 — 초원 가장자리 소나무 머리가 지평선 띠에 잠겨 사라진다(s03·s04 (30–100, 225–340): 꼭대기 층 = 하늘값, 중간 반투명). `autumn.ts` 가장자리 나무가 바탕에 굽혀 하늘 hz 페이드·지평선 띠 **뒤**.
   Fix: 지평선 굽기 뒤 y-sort 라이브 패스로. 초원은 달력 뒤 기본 화면.

**요약** — 가장 넓게 걸린 문제: **#1 세로 간격이 실루엣 높이를 모른다**(나무 있는 5장면, claimSpot stand 한 곳). 안 고쳐도 되는 것: #4 쌍둥이 돌(15와 묶기).

## (1) 정서로 본 배치 질서

**군집(안 깬다)**: ① 발 x 어긋남 ≥ .5·(R1+R2) ② 크기 편차 ≥ 1.25×, 큰 것이 앞 ③ 그림자 각각 발밑 ④ 무리 밖에 무리 지름 이상의 빈터 — s01 왼쪽 무리 · s11 오른쪽 · s09 왼쪽.
**겹침(깬다)**: ① 같은 열에 앞 꼭대기가 뒤 수관 안(토템) ② 같은 크기·같은 스프라이트 맞닿음(복붙) ③ 뒤 것의 발·그림자 소실 ④ 소품이 소품 위(바위가 소나무 끝에, 관목·풀이 바위에).
형태별: 노두(큰 1 + 작은 ≤ 5, 비 ≥ 1.4)는 맞닿아도 군집 · 갈대·억새 맞닿음은 표지(✔) · 유목은 수면선 있으면 물 위(✔), 띠에 줄서면 선반(✘) · 해안 물가 사다리꼴 둘은 물 띠 아래 "젖은 판지"(✘ → 13).

## (2) 라운드 11 판정 — 안개는 "필터"에서 "공간"으로: **절반**

① 깊이 단조 + 원경 소실 ✔(far > mid > near 12/15, 발치 ≤ 1.5L 12/15, s10 y ≥ 770 ΔL 0.0 — 발 아래에서 필터가 걷혔다) ② 저지대 체류 **절반**(발치는 비웠지만 고이는 곳이 안 보인다 — s06 상류 육안 동일, s10 ①②③ +2.5~3.3 균일)
③ 수면 반응 ✘(s12 물 ΔL 0/−0.4인데 물 위 섬 +3.5 → 역전; s13 바다 0.0, 먼 섬 소실 ✘) ④ 근경 채도 보존 ✔ 대체로(10/15 ≤ −1.5) ⑤ 하늘 정서: 원반 0 ✔ / **halo 안 보임**(해 자리 L +0.8 · 달 +1.5 — α .16 → ≥ .35 제안) ⑥ 뭉치 ✔(존재감 0에 가까움).
라운드 10 회귀 둘: hz 절단선 **눈에 없음** ✔ · 산 sky↔① ✔.

## (3) 회귀 — 없음(12/16 픽셀 동일, 바뀐 건 안개·새벽 4장면뿐)

## (4) 다음 라운드 — 정서적으로 가장 급한 것: **13 해안 접촉**(s13 물가 사다리꼴 바위 둘 — 젖은 띠·포말이 몸통 위로 지나가 "물에 얹힌 판지" = "틀림"), 차점 14, 그다음 15(물풀 섬 소프트 타원).
