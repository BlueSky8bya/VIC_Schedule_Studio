# ROUND-12 — Agent B(Spatial Ecology Inspector) 보고 원문(요약 보존판)

빌드 `daab140` · 시드 42 · 1400×860 · 도구 `.scratch-pw/r12b-{props,tuft,fogratio,alpha,dmap,veil,shore,sample}.mjs`(신설) + r08b/r09b/r11b 재사용
전제: `daab140`은 `propField`를 노출하지 않아 before의 overlapRatio(필드 쌍)는 **정확값 측정 불가** — 크롭 단위 픽셀 대리 지표로 기준선을 잡았다. after는 `spots()`로.

## 발견(P0 0 · P1 3 · P2 2)

1. **P1 S-1 — 소품 필드가 세로 실루엣을 모르고(0.62 할인 원판만), 다섯 배치 루프가 필드를 안 거친다.**
   s15 (843,447)/(838,462): 뒤 바위 밑단이 앞 바위에 가려진 열 9/34(26%), 접촉 12px — 이 쌍은 `claimSpot(18k)`를 **통과한다**(원판 규칙엔 합법,
   실루엣 규칙 near.y−.9·hy < far.y ≤ near.y+4엔 거부) = 규칙 부재. s09 (1212,547)/(1188,570): `큰 바위 6` 루프(claimSpot 없음) — 위 바위 발이 아래 몸통 안 100% 은폐,
   그 뒤 벨트 소나무가 필드에 없는 두 바위를 덮는다. s12 바위 (402,467) 발이 물풀 섬 (430,512) 윗변에 — `summer.ts`는 claimSpot/resetPropField를 **한 번도 import하지 않는다**.
   필드는 모듈 전역; reset은 land/spring/autumn/winter/coast만 → "장면별 독립" 아니오.
   Fix: 부록 C C-1·C-3 그대로 + 언덕 능선 나무(L407)도 claim. Acceptance: after `spots()` overlapRatio 16 시나리오 0 · 위 세 좌표 접촉 0 · s12 바위–섬 접촉 0 · 산 큰 바위 6이 `spots()`에.
2. **P1 A-3 — 풀 층이 소품이 구워진 바탕 위에 합성되고 필드를 모른다.** "뒤에서 관통"(몸통 안에서 끝나는 획) 실측: s15 4덩이/99px · s16 관목 무리 **20/959px** ·
   s10 소나무 11/295 · s10 (560,480)-(720,740) **25/858** · s09 바위 4/96. "앞"(밑변까지 닿는 획 = 정상 가림) s16 1716px · s15 54px.
   **판정 (2): 풀 층을 소품 뒤로 옮기지 않는다 — `behindStand(x, y)` 스킵만으로 관통은 사라진다.** 관통 획은 전부 뒤 뿌리(s10 25/25 · s09 4/4 · s15 5/6)이고 앞 뿌리는
   소품 발을 가리는 정상 가림이라 풀 층이 위에 있어야만 생긴다(뒤로 보내면 "받침대 위 관목"). `standing[]` y-sort는 **이번 라운드 필수 아님**(16장에서 매 프레임 나무가
   앞의 구운 소품을 덮는 사례 검증 못 함; 완전 재배치의 위험 = 매 프레임 30~60 drawProp + 안개/depthFade 이중 적용 + 캐시 무효). 값싼 닫기 하나: **`scatterProps`가 후보를
   y로 정렬해 그리면** 관목이 앞 바위를 덮는 잠재 A-3가 굽기 시점에 닫힌다.
   Acceptance: 위 7개 창 "뒤에서 관통" 0 · "앞" px 유지(s16 ≥ 1500 · s15 ≥ 40 — 풀이 뒤로 갔다면 0이 되어 실패) · scatterProps y 정렬 후 관목–바위 뒤 밑단 가림 0.
3. **P1 D-1 — 라운드 11 안개 밀도장은 "깊이"는 알지만 "지형"은 아직 모른다.** 민물 물:기슭 D **.06:1**(목표 2:1 — 안개색 = 하늘 지평선 스톱 L 72.6이 새벽 수면 반사색 L 70.9와
   같아 α를 올려도 물은 안 변한다 → 물엔 **베일**(앵커 대비·글린트·반영 ×(1−F))) · 언덕 능선 위:아래 D 1.05/.79/1.04(목표 3/2/1.3 — H_FOG 191px에 기복 ±30px은 α ≤ 8%;
   단일 지면선 "위로 뜨면 옅다"로는 능선 **뒤** 골에 고이는 안개를 못 만든다) · 산 (①+②발):(②능선+③):④ = 1.09:1:.18(목표 4:1:.3 — floor = scree.top이라 ①이 150px "뜬" 것으로 계산) ·
   **계곡 상류 2.67:1 ✔ · 갯벌 3.2:1 ✔**. Fix: P(x,y) = Σ pool·exp(−max(0, floorᵢ(x)−y)/τ)(언덕 능선마다 위 0~.05h pool · 산 ① 창 + ② 발), H_FOG를 기복 스케일로, 민물 물 베일.
4. P2 D-2 — 산 겨울 점심(눈) sky↔① 2.2(라운드 10·11·12 동일 — 구조: ① = ④+6이 눈하늘 83.4와 눈밭 77.8 사이 5.6L 폭에 갇힘). 라운드 15 산 묶음.
5. P2 S-4 — 민물 잠긴 바위 셋 중 (632,560) 밑단 37/49px 직선(다른 둘 3/45 · 21/41 닫힘). 평평한 변형 + 큰 k. Fix: 밑변 2px 물색 100% 또는 잠김 12k.

**요약** — 가장 넓게 걸린 문제: **소품 필드가 세로 실루엣과 풀 층을 모른다**(#1·#2 한 뿌리, 육지 12 시나리오). 안 고쳐도 되는 것: #4 · 갯벌 밀물 띠 · 산 밤 ①↔②(구조).

## 부록 A — before 기준선

| 지표 | before | 창·방법 |
|---|---|---|
| overlapRatio | 측정 불가(필드 미노출) → after `spots()` | — |
| 바위 발이 다른 바위 몸통 안 | **2**(s15 (843,447)/(838,462) 접촉 12px · s09 (1212,547)/(1188,570) 발 완전 은폐) | r12b-props |
| tuftBehindProp(뒤 관통) | s15 4/99 · 1/77 · s16 20/959 · 2/11 · s10 11/295 · 25/858 · s09 4/96 | r12b-tuft |
| 무리 밖 수관 겹침 > 15% | 0 | r08b-trees |
| 민물 기슭 소품 물 안 | 0(9 소품 −12~−43px 뭍) | r12b-shore |
| 민물 물 위 앵커 접촉 | 1(바위 (402,467) – 섬 (430,512)) | 크롭 |
| 필드 장면별 독립 | 아니오 | 코드 |

## 부록 B — 라운드 11 재측정

hz 절단선 s04 1.38 · s16 −1.19 · s02 −1.22 · s10 −1.37, 그늘−해 ≤ 1 → **닫힘 유지** · 산 sky↔① 밤 7.9/12.5/6.9 · 새벽 5.8/10.0/13.1 → 닫힘(s09 점심 2.2 → #4) ·
산 ①↔② 밤 7.7/1.0/0.2(구조) · 안개 × 지형 → #3 · 다람쥐 8/8(경로 최소 v .324) · `drawSubmerged` (a) 3/45 · **37/49** · 21/41 (b)(c) ✔ (d) 13 (e) 15 ·
갯벌 바다 띠 점심 9.4% · 깊은 바다 층 질서 유지 · 버섯 3무리 유지.
