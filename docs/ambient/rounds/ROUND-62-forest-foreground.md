# R62 — Finer forest foreground artwork

2026-09-15 KST. Owner requested substantially better foreground ground detail after R61 tree depth matching. Local implementation; no commit/push or owner approval.

The four forest seasons now blend new detailed soil, small litter and low plant artwork into the near half of the existing R60 ground. Source rows above 55% remain unchanged; a smooth alpha ramp from 55–82% merges the new art, reaching full detail at 82%. Same terrain source, contour field, parallax and root-contact sampling prevent an independent moving overlay or a hard quality boundary. Trees retain R61 detail matching. No authority, public DTO, KST, motion gating or other biome changes.

This is new finer artwork, not a claim of doubled native pixel dimensions. Final images are 2172×724 except spring 2170×725; actual measurements/hashes are in run06 delivery.json. First attempt (run04) did not improve detail enough. Run05 introduced oversized whole leaves and was rejected after actual composition. Run06 replaces them with smaller fragmented litter. Rejected raw images remain archived; unused run05 public copies were removed only after byte matching their archive.

One initial cached composite; temporary canvas released, decoded image references cleared on completion/disposal. Persistent source allocation remains 6,290,112 bytes for summer and no new per-frame image processing. Cost: one additional 3.56–3.87 MB PNG download/decode per selected forest season, with temporary compositing memory and initial waiting time. No device FPS or production/network speed claim. Failed detail decode falls back to the R60 ground.

Validation on final v2 detail pack: typecheck, lint, production build and four forest unit tests passed. Four seasons noon/night captured; passive movement and grab/drag/release passed without browser errors. 900/2560 desktop captures; actual summer/winter root front/back, canopy-hole occlusion, parallax and disposal probes passed. Across90 full/lite frames, terrain bakes and tree-detail bakes stayed1; terrain cached bytes12,099,840/2,588,352, tree-detail bytes5,325,580/3,053,400. Aborted detail request still produced ready base terrain (local probe59ms, not a production loading benchmark). Evidence `.scratch-pw/qa/r62/`.

B reviewed shared-source/root alignment and fallback/disposal; C reviewed cache/time/motion boundaries and flagged the initial download cost recorded above. A inspected final summer/winter captures: materially finer foreground, no new straight seam, rectangular patch or floating root; no blocker. Remaining aesthetic limits: finer ground is more realistic than the pixel-painted crowns, and some broad understory leaves remain large. Main inspected summer before/after, winter noon and wide layout. Final aesthetic acceptance remains with owner.
