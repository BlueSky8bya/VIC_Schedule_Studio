# Current UI rules

Updated: 2026-09-09. Read only sections affected by the task. Shared security, KST and authorization rules live in [AGENTS](../../AGENTS.md). This file preserves standing UI constraints formerly mixed with history in CLAUDE; historical explanations remain in git and dated reports.

## Materials and meaning

- UI-01: (금생수·오행) Work controls use the existing metal tokens; containers use water/glass tokens. Keep a metal hairline edge, quiet top sheen, slate ink, small work-control radii and larger container radii. Water/glass faces may sit inside metal frames, including 저장. Reason: related surfaces must read as one product. Evidence: [ADR-0016](../agent/decisions/ADR-0016-metal-water-design-language.md), `app/metal-water.css`; current translucent fills use `--glass-*`, not retired `--gs-water-*` names.
- UI-02: (오행·의미색) Owner screens must not introduce strong red/orange/yellow decoration. Preserve the eight semantic colors: 업 도움/support rose, 기간 안내/period cyan, 일정 편집/editing violet, 신규/new green, 오늘/today gold, 미정/undecided orange, 떡밥/teaser violet ring, 다시보기/VOD forest blue. Existing tag colors and hearts retain their meaning. Never deliberately create stress with clashing 극 colors or awkward placement; warnings belong to real errors/unsaved changes. HCI/accessibility outrank palette preference. Personal-fit source notes remain local; never put them in UI/API or commits.
- UI-03: (방향·저장) North/top chrome uses water, west/left tools use silver, calendar content stays warm; do not introduce hot decorative accents at bottom/right. Placement still follows task flow and thumb/pointer reach. Save is the one water-colored primary action within the editor; cells/cards are selection targets.
- UI-04: (차분한 편집실) Studio calm styling is always on. New studio surfaces use `--studio-*` tokens under `html[data-studio-calm]`; do not restore a calm toggle or remove meaningful motion. Evidence: `app/layout.tsx`, studio calm CSS.
- UI-05: (눈 편한 테마) Eye comfort defaults on and changes UI token palettes, including `--glass-cell/head/panel`; never reintroduce a root CSS filter or tint the ambient canvas. Every new translucent surface takes its RGB base from these tokens, with alpha at the use site. Reason: a root filter affects everything and caused frame jank; literal glass fills made the toggle ineffective. The ≥50% pixel-change and background-invariance acceptance contract is below. Evidence: `app/globals.css`, [ambient engine rules](../ambient/ENGINE_RULES.md).
- UI-06: (모바일 재질) Mobile uses its own cream, 12px/pill radii and meaning-tinted controls. Web metal styles are gated by actual topology classes `.studio-narrow`/`.poster-agenda`, not width alone. Reason: landscape touch devices also enter agenda layout. Evidence: `lib/ui/breakpoints.ts`.

## Layout, typography and chrome

- UI-07: (웹·아젠다) Web and agenda are distinct layouts. Use `MOBILE_QUERY`, `STUDIO_AGENDA_QUERY` and `POSTER_AGENDA_QUERY`; do not confuse the mobile-device threshold with the studio/poster agenda thresholds. Never compress a desktop surface until text is unreadable. Mobile is a compact single column with thumb targets and sheets, with zero horizontal overflow; web uses horizontal space and larger type.
- UI-08: (디자인 통일) Use shared spacing, type, radius, shadow and motion tokens. Keep sibling heights and symmetric padding consistent; enlarge content or redistribute unused space without stretching a cramped box. Native-looking controls and legible text outrank decorative density.
- UI-09: (편집실 배치) Studio web chrome is one north row: build badge, month navigation, save state, account/role, viewer preview, logout. The decorative product title belongs on the viewer. Tools live in the west rail; do not restore a second full-width action row. Evidence: `components/studio/studio-shell.tsx`, [chrome compaction](chrome-compaction-manual.md).
- UI-10: (계정 카드·저장 상태) Account controls use one rail-width grid; fixed-content cells remain stable and preview uses remaining space. Save-state labels reserve their longest width. A successful save may put its KST time in the role badge while role remains available by its existing cues; do not make the card jump. Viewer preview navigation aligns to the same measured rail. Rail/grid measurements are below.
- UI-11: (아바타 자리·세로 압축) Avatar left/right control stays bottom-center, outside the moving rail. The rail prioritizes usable filter/tools height; the avatar's empty area yields first. Keep the ≥132px filter floor and 620px/470px height contracts below. The bottom floating row moves aside for the editor or falls behind it when space is insufficient. Evidence: chrome compaction manual and `placeEditorPopover`.
- UI-12: (크롬 실측 압축) Chrome compacts from measured overflow: cold labels and decorative chrome fold before calendar/save/month navigation. Never measure detached/zero-size nodes (`isConnected && clientWidth > 0`); rebind observers on remount. Keep accessible names when labels fold. Width/height/remount checks are below. Evidence: `chromeTier`/`data-pchrome` and the manual.
- UI-13: (투명 상단바) Background-enabled topbars remain transparent and non-sticky; empty chrome containers use pointer-events none with actual controls auto. Never give `html` a background that blocks body-background propagation and covers negative-z ambient layers. Reason: invisible chrome must not eat background interactions or cover the calendar. Evidence: `app/globals.css` html-background restriction.
- UI-14: (아바타 자리·상태 라디오) Viewer chrome uses 36px control cells, 44px outer/central cards, 13.5px `--text-body` and shared ink. State radios always show all states: avatar `끔/왼쪽/오른쪽` in owner/developer preview, ambient `켜기/흐리게/끄기`. Any target state is one click away and width is stable; never glue a cycling next-action button to a state radio. Size and transition checks are below.
- UI-15: (짧은 이름·툴팁) One short label per control on every platform. No long/short label pairs. A tooltip adds only missing information: hidden label → name only while folded, using `data-tip`; visible but ambiguous action → short result, about 20 characters or fewer and no parenthetical aside; self-explanatory visible label → none. Do not use a native title to repeat a visible label. Keep aria-label as the name. Evidence: `data-tip` chrome rules.
- UI-16: (칸 안 표식) One useful icon per control and one icon vocabulary per group. Chrome uses lucide strokes; emoji remain on playful surfaces without neighboring stroke icons. If folding would leave two icons, keep the leading identity icon and express popup semantics with aria-haspopup. In cells, icons are inlaid in host ink at one stroke weight, without drop shadow/white ring; saturated color is for data and selected states. Desktop calendar icon sizes are below.
- UI-17: (스크롤바 숨김) Scrollbars are visually hidden globally, while wheel/touch/keyboard scrolling remains. Do not override with thin/auto on a new scroller. Evidence: global scrollbar rule in `app/globals.css`.
- UI-18: (상태 선택 강조) Use shared `--seg-on-*`/`--seg-off-op` for state segments. Selected state is tint plus inset hairline, not another bordered box inside a segmented card.

## Editor, settings and overlays

- UI-19: (편집 팝오버) Editor popover prefers left of the selected date; flip only when space is insufficient. Reason: right-side popovers obscure the next day in chronological editing. Keep anchoring, viewport clamp, manual drag and leader line consistent through zoom. Evidence: `placeEditorPopover`.
- UI-20: (확대·드래그 좌표) Convert measured screen coordinates into local coordinates before CSS absolute/SVG positioning under studio zoom. Direct DOM gesture writes require direct DOM synchronization on release even when React state compares equal. Reason: state equality does not erase a manually changed style.
- UI-21: (설정) Settings have one shared `StudioSettingsList`: web settings modal, mobile role-badge slot. All new preferences enter this list. Web settings use modal history/focus/scroll-lock/Esc behavior and custom `RhhSelect`, not native selects.
- UI-22: (배경 감상 설정) Showcase's developer gear is the additional settings surface because chrome is hidden during viewing. It shares force/gfx state with the main settings modal. Any force surviving showcase must have a reset handle in the modal. Changing season preserves current biome and updates allowed weather; Esc closes gear before showcase. Evidence: `showcase.tsx` and studio settings.
- UI-23: (공개 미리보기) Public preview and drawing-board data remain server-sanitized even when the studio uses optimistic state. Preview never inherits private fields or actual permissions from a simulated role.
- UI-24: (겹창 닫기) Use existing overlay history/focus primitives for nested windows. Do not let closing an inner overlay navigate out of preview. If close triggers refresh/navigation, coordinate history unwinding first; simultaneous history.back can cancel navigation. Evidence: `lib/ui/overlay-pop.ts`.
- UI-25: (로딩·회복) Loading/empty/error states retain position and explain recovery. Never expose backend errors verbatim. Keep related controls close and gate only the action actually unavailable.
- UI-26: (그림판) Drawing board has no persistent drawing storage. Keep schedule layer structural, existing undo/redo and pen/touch semantics; consult [inking research](broadcast-panel-inking-research.md) and [tools QA](broadcast-tools-qa-checklist.md) for those changes.
- UI-36: (저장 중·나가기 경고) Server revalidation must not overwrite optimistic state while writes are in flight. Derive `beforeunload` warnings from actual pending operation counts, never a fixed timer. Reason: stale responses and delayed saves must not erase the latest edit or produce false leave warnings. Evidence: `components/studio/studio-shell.tsx`.

## Motion and ambient controls

- UI-27: (생동감 있는 동작·햅틱) Use immediate pressed feedback, shared transitions/springs, meaningful enter/exit and `hapticTick` on toggles/selectors/confirms; an action with press and server-confirm stages has two ticks. Chart hover values stay centered and clamped to their available bounds. Honor the in-app `html[data-reduce-motion]` state. The “생동감 있는 동작” switch defaults on; storage `vic.reduceMotion` keeps its inverse meaning. Do not independently seed/gate motion from the OS media query.
- UI-28: (생동감 있는 동작·배경 존재) “생동감 있는 동작” controls movement, not existence. When motion is off, ambient stays as a baked still frame if enabled. Its sole visibility preference is the ambient mode. Transparency selectors containing `:not([data-gfx="soft"])` must not also carry `:not([data-reduce-motion])` unless the block actually starts an animation; otherwise opaque UI hides the still canvas. No duplicate off option in the gfx selector. Evidence: `scene-engine.ts stillFrame` (dt=0 initialization and bounded late-asset retries) and `StudioSettingsList`.
- UI-29: (좌우 전환·튐) An element that crosses sides needs side-keyed animation names so CSS restarts the transition. Delayed both-filled animations must have identical direction-free held values; otherwise repeated toggles snap. Repeated-toggle measurement is below. Evidence: rail/avatar keyframes.
- UI-30: (계절 진입·편집 집중) Season entry animation uses backwards fill; both can pin opacity and defeat dim/showcase rules. Editor-open/drag focus dim affects only background opacity and work rate, never text or a blur filter. Ambient runtime rules are in ENGINE_RULES.
- UI-31: (미디어 일시정지) Heavy VOD/insights/studio modals pause ambient through `lib/ui/ambient-pause.ts`; settings are exempt for live inspection. Do not keep a full-screen animation behind blur/iframe media. Use events/ResizeObserver for poster follow/measure logic, not always-running rAF.
- UI-32: (오늘·업 도움·기간 안내) Today is marked on the full date label with the accepted gold tile and fading ends. Keep date-header height fixed, digits readable and selection ring above support/period bands. The 12px fade, 1.3em digits, 21px/18px badge and 8 > 7 > 6 layering contracts are below. Do not restore hard pills, elliptical radial glows or corner hairlines. Evidence: `--z-cal-select-ring`, `.d-num`, `.studio-day-head`.
- UI-33: (시청자 캡처·내보내기 은퇴) Public poster capture surfaces contain content, not editing/login/unlock controls. In-app PNG export and stickers are retired; do not recreate them or preserve constraints solely for removed sticker coordinates. Preserve current desktop geometry and explicit visual contracts when changing layout.

## Analytics UI

- UI-34: (이용 기록·장소) An activity area is a place, never a device or generic grouping. Use `AREA_ORDER`/`lib/activity/labels.ts` and registered `data-act` identifiers. Do not infer event ids from user-facing text or titles. Reason: it leaks content or merges unrelated actions.
- UI-35: (공개 인사이트) Public insights remain aggregate. Internal diagnostics use existing role/data boundaries; unknown roles stay unknown in records while authorization remains closed. Display partial/current periods as partial rather than comparing them to completed periods as equal.

## 검수 조건 — 수치와 측정 절차

These are acceptance contracts recovered from the original CLAUDE rules, not claims that they were remeasured on 2026-09-09. **Changing owner-confirmed visual values below requires owner judgment**; preserve the current value while preparing evidence for that decision. Code remains the numeric source for implementation, so do not create independent runtime copies in another component. Historical failed values are evidence, never fallback designs. Camera contracts are in [ENGINE_RULES](../ambient/ENGINE_RULES.md).

| Rule | Acceptance and measurement | Numeric source / scope |
|---|---|---|
| UI-05 | With 계절 배경 **ON**, compare eye-comfort OFF/ON over the same calendar region and fixture: **≥50% of calendar-area pixels change**, with blue-channel change **≈−8** as the recorded reference, not an invented exact tolerance. Computed **filter, opacity and mix-blend-mode** of `<html>`, `.gs-season` and its canvas are identical in both states. The background must look as if eye comfort were off while the glass UI visibly changes. | `app/globals.css` `--glass-cell/head/panel`; `lib/ui/motion.ts` `eyeComfortEnabled`. Test both `data-eye-comfort="1"` and `"lite"` palette routes when affected. |
| UI-10 | The web account card matches rail width **`20vw - 16px`** via `--rail-w`; content-fixed save/role/logout columns and remaining-width preview use **`auto auto minmax(0, 1fr) auto`**. Compare card width and edge before/after save/role changes: no jump. Preview navigation uses measured `--pv-w`/`--pv-mr`, corrected for zoom. | `components/studio/studio-calm-layer.css` `.studio-role-tools`; `components/studio/studio-shell.tsx` preview rail measurement. Save time is not restricted to the historical tooltip-only presentation; UI-10 is current. |
| UI-11 | The web tag filter remains **≥132px** (title + two chip rows); the empty avatar space uses **`flex: 0 4 58%`**, yielding first. At height **≤620px**, tools become icon-only and avatar basis becomes **45%**; at **≤470px**, the avatar space is hidden. Sweep **900→380px height at 1147px width**: filter title stays inside its card, filter ≥132px, rail overflow **0**. | `components/studio/studio-shell.css` `.avatar-rail-filter` / `.avatar-slot`; `studio-calm-layer.css` height media queries. Applies when the web topology is active, not to agenda. |
| UI-11 | With the editor open, inspect `.bottom-float-row`: it moves aside without intersection; when workspace is forced to **500px**, the no-room case fades below the popover and `elementFromPoint` reaches the popover. Closing restores the natural position. | [Chrome compaction measurement procedure](chrome-compaction-manual.md); `placeEditorPopover` and bottom-row dodge. The 500px case is a QA fixture, not a layout breakpoint. |
| UI-12 | Sweep **1600→1000px width** for owner and developer: header overflow, overlap and unwanted second rows are **0**. Tiers **0–3** come from rendered measurements, not memorized threshold widths. Enter/leave preview, then resize: the remounted shell must be observed; no measurement on disconnected or **0×0** nodes. | `components/studio/studio-shell.tsx` `chromeTier`, `shellEl`, `overflows`; viewer `data-pchrome` follows the same measured-overflow principle. Historic tier changes at one viewport are observations, not fixed cutoffs. |
| UI-14 | Viewer control **cells 36px**, outer cards and central trio **44px**, body type **13.5px**; preserve measured widths when changing padding/type. Avatar has **3** continuously available states; from OFF select right in **1 click**, and compare all state widths for **0** change. | `components/poster/public-poster.css` account/preview/avatar segment rules; `app/globals.css` `--text-body`; `public-poster.tsx` avatar radiogroup. Width stability does not prohibit intentional responsive tier changes. |
| UI-15 | A visible but ambiguous action gets an effect tooltip of **about 20 characters or fewer**, with no parenthetical aside. Inspect expanded and folded chrome separately: names appear as tooltips only while folded; visible self-explanatory labels produce **0** duplicate tooltips. | `data-tip` selectors and aria-label; one `.lbl` per control. |
| UI-16 | Desktop calendar slots: band icons **13px**, heart **14px**, play **11px**. Keep host-colored lucide strokes, no white ring/drop shadow. | `components/poster/public-poster.tsx` calendar `Pin`/`Sprout`/`Heart`/`Play` JSX. Existing agenda play glyphs use **13px** in code; that is a distinct topology, not permission to change the accepted desktop sizes. |
| UI-29 | Toggle left/right **5 times** and inspect delayed held frames: side changes introduce **0px positional snap**. `-l`/`-r` names restart animation, but both hold the same direction-free scale/opacity. The rejected **±6px** directional holds produced **12px** jumps; do not restore them. | Studio/poster rail and avatar keyframes. Measure the discontinuity introduced by the held value, not legitimate interpolated movement. |
| UI-32 | The full **“n 일”** label sits on a gold tile with horizontal pixel stops: transparent → gold at **12px** → gold until **12px** before the end → transparent. Keep **1px** fading top sheen, **3px** same-hue bloom and **`padding: 0 11px`**. No hairline/border/drop-shadow shape; the existing CSS `box-shadow` is the specified soft bloom. | `components/poster/public-poster.css` `.public-day.today .day-strip strong`; `components/studio/studio-shell.css` `.studio-day.today .studio-day-head strong`. The top sheen currently reaches its plateau at **14px**; the accepted **12px** contract refers to the gold fade. |
| UI-32 | Every poster date's `.d-num` is **1.3em**, including non-today dates. Today badge height is **21px poster / 18px studio** and must not increase the date-header height or move band lanes/cards. Compare today and neighboring date-header geometry. | `.day-strip .d-num`, the two today selectors above. The rejected **1.12em** and legacy **12.5px** white-chip digits are history. |
| UI-32 | The today/selection ring uses **`--z-cal-select-ring: 8`**, above ribbon head **7** and ribbon **6**. Render today with a crossing 업 도움/기간 안내 band: the ring remains visible over the band; the full date label remains outside band lanes. | `app/globals.css` `--z-cal-select-ring` / `--z-cal-ribbon-head` / `--z-cal-ribbon`; studio/poster ring pseudo-elements. A numeric ordering alone is not a rendered overlap check. |

Engineering-owned values are still traceable: `lib/ui/breakpoints.ts` owns **640px** device checks, **999px** studio agenda and **1040px** poster agenda plus the **max-height 640px / coarse-pointer** alternative. The original blanket “641px means web” cannot override those topology queries. `lib/ui/motion.ts:SETTINGS_EPOCH` owns the one-time **2026-09-04** reset; the historical statement that all four preferences default ON is superseded for ambient (**OFF**, AMB-12) and calm is no longer a switch (UI-04). These distinctions preserve current behavior rather than reactivating old migration instructions.

For a change, select relevant rules and rendered checks. Do not reread historical archives as a substitute for current code.
