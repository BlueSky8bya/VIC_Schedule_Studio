# Export Rules

- Official poster PNG generation uses Playwright against the public poster route.
- Browser clipboard export is only a convenience feature.
- Export surfaces must use `[data-export-surface]`.
- Poster mode must not render Studio navigation, private-layer badges, edit controls, or unlock controls.
